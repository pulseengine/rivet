artifacts:
  - id: REQ-001
    type: requirement
    title: Text-file-first artifact management
    status: approved
    description: >
      All lifecycle artifacts are stored as YAML files in git repositories.
      No database or external service required for core operation.
    tags: [core, hook-test]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-002
    type: requirement
    title: STPA artifact support
    status: approved
    description: >
      Support STPA methodology artifacts: losses, hazards, sub-hazards,
      system constraints, control structure, UCAs, controller constraints,
      and loss scenarios with proper link semantics.
    tags: [stpa, safety]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-003
    type: requirement
    title: Automotive SPICE V-model traceability
    status: approved
    description: >
      Support full ASPICE V-model artifact types from stakeholder requirements
      through system/software requirements, architecture, design, and testing
      with bidirectional traceability rules.
    tags: [aspice, automotive]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-004
    type: requirement
    title: Validation engine
    status: approved
    description: >
      Validate link integrity, coverage completeness, required fields,
      allowed values, and traceability rules. Report diagnostics with
      severity levels (error, warning, info).
    tags: [core, validation]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-1
      - type: satisfies
        target: SC-3
      - type: satisfies
        target: SC-15
      - type: constraint-satisfies
        target: SC-1
      - type: constraint-satisfies
        target: SC-3
      - type: constraint-satisfies
        target: SC-15
      - type: constraint-satisfies
        target: SC-LSP-002
      - type: constraint-satisfies
        target: SC-LSP-004
      - type: constraint-satisfies
        target: SC-IMPL-001
      - type: constraint-satisfies
        target: SC-IMPL-004
      - type: constraint-satisfies
        target: SC-IMPL-006
      - type: constraint-satisfies
        target: CC-C-1
      - type: constraint-satisfies
        target: CC-C-2
      - type: constraint-satisfies
        target: CC-C-3
      - type: constraint-satisfies
        target: CC-C-4
      - type: constraint-satisfies
        target: CC-C-5
      - type: constraint-satisfies
        target: CC-C-6
      - type: constraint-satisfies
        target: CC-C-7
      - type: constraint-satisfies
        target: CC-C-8
      - type: constraint-satisfies
        target: CC-C-9
      - type: constraint-satisfies
        target: CC-C-24
      - type: constraint-satisfies
        target: CC-C-25
      - type: constraint-satisfies
        target: CC-D-3
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-005
    type: requirement
    title: ReqIF 1.2 import/export
    status: approved
    description: >
      Support OMG ReqIF 1.2 XML format for cross-organization requirements
      interchange. Import ReqIF into trace artifacts, export trace artifacts
      to ReqIF.
    tags: [interchange, reqif]
    fields:
      priority: should
      category: interface
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-4
      - type: satisfies
        target: SC-9
      - type: constraint-satisfies
        target: SC-4
      - type: constraint-satisfies
        target: SC-9
      - type: constraint-satisfies
        target: CC-Q-1
      - type: constraint-satisfies
        target: CC-Q-2
      - type: constraint-satisfies
        target: CC-Q-3
      - type: constraint-satisfies
        target: CC-Q-4
      - type: constraint-satisfies
        target: CC-Q-5
      - type: constraint-satisfies
        target: CC-Q-6
      - type: constraint-satisfies
        target: CC-Q-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-006
    type: requirement
    title: OSLC-based tool synchronization
    status: approved
    description: >
      Bidirectional sync with Polarion, DOORS, and codebeamer via OSLC
      (Open Services for Lifecycle Collaboration) rather than per-tool
      REST adapters. Support RM, QM, CM domains.
    tags: [oslc, sync, interchange]
    fields:
      priority: should
      category: interface
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-5
      - type: satisfies
        target: SC-8
      - type: constraint-satisfies
        target: SC-5
      - type: constraint-satisfies
        target: SC-8
      - type: constraint-satisfies
        target: CC-O-1
      - type: constraint-satisfies
        target: CC-O-2
      - type: constraint-satisfies
        target: CC-O-3
      - type: constraint-satisfies
        target: CC-O-4
      - type: constraint-satisfies
        target: CC-O-5
      - type: constraint-satisfies
        target: CC-O-6
      - type: constraint-satisfies
        target: CC-O-7
      - type: constraint-satisfies
        target: CC-O-8
      - type: constraint-satisfies
        target: CC-O-9
      - type: constraint-satisfies
        target: CC-O-10
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-007
    type: requirement
    title: CLI and serve pattern
    status: approved
    description: >
      Provide a CLI binary for validation, querying, and reporting.
      Support an HTTP serve mode (axum + HTMX) for dashboard visualization.
      No Tauri or desktop GUI.
    tags: [core, ui]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0
    links:
      - type: satisfies
        target: SC-18
      - type: constraint-satisfies
        target: SC-18
      - type: constraint-satisfies
        target: SC-22
      - type: constraint-satisfies
        target: SC-IMPL-003
      - type: constraint-satisfies
        target: CC-L-1
      - type: constraint-satisfies
        target: CC-L-2
      - type: constraint-satisfies
        target: CC-L-3
      - type: constraint-satisfies
        target: CC-L-4
      - type: constraint-satisfies
        target: CC-L-5
      - type: constraint-satisfies
        target: CC-D-1
      - type: constraint-satisfies
        target: CC-D-2
      - type: constraint-satisfies
        target: CC-D-4
      - type: constraint-satisfies
        target: CC-C-27
      - type: constraint-satisfies
        target: CC-M-1
      - type: constraint-satisfies
        target: CC-M-2
      - type: constraint-satisfies
        target: CC-M-3
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-008
    type: requirement
    title: WASM component adapters
    status: approved
    description: >
      Support pluggable adapters as WASM components via WIT interface.
      Adapters can import/export artifacts for custom formats.
    tags: [extensibility, wasm]
    fields:
      priority: should
      category: functional
      baseline: v0.1.0
    links:
      - type: satisfies
        target: SC-16
      - type: constraint-satisfies
        target: SC-16
      - type: constraint-satisfies
        target: CC-C-21
      - type: constraint-satisfies
        target: CC-C-22
      - type: constraint-satisfies
        target: CC-C-23
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-009
    type: requirement
    title: Test results as release evidence
    status: approved
    description: >
      Tie test execution results (JUnit XML, coverage reports) to
      GitHub releases as per-version evidence. Support fetching and
      caching from release assets.
    tags: [testing, evidence]
    fields:
      priority: should
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-6
      - type: constraint-satisfies
        target: SC-6
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-010
    type: requirement
    title: Schema-driven validation
    status: approved
    description: >
      Artifact types, fields, link constraints, and traceability rules
      are defined in mergeable YAML schemas. The tool validates artifacts
      against loaded schemas.
    tags: [core, schema]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-1
      - type: constraint-satisfies
        target: SC-IMPL-005
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-011
    type: requirement
    title: Rust edition 2024 with MSRV 1.85
    status: approved
    description: >
      Use Rust edition 2024. Maintain minimum supported Rust version of 1.85.
    tags: [toolchain]
    fields:
      priority: must
      category: constraint
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-012
    type: requirement
    title: Comprehensive CI quality gates
    status: approved
    description: >
      CI pipeline must include fmt, clippy -D warnings, tests, security audit,
      cargo-deny, code coverage, Miri, proptest, supply chain verification,
      and MSRV check.
    tags: [ci, quality]
    fields:
      priority: must
      category: non-functional
      baseline: v0.1.0
    links:
      - type: constraint-satisfies
        target: CC-I-1
      - type: constraint-satisfies
        target: CC-I-2
      - type: constraint-satisfies
        target: CC-I-3
      - type: constraint-satisfies
        target: CC-I-4
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-013
    type: requirement
    title: Performance benchmarks with KPI baselines
    status: approved
    description: >
      Criterion benchmarks for core operations (store, link graph, validation,
      matrix) at multiple artifact scales (100, 1000, 10000). Results serve
      as performance KPI baselines and regression detection.
    tags: [testing, performance]
    fields:
      priority: must
      category: non-functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-014
    type: requirement
    title: ASPICE SWE.4/5/6 test structure
    status: approved
    description: >
      Test suite organized to mirror ASPICE verification levels.
      SWE.4 unit verification (proptest, Miri), SWE.5 integration
      verification (cross-module tests), SWE.6 qualification testing
      (full pipeline validation, benchmark KPIs).
    tags: [testing, aspice]
    fields:
      priority: must
      category: non-functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-015
    type: requirement
    title: ASPICE 4.0 aligned schemas
    status: approved
    description: >
      Schema definitions must align with Automotive SPICE v4.0 process
      reference model. SWE.5 is "Software Component Verification and
      Integration Verification", SWE.6 is "Software Verification".
      Use "verification measure" terminology instead of "test".
      Expanded method values (static-analysis, formal-verification,
      simulation, inspection, walkthrough). Verification-criteria field
      on requirement types.
    tags: [aspice, schema]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-4
      - type: constraint-satisfies
        target: SC-4
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-016
    type: requirement
    title: Cybersecurity schema (ISO 21434 / ASPICE SEC.1-4)
    status: approved
    description: >
      Support automotive cybersecurity engineering artifacts aligned with
      ISO/SAE 21434 and ASPICE v4.0 SEC.1-4 processes. Cover TARA
      (assets, threats, risk assessment), cybersecurity goals/requirements,
      cybersecurity design, implementation, and verification.
    tags: [cybersecurity, aspice, schema]
    fields:
      priority: should
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-4
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-017

    type: requirement
    title: Commit-to-artifact traceability
    status: approved
    description: >
      The system must parse git commit trailers and link them to artifacts
      at runtime, injecting ephemeral commit nodes into the link graph.
      Must support configurable trailer-to-link-type mapping and produce
      five report types: linked commits, broken references, orphan commits,
      artifact commit coverage, and unimplemented artifacts.
    tags: [core, traceability, git]
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-7
      - type: satisfies
        target: SC-17
      - type: constraint-satisfies
        target: SC-7
      - type: constraint-satisfies
        target: SC-17
      - type: constraint-satisfies
        target: CC-C-18
      - type: constraint-satisfies
        target: CC-C-19
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-018
    type: requirement
    title: Commit message validation at commit time
    status: approved
    description: >
      The system must validate commit messages via a pre-commit hook,
      ensuring non-exempt commits reference at least one valid artifact
      ID in git trailers. Must support conventional-commit type exemptions
      and an explicit skip trailer for opt-out.
    tags: [core, validation, git]
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-019
    type: requirement
    title: Orphan commit detection
    status: approved
    description: >
      The system must identify commits that modify files in configured
      traced paths without referencing any artifact. Must support
      path-based configuration for traced vs. exempt directories and
      an artifact whitelist for known-unimplemented items.
    tags: [core, traceability, git]
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-020
    type: requirement
    title: Cross-repository artifact linking via prefixed IDs
    status: approved
    description: >
      Rivet projects must be able to declare external dependencies on other
      rivet repositories and reference their artifacts using prefix:ID syntax.
    tags: [cross-repo, traceability]
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-10
      - type: satisfies
        target: SC-19
      - type: constraint-satisfies
        target: SC-10
      - type: constraint-satisfies
        target: SC-19
      - type: constraint-satisfies
        target: CC-C-20
      - type: constraint-satisfies
        target: CC-L-6
      - type: constraint-satisfies
        target: CC-L-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-021
    type: requirement
    title: Distributed baselining via convention tags
    status: approved
    description: >
      Multiple rivet repositories must be able to form consistent baselines
      using git tags without requiring a central platform repository.
    tags: [cross-repo, baseline]
    fields:
      priority: should
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-10
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-022
    type: requirement
    title: Single-binary WASM asset embedding
    status: draft
    description: >
      The rivet binary must optionally embed all WASM and JavaScript assets
      so it can be distributed as a single self-contained executable.
    tags: [packaging, wasm]
    fields:
      priority: should
      category: functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-023
    type: requirement
    title: Conditional validation rules
    status: approved
    description: >
      The validation engine must support conditional rules where field
      requirements or link cardinality depend on the value of another field.
      For example, an artifact with status "approved" must have a non-empty
      verification-criteria field, or an artifact with safety level ASIL_B
      must have mitigated_by links. This enables safety-critical constraint
      enforcement that static per-type validation cannot express.
      Contradictory conditional rules must be detected at schema load time.
    tags: [validation, schema, safety]
    links:
      - type: satisfies
        target: SC-12
      - type: constraint-satisfies
        target: SC-12
      - type: constraint-satisfies
        target: CC-C-12
    fields:
      priority: should
      category: functional
      upstream-ref: "eclipse-score/docs-as-code#180"
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-024
    type: requirement
    title: Change impact analysis
    status: approved
    description: >
      The system must detect which artifacts changed between two baselines
      or commits and compute the transitive set of downstream artifacts
      affected via the link graph. This supports change management workflows
      required by ASPICE SUP.10 and ISO 26262 part 8.
    tags: [traceability, baseline, safety]
    fields:
      priority: should
      category: functional
      upstream-ref: "eclipse-score/docs-as-code#314, eclipse-score/process_description#535"
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-3
      - type: constraint-satisfies
        target: SC-IMPL-002
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-025
    type: requirement
    title: sphinx-needs JSON import
    status: approved
    description: >
      The system must import artifacts from the sphinx-needs needs.json
      export format, mapping sphinx-needs types, links, and fields to
      rivet schema types via configurable mappings. This provides a
      migration path for projects using sphinx-needs-based toolchains.
    tags: [interchange, adapter, migration]
    fields:
      priority: should
      category: interface
      upstream-ref: "eclipse-score/score#1695"
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-4
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-026
    type: requirement
    title: Test-to-requirement traceability extraction
    status: approved
    description: >
      The system must extract traceability markers from test source code
      and test results, linking test cases to requirements without requiring
      manual YAML maintenance. Must support language-specific markers
      (attributes, decorators, comments) and test result formats (JUnit XML,
      cargo test JSON).
    tags: [testing, traceability, automation]
    fields:
      priority: should
      category: functional
      upstream-ref: "eclipse-score/score#2521, eclipse-score/score#2619"
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-027

    type: requirement
    title: Build-system-aware cross-repo discovery
    status: approved
    description: >
      The system must discover cross-repo dependencies from build system
      manifests (Bazel MODULE.bazel, Nix flake.lock, or custom manifests)
      rather than requiring manual external declarations. This includes
      resolving pinned commits, workspace paths, and source code locations
      across the dependency graph for traceability validation and source
      code linking.
    tags: [cross-repo, bazel, nix, traceability]
    fields:
      priority: should
      category: functional
      upstream-ref: "eclipse-score/reference_integration (known_good.json)"
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-10
      - type: constraint-satisfies
        target: SC-20
      - type: constraint-satisfies
        target: CC-C-15
      - type: constraint-satisfies
        target: CC-C-16
      - type: constraint-satisfies
        target: CC-C-17
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-028
    type: requirement
    title: Diagnostic-quality parsing with lossless syntax trees
    status: approved
    description: >
      All parsers for build-system manifests and configuration files must
      produce lossless concrete syntax trees (CST) with full span information
      for byte-exact error reporting. Parsers must recover from errors and
      produce partial results rather than failing completely. Uses rowan
      for CST representation, consistent with the spar AADL toolchain.
    tags: [parsing, rowan, diagnostics]
    links:
      - type: satisfies
        target: SC-13
      - type: constraint-satisfies
        target: SC-13
      - type: constraint-satisfies
        target: SC-LSP-001
      - type: constraint-satisfies
        target: SC-LSP-005
    fields:
      priority: must
      category: non-functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-029
    type: requirement
    title: Incremental validation via dependency-tracked computation
    status: approved
    description: >
      The validation pipeline must support incremental recomputation where
      changing a single artifact file only re-evaluates affected validation
      rules, link graph edges, and coverage computations. Uses salsa for
      dependency tracking, consistent with the spar AADL toolchain. This
      enables sub-millisecond revalidation for IDE integration and efficient
      conditional rule evaluation. Incremental results must be identical
      to full validation results for the same inputs.
    tags: [validation, salsa, incremental, performance]
    links:
      - type: satisfies
        target: SC-11
      - type: constraint-satisfies
        target: SC-11
      - type: constraint-satisfies
        target: SC-21
      - type: constraint-satisfies
        target: SC-LSP-003
      - type: constraint-satisfies
        target: SC-LSP-006
      - type: constraint-satisfies
        target: SC-LSP-007
      - type: constraint-satisfies
        target: CC-C-10
      - type: constraint-satisfies
        target: CC-C-11
      - type: constraint-satisfies
        target: CC-C-13
      - type: constraint-satisfies
        target: CC-C-14
      - type: constraint-satisfies
        target: CC-C-26
    fields:
      priority: must
      category: non-functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-031
    type: requirement
    title: Schema-validated artifact mutation from CLI
    status: approved
    description: >
      The CLI must provide commands to create, modify, remove, link, and
      unlink artifacts directly, with full schema validation at write time.
      All mutations must validate the artifact ID is unique (or exists for
      modify), the type exists in the loaded schema, required fields are
      present, link targets exist, link types are valid for the source and
      target types, and status values are in the allowed set. The CLI must
      write valid YAML that preserves existing file formatting and comments
      where possible. This eliminates the need for external agents or manual
      YAML editing to maintain artifacts correctly.
    tags: [cli, mutation, validation, safety]
    links:
      - type: satisfies
        target: SC-1
      - type: satisfies
        target: SC-2
      - type: constraint-satisfies
        target: SC-2
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-030
    type: requirement
    title: Formal correctness guarantees for validation engine
    status: draft
    description: >
      Core validation algorithms must have formal correctness proofs at
      three levels. Bounded model checking (Kani) for panic freedom.
      Functional correctness proofs (Verus) for validation soundness
      and completeness. Metamodel semantic proofs (Rocq/coq-of-rust)
      for schema satisfiability and rule consistency. These proofs
      serve as ISO 26262 tool qualification evidence at TCL 1.
      Proofs must verify the actual implementation, not a separate model.
    tags: [formal-verification, safety, tool-qualification]
    links:
      - type: satisfies
        target: SC-14
      - type: constraint-satisfies
        target: SC-14
    fields:
      priority: should
      category: non-functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-032
    type: requirement
    title: Markdown rendering in artifact descriptions
    status: approved
    description: >
      Artifact descriptions rendered as CommonMark HTML via pulldown-cmark with tables, strikethrough, task lists, and code blocks.
    tags: [rendering, markdown]
    fields:
      category: functional
      priority: should
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-033
    type: requirement
    title: Rich artifact embedding in documents with schema-driven link traversal
    status: approved
    description: >
      Document embeds support modifiers (full, links, upstream, downstream, chain, table) with schema-driven link traversal.
    tags: [documents, embedding, traceability]
    fields:
      category: functional
      priority: should
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-034
    type: requirement
    title: YAML 1.2.2 spec-compliant artifact file editing
    status: approved
    description: >
      YAML artifact file editing uses indentation-aware parser per YAML 1.2.2 spec for lossless modification.
    tags: [yaml, parsing, spec-compliance]
    fields:
      category: functional
      priority: must
      baseline: v0.2.0-dev
    links:
      - type: satisfies
        target: SC-2
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-035
    type: requirement
    title: HTML export includes rendered documents with resolved embeds
    status: approved
    description: >
      HTML export includes rendered documents with resolved wiki-links and artifact embeds as static pages.
    tags: [export, documents]
    fields:
      category: functional
      priority: must
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-036
    type: requirement
    title: HTML export supports version switcher and homepage link
    status: approved
    description: >
      HTML export supports runtime config.js for version switcher dropdown and homepage back-link.
    tags: [export, navigation, versioning]
    fields:
      category: functional
      priority: should
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-037
    type: requirement
    title: SysML v2 artifact import
    status: draft
    description: >
      Import requirement and constraint elements from SysML v2 textual notation
      files (.sysml) as rivet artifacts. Parse SysML v2 using a rowan-based
      lossless parser (same pattern as spar's AADL parser). Extract requirement
      elements with their attributes, rationale, and satisfy/verify relationships,
      mapping them to rivet artifact YAML.
    tags: [sysml, adapter, future]
    fields:
      priority: should
      category: functional
      baseline: v0.3.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-038
    type: requirement
    title: SysML v2 to AADL model lowering
    status: draft
    description: >
      Lower SysML v2 system models to AADL execution platform models where
      timing, scheduling, and resource binding semantics exist. Implements
      the mapping rules specified by SEI's "Extending SysML v2 with AADL
      Concepts" research. Enables multi-level architecture analysis:
      SysML v2 (system-level) to AADL (deployment-level) to implementation.
    tags: [sysml, aadl, architecture, future]
    fields:
      priority: could
      category: functional
      baseline: v0.3.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-040
    type: requirement
    title: Test report lifecycle integration
    status: draft
    description: >
      CI test runs produce structured results (JUnit XML) that are imported
      into rivet as test result artifacts. The workflow is: run tests, import
      results via rivet import --format junit, create a test report artifact
      in results/, conformance review, commit report as PR, include in
      release. Test evidence becomes part of the traceability chain — rivet
      dogfoods its own test tracking.
    tags: [testing, ci, dogfood, future]
    fields:
      priority: should
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-039
    type: requirement
    title: Draft-aware validation severity
    status: draft
    description: >
      Validation diagnostics for traceability rules (missing required links,
      incomplete coverage) must respect artifact maturity. Artifacts with
      status "draft" produce info-level diagnostics for unfulfilled links
      instead of errors. Only "active" or "approved" artifacts are held to
      full traceability enforcement. This distinguishes "not yet done" from
      "broken" in projects with work-in-progress artifacts.
    tags: [validation, workflow, core]
    fields:
      priority: must
      category: functional
      baseline: v0.3.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-041
    type: requirement
    title: S-expression query and constraint language
    status: draft
    description: >
      The system must provide a single s-expression-based language for
      artifact filtering, traceability queries, feature model constraints,
      and variant-scoped validation. The language must support predicates
      over artifact fields (type, status, tags, custom fields), logical
      connectives (and, or, not, implies, excludes), quantifiers (forall,
      exists) over artifact sets, link traversal predicates (linked-by,
      linked-from, reachable-from), and aggregation (count, ratio). The
      same syntax must be usable in CLI --filter flags, schema traceability
      rules, feature model constraints, and binding validation rules. No
      alternative syntax or sugar — one canonical form.
    tags: [core, query, s-expression]
    links:
      - type: constraint-satisfies
        target: SC-VAR-004
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-042
    type: requirement
    title: Feature model for product line engineering
    status: draft
    description: >
      The system must support FODA-style feature models that describe the
      logical problem space of a product line. Feature models define a
      tree of features with group types (mandatory, optional, alternative,
      or) and cross-tree constraints expressed as s-expressions (implies,
      excludes, requires). Feature models are stored as YAML artifacts
      separate from implementation artifacts, maintaining the separation
      between logical (problem space) and implementation (solution space)
      concerns. Feature models must be validatable independently — the
      constraint set must be satisfiable.
    tags: [variant, ple, feature-model]
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-043
    type: requirement
    title: Variant configuration with constraint solving
    status: draft
    description: >
      The system must support named variant configurations that select a
      subset of features from a feature model. A constraint solver must
      propagate implications (selecting feature A forces feature B via
      constraints), detect conflicts (mutually exclusive selections), and
      validate that the final feature set satisfies all cross-tree
      constraints. Invalid configurations must produce clear diagnostics
      identifying which constraints are violated. The solver must use
      propositional logic (boolean SAT) at minimum, with optional
      first-order extensions (forall/exists over feature sets) for
      advanced constraints.
    tags: [variant, ple, constraint-solver]
    links:
      - type: constraint-satisfies
        target: SC-VAR-002
      - type: constraint-satisfies
        target: SC-VAR-006
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-044
    type: requirement
    title: Feature-to-artifact binding model
    status: draft
    description: >
      The system must support a binding model that maps features from the
      logical feature model to implementation artifacts and source code
      globs. Bindings are stored in separate YAML files, maintaining the
      three-layer separation (feature model / variant config / binding).
      Each feature can bind to zero or more artifact IDs and zero or more
      source file glob patterns. Validation must detect: unbound features
      (feature with no artifacts), unbound artifacts (artifacts not mapped
      to any feature), and stale bindings (referencing deleted artifacts
      or non-existent source paths).
    tags: [variant, ple, binding]
    links:
      - type: constraint-satisfies
        target: SC-VAR-003
      - type: constraint-satisfies
        target: SC-VAR-005
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-045
    type: requirement
    title: Per-variant validation and coverage
    status: draft
    description: >
      The system must validate traceability and coverage independently for
      each variant configuration. Given a variant, the system derives the
      effective feature set, collects all bound artifacts, and validates
      that traceability chains are complete within that subset. Coverage
      reports must be per-variant. An --all-variants mode must validate
      every defined variant independently and report per-variant results.
      A variant that passes validation when the union of all artifacts
      passes must still fail if its own subset has broken traceability.
    tags: [variant, validation, coverage]
    links:
      - type: constraint-satisfies
        target: SC-VAR-001
      - type: constraint-satisfies
        target: SC-VAR-007
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-046
    type: requirement
    title: Variant-scoped CLI and API commands
    status: draft
    description: >
      All relevant CLI commands must accept --variant flags to scope
      operations to a specific product configuration. This includes
      validate, coverage, matrix, list, stats, impact, export, and the
      dashboard API endpoints. The --variant flag composes with existing
      --type and --status filters. rivet validate without --variant
      validates the union (all artifacts). rivet validate --variant X
      validates only the subset bound to variant X's features.
    tags: [variant, cli, api]
    fields:
      priority: should
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-047
    type: requirement
    title: MCP mutation audit logging
    status: draft
    description: >
      Every MCP write operation (rivet_add, rivet_modify, rivet_link,
      rivet_unlink, rivet_remove) must produce a structured JSON log entry
      containing: tool name, artifact ID, fields changed, old value,
      new value, timestamp, and MCP session ID. The log must be append-only
      and written to a configurable path (default: .rivet/mcp-audit.jsonl).
      This enables forensic analysis of AI agent activity and rollback of
      unintended changes. Without audit logging, a prompt-injected agent's
      modifications cannot be detected or reversed.
    tags: [mcp, security, audit, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-005
      - type: constraint-satisfies
        target: SSC-TQ-002
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-048
    type: requirement
    title: Regex complexity bounds in s-expression evaluator
    status: draft
    description: >
      The matches predicate in the s-expression evaluator must enforce
      a size limit on compiled regex patterns (via regex::RegexBuilder::
      size_limit) and a match timeout to prevent ReDoS attacks. Invalid
      or oversized patterns must produce a clear diagnostic rather than
      hanging. Default size limit: 1MB. Default match timeout: 1 second.
    tags: [security, s-expression, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-001
      - type: constraint-satisfies
        target: SSC-TQ-001
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-049
    type: requirement
    title: Export validation embedding
    status: draft
    description: >
      The Zola and HTML export commands must embed the validation result
      (PASS/FAIL, error count, warning count, timestamp, git commit) into
      the exported output. For Zola: written to data/<prefix>/validation.json.
      For HTML: embedded in the index page. Consumers can verify the export
      was generated from a validated project state. A --clean flag must
      remove stale content/<prefix>/ before writing to prevent deleted
      artifacts from persisting as published pages.
    tags: [export, validation, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-003
      - type: constraint-satisfies
        target: SSC-TQ-004
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-050
    type: requirement
    title: Import link-target verification
    status: draft
    description: >
      After importing artifacts from needs-json or ReqIF, the import
      command must verify that all link targets exist in the combined
      store (existing project + imported artifacts). Unresolved targets
      must be reported as warnings with the specific artifact ID and
      link type. A --strict flag must promote unresolved warnings to
      errors, blocking the import. This prevents crafted imports from
      creating traceability links to non-existent artifacts.
    tags: [import, validation, security, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-004
      - type: constraint-satisfies
        target: SSC-TQ-003
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-051
    type: requirement
    title: CI-enforced traceability (hooks are convenience only)
    status: draft
    description: >
      The CI pipeline must include rivet commits and rivet validate as
      required checks, independent of local git hooks. Git hooks (commit-msg,
      pre-commit) are convenience tooling that can be bypassed via
      --no-verify. The system documentation must explicitly state that
      hooks do not provide security assurance — only CI enforcement does.
      A rivet validate --check-hooks flag should report whether hooks are
      installed, but hook absence must not block local development.
    tags: [ci, hooks, security, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-006
      - type: constraint-satisfies
        target: SSC-TQ-005
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-052
    type: requirement
    title: Variant solver fuzz testing
    status: draft
    description: >
      The feature model constraint solver must be tested with randomly
      generated feature models (proptest) to verify: (a) solver never
      panics, (b) solver result is consistent with brute-force enumeration
      for small models (≤10 features), (c) every resolved variant satisfies
      all constraints when re-checked. This addresses the gap between the
      12 hand-written unit tests and the combinatorial space of real
      feature models.
    tags: [variant, testing, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-002
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-053
    type: requirement
    title: Quantifier scope correctness testing
    status: draft
    description: >
      The forall/exists evaluator must have tests that verify: adding an
      artifact to the store changes quantifier results, removing an artifact
      changes results, and the store parameter is the actual iteration set
      (not a cached or global set). These tests must use different store
      contents to confirm scoping, not just check the happy path.
    tags: [s-expression, testing, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-007
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-054
    type: requirement
    title: Independent verification for AI-generated tests
    status: draft
    description: >
      Every safety-critical module with AI-generated tests must also have proptest properties and/or Kani proofs verifying mathematical invariants.

    tags: [ai, testing, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: must
    links:
      - type: constraint-satisfies
        target: SC-AI-001
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-055
    type: requirement
    title: Human review gate for AI-generated STPA
    status: draft
    description: >
      AI STPA must remain draft until human domain expert reviews.
    tags: [ai, stpa, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: must
    links:
      - type: constraint-satisfies
        target: SC-AI-002
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-056
    type: requirement
    title: Semantic review checklist for bulk AI artifact creation
    status: draft
    description: >
      More than 5 artifacts in one session needs semantic review checklist.
    tags: [ai, review, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: should
    links:
      - type: constraint-satisfies
        target: SC-AI-003
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-057
    type: requirement
    title: Assumption documentation in AI-generated modules
    status: draft
    description: >
      AI-written modules must document runtime assumptions in headers.
    tags: [ai, documentation, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: should
    links:
      - type: constraint-satisfies
        target: SC-AI-004
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-058
    type: requirement
    title: Risk-proportional PR review for AI-generated code
    status: draft
    description: >
      Safety-critical paths need manual edge case checks.
    tags: [ai, review, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: must
    links:
      - type: constraint-satisfies
        target: SC-AI-005
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-059
    type: requirement
    title: AI provenance with model ID and session context
    status: draft
    description: >
      Provenance must include model ID, Co-Authored-By, and session refs.
    tags: [ai, provenance, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: must
    links:
      - type: constraint-satisfies
        target: SC-AI-006
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-084
    type: requirement
    title: "Verus proof CI must fail loudly when its Nix install fails — a continue-on-error proof job that verifies nothing is a silent failure"
    status: draft
    description: |
      The Verus Proofs CI job (.github/workflows/ci.yml) installs Nix
      via DeterminateSystems/nix-installer-action, then runs
      `bazel test //verus:rivet_specs_verify`. The job is
      `continue-on-error: true` — intentionally soft-gated, because
      Verus has an open SMT proof gap (ci.yml lines 555-558).

      On the self-hosted runner (`[self-hosted, linux, x64, lean-mem]`,
      NoNewPrivileges=true, no sudo) the `nix-installer` binary
      escalates via `sudo` and fails; the `Install Nix` step errors and
      `Verify Verus specs` is then SKIPPED. Because the job is
      `continue-on-error`, the workflow stays green. Net effect: the
      Verus proof job has been verifying nothing while reporting no
      blocking failure — the F2 silent-failure class. A proof job that
      is not proving anything is worse than a red one.

      Confirmed 2026-05-21 on PR #311 run 26246893086: the Verus job
      step sequence was `failure Install Nix` -> `skipped Verify Verus
      specs`.

      Pinning the action + `determinate: false` (shipped in v0.11.1)
      fixed the Rocq job on ubuntu-latest but cannot fix Verus: no
      version of `nix-installer-action` installs Nix without root. The
      durable fix is to run the Verus (and Rocq) Bazel/Nix jobs in a
      rootless `nixos/nix` container — Nix pre-installed in the image,
      no installer, no daemon, no sudo, no unpinned-action drift
      surface.

      Unblocker (2026-05-23): smithy runner9 is now podman-capable —
      `podman_userns: true`, `NoNewPrivileges=0`,
      `ProtectKernelTunables=false` deployed and verified live
      (`podman run nixos/nix@sha256:fd7a5c67... nix --version` returns
      `nix (Nix) 2.24.9`). GHA label set is
      `[self-hosted, Linux, X64, hetzner, rust-cpu, podman]`. The
      Verus job can switch `runs-on:` to include the `podman` label
      and drive the Bazel/Nix work through rootless podman against
      `docker.io/nixos/nix`; per the smithy note, "the flag is one
      line per runner — trivial to expand" if contention on runner9
      becomes a concern. The rivet implementation should follow the
      `spar` validation of the same pattern (sequential, so we inherit
      a tested approach rather than co-debug two repos in parallel).

      Acceptance:
        - The Verus CI job's `Verify Verus specs` step actually
          executes — assert it is not `skipped` in the job step list.
        - Environment breakage (Nix/toolchain unavailable) makes the
          job FAIL visibly, distinct from a soft-gated proof result:
          `continue-on-error` must not be able to mask a job that
          never ran the verifier.
        - A completed Verus job's `verus-test-log` artifact contains a
          real Verus solver result.
        - The Verus job's `runs-on:` targets the `podman` label
          (scoped to a podman-capable runner) and Nix work runs inside
          a `nixos/nix` rootless container — no `nix-installer-action`
          and no host-installed Nix.
    tags: [ci, verus, silent-failure, f2-family, nix]
    fields:
      priority: should
      category: non-functional
      baseline: v0.12.0-track
    links:
      - type: derives-from
        target: FEAT-135

  - id: REQ-087
    type: requirement
    title: "rivet export --single-page and --filter must actually narrow output for large projects, not silently emit the full artifact set"
    status: draft
    description: |
      Empirical observation on a 5120-artifact rivet project (2026-05-23):
      `rivet export --html --single-page` still produces all per-artifact
      pages instead of a single document, and `--filter '<s-expr>'`
      emits the full artifact set rather than the filtered subset. The
      flag help text claims semantics the implementation does not
      deliver — the F2 silent-failure class catalogued by
      REQ-078..082.

      Verified plausible in the code: `--single-page` is wired through
      `cmd_export` -> `cmd_export_html` (rivet-cli/src/main.rs:7976,
      8000-8017, calling `rivet_core::export::render_single_page`); but
      something in the path either ignores the flag for the multi-page
      generator that also runs, or the renderer falls back to per-page
      output for input sizes above a threshold. The user observed both
      flags non-functional on the 5120-artifact project.

      Acceptance:
        - `rivet export --html --single-page --out single.html` on a
          5000+ artifact project produces ONE HTML file at the given
          path, not a directory tree of per-artifact pages.
        - `rivet export --html --filter '<s-expr>' --out tree/` on a
          5000+ artifact project produces only the artifacts that match
          the s-expr (verified by counting files in `tree/artifacts/`).
        - If a requested flag combination cannot be honoured on a given
          input, the exporter errors loudly with a clear message — it
          must not silently emit "default" output that contradicts the
          user's flags.
        - Regression test in `rivet-cli/tests/` driving the real binary
          against a small fixture exercising both flags.
    tags: [export, html, silent-failure, f2-family]
    fields:
      priority: should
      category: functional
      baseline: v0.13.0-track
    links:
      - type: derives-from
        target: FEAT-135
      - type: traces-to
        target: REQ-078

  - id: REQ-088
    type: requirement
    title: "rivet export HTML must not embed the full CSS/JS framework per page — extract to shared assets so large exports are usable"
    status: draft
    description: |
      Each rendered per-artifact HTML page embeds the full CSS + JS
      framework inline: `<style>{fonts_css}{css}</style>` and
      `<script>{mermaid_js}</script>` (rivet-cli/src/main.rs:8142-8143),
      with `MERMAID_JS = include_str!("../assets/mermaid.min.js")` —
      mermaid.min.js alone is ~3MB. 4000+ artifact pages times ~3MB
      = ~13 GB by construction; the user measured exactly that.

      The fix is well-bounded: extract the framework into a single
      `_assets/` directory under the export root and reference it from
      each page via relative `<link rel="stylesheet" href="...">` /
      `<script src="...">`. Per-page payload drops to ~50KB
      (artifact-specific markup only); total export of a 5000-artifact
      project should land at ~50-100 MB uncompressed.

      Acceptance:
        - `rivet export --html` on a 5000-artifact project produces
          exactly one copy of every shared asset (mermaid.min.js, base
          CSS, fonts) under `<out>/_assets/`, and per-artifact pages
          reference them by relative URL.
        - Total uncompressed export size on a 5000-artifact project
          is ≤ 100 MB.
        - A randomly-sampled per-artifact page renders correctly when
          opened directly from disk (the relative paths resolve).
        - Regression test asserts the per-page byte budget (e.g.
          `assert!(per_page_bytes < 100_000)` for a representative
          artifact).
    tags: [export, html, performance, bloat, audit]
    fields:
      priority: should
      category: non-functional
      baseline: v0.13.0-track
    links:
      - type: traces-to
        target: REQ-087

  - id: REQ-089
    type: requirement
    title: "Diagnostics must be identical across the VSIX extension, `rivet serve`, and `rivet validate` — no per-frontend divergence"
    status: draft
    description: |
      Empirical observation against v0.11.1: the VS Code extension
      (vscode-rivet) surfaced different warnings than `rivet serve` or
      `rivet validate` for the same project. There is no integration
      test asserting the three rendering paths agree on the diagnostic
      set. A user fixing what the VSIX shows may still have unsurfaced
      `rivet validate` errors that gate CI — itself an F2-class
      silent failure on the QA surface.

      The structural fix: all three frontends consume the same
      diagnostic-producing function (`validate::validate` and friends);
      no frontend re-implements or filters diagnostics independently.
      An integration test that runs the three paths over a fixture
      project and diffs the diagnostic SET (rule, severity,
      artifact_id, message-prefix) enforces this.

      Acceptance:
        - For a fixture project with a known mix of errors / warnings /
          infos, an integration test extracts diagnostics from:
          (a) `rivet validate --format json`,
          (b) `rivet serve` and scraping the validate page, and
          (c) the VSIX extension's published diagnostics (LSP-style
          test or a headless VS Code extension test).
          The three sets must match by (rule, severity, artifact_id,
          message-prefix).
        - Any per-frontend filtering (e.g. severity threshold display)
          is explicit and documented in the diagnostic-emission API,
          not buried in the frontend.
        - The same shared diagnostic function powers all three.
    tags: [vsix, serve, validate, silent-failure, f2-family, qa, consistency]
    fields:
      priority: should
      category: non-functional
      baseline: v0.13.0-track
    links:
      - type: derives-from
        target: FEAT-135

  - id: REQ-090
    type: requirement
    title: "GitHub Release attaches a compliance bundle (~51 MB: documents + coverage + matrix + validate + ReqIF), not the per-artifact HTML drilldown"
    status: draft
    description: |
      The v0.12.0 GitHub Release attached HTML navigation shells
      (~7 MB, useless without the per-artifact pages) and not the
      rendered specifications + machine-importable ReqIF auditors
      actually need. The audit content map for a 5120-artifact project:

      - 51 MB bundle = `documents/` (44 rendered specs with resolved
        artifact tables — ID/Type/Title/Status per reference) +
        `coverage/` + `matrix/` + `validate/` + `stpa/` +
        `eu-ai-act/` + `graph/` + `index.html` + `ReqIF` (XML, OMG
        standard, importable into DOORS / Polarion / codeBeamer).
        Auditors get full review material; fits a GitHub release
        artifact.
      - Per-artifact HTML drilldown (~13 GB once REQ-088's bloat is
        fixed, ~50 MB) is overkill for compliance review — the doc
        tables already show ID/Type/Title/Status for every
        referenced artifact.

      Acceptance:
        - The release workflow produces a single bundled tarball
          (e.g. `rivet-compliance-bundle-vX.Y.Z.tar.gz`) containing
          documents/, coverage/, matrix/, validate/, stpa/,
          eu-ai-act/, graph/, index.html, plus the project's
          ReqIF + generic-yaml export.
        - The tarball is attached to the GitHub Release.
        - The per-artifact `artifacts/` HTML tree is NOT in the
          tarball (until REQ-088's CSS/JS dedup lands, including it
          balloons the bundle; even after REQ-088 it's still optional
          drilldown, not compliance evidence).
        - A README in the tarball documents what each directory is and
          how to import the ReqIF.
        - The release workflow change is gated on REQ-088 landing
          first — until per-page assets are de-duplicated, the
          per-artifact pages can't be cleanly included anyway.
    tags: [release, packaging, compliance, audit, reqif]
    fields:
      priority: should
      category: non-functional
      baseline: v0.13.0-track
    links:
      - type: traces-to
        target: REQ-088
      - type: traces-to
        target: REQ-005

  - id: REQ-091
    type: requirement
    title: "rowan-yaml salsa parser silently drops flush-left artifacts from the default validate path"
    status: draft
    description: |
      Default `rivet validate` (the salsa-incremental path with the
      `rowan-yaml` feature enabled, the default) drops artifacts
      written in YAML's flush-left list style from its grading. The
      artifact is loaded by other paths (`rivet list`, lifecycle
      coverage, validate `--direct`) and the artifact file parses
      cleanly via `parse_generic_yaml`, but the rowan parser
      (`yaml_hir::extract_schema_driven`, the default salsa load
      path) returns ZERO artifacts and ZERO diagnostics for the same
      file. The link graph the validator should grade is invisible.

      Discovered via clean-room verification of a parallel agent's
      bug report (sphinx-needs importer corpus — Python tool 2507
      warnings, Rust port 488 — the 2019 gap is exactly the lost
      grading). The agent attributed the bug to
      `GenericYamlAdapter::export` / `parse_generic_yaml`; probe
      reproduced their fixture and FALSIFIED that attribution —
      `parse_generic_yaml` returns 1 link for both fixtures,
      identical link_type/target. The real failure is in
      `yaml_hir::extract_schema_driven`:

        Fixture (flush.yaml, 7 lines):
          artifacts:
          - id: COMP_REQ__FLUSH__A
            type: comp-req
            title: Flush-left
            status: approved
            links:
            - type: satisfies
              target: FEAT_REQ__NOPE__DANGLE

        extract_schema_driven(...) -> artifacts: 0, diagnostics: 0   ← bug
        parse_generic_yaml(...)    -> 1 artifact, 1 link             ← fine
        rivet list                 -> 1 artifact, 1 link             ← fine
        rivet validate (default)   -> no link errors for FLUSH       ← bug
        rivet validate --direct    -> link errors for both           ← fine

      A contributing factor: `extract_links_via_serde` (the rowan
      parser's fallback) silently returns `Vec::new()` on
      `serde_yaml::from_str` parse error — F2 silent-failure ethos
      violation. Even the fallback can't surface "I tried to parse
      links and couldn't."

      Impact: any project using generic-yaml whose artifacts use
      flush-left lists (which is exactly what serde_yaml's
      `to_string` emits — so rivet's own
      `GenericYamlAdapter::export` produces files rivet's default
      validate cannot fully grade) silently loses link-graph
      validation in CI.

      Bug-doc + 2-file fixture authored by the parallel agent:
      `docs/design/rivet-link-parse-bug.md` +
      `docs/design/rivet-link-parse-bug-fixture/` (on the
      `feat/import-results-sphinx-needs` branch). Description is
      attribution-corrected here.

      Acceptance:
        - `yaml_hir::extract_schema_driven` returns 1 artifact + 1
          link for the flush.yaml fixture (identical to indent.yaml).
        - Default `rivet validate` (salsa + rowan-yaml) emits the
          broken-link ERROR for COMP_REQ__FLUSH__A — same as
          `rivet validate --direct` does today.
        - `extract_links_via_serde`'s parse-error branch emits a
          parse diagnostic instead of `Vec::new()` (loud-fail).
        - Regression test in `rivet-core/tests/` driving
          `extract_schema_driven` directly with both fixture shapes,
          asserting identical artifact + link counts.
        - The parallel agent's sphinx-needs port corpus, re-graded
          after the fix, matches the Python reference's ~2500
          warnings (the falsification gate the agent's handoff
          described).
    tags: [yaml, rowan, salsa, validate, silent-failure, f2-family]
    fields:
      priority: must
      category: functional
      baseline: v0.13.1-track
    links:
      - type: derives-from
        target: FEAT-135

  - id: REQ-092
    type: requirement
    title: "Per-source-line traceability linker — Eclipse S-CORE source_code_linker equivalent"
    status: draft
    description: |
      Eclipse S-CORE ships `score_source_code_linker` — a tool that
      scans source files for inline comments of the shape
      `// req-Id: <id>` and emits per-line traceability records
      linking that source location to the named requirement (need).
      Rivet has `rivet commits` (REQ-051), which links per-commit via
      trailers (`Implements:`, `Refs:`, `Fixes:`, `Verifies:`).
      Different granularity: per-line vs per-commit; both auditable.

      For projects converging from S-CORE → rivet (the Eclipse S-CORE
      sphinx-needs importer landing in the parallel agent's PR is one
      such case), a per-source-line linker closes the granularity gap
      and lets one tool surface both kinds of evidence under the
      existing traceability rules.

      Proposed shape (subject to design):

      - `rivet source-link` — new subcommand. Scans source paths
        configured in `rivet.yaml` for `// rivet: <req-id>`
        comments (marker configurable; default `// rivet:` to avoid
        colliding with S-CORE's `// req-Id:` until a migration story
        exists). Emits a per-line traceability artifact set: file,
        line range, linked artifact-id, comment text.
      - Output integrates with existing traceability — the per-line
        evidence shows up in `rivet validate`'s coverage metrics and
        the matrix view, indistinguishable from a per-commit link
        once aggregated by target.
      - Optional `--marker '// req-Id:'` switch lets a downstream
        project consume S-CORE-style comments without migration.

      Acceptance:
        - `rivet source-link` on a project with `// rivet: <req-id>`
          comments in tracked source files emits artifacts naming
          file / line / linked artifact-id.
        - `rivet validate` after `source-link` counts those per-line
          links toward the same coverage metrics as commit-trailer
          links (REQ-051 family).
        - `--marker '// req-Id:'` ingests S-CORE-style comments.
        - Malformed markers (`// rivet: NOTASHAPE`, missing ID,
          duplicate per line) emit diagnostics — never silent skip.
        - A worked example added under `examples/source-link/`.
    tags: [traceability, source-link, eclipse-score, parity, new-capability]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-051

  - id: REQ-093
    type: requirement
    title: "Implement the FUTURE oracles — asil-decomposition, coverage-threshold, method-table-compliance — turn the ISO 26262 narrative into mechanical checks"
    status: draft
    description: |
      The schema declares three oracles today (`asil-decomposition`,
      `coverage-threshold`, `method-table-compliance`) but their
      evaluators are FUTURE stubs — `rivet validate` parses the
      pipeline declaration and never fires. 10-persona review row A1
      (Hess, ISO 26262 assessor): "your schema parses pipelines that
      don't fire." Until they fire, the per-ASIL rigour rivet claims
      is narrative, not mechanical.

      Acceptance:
        - `asil-decomposition` oracle: ISO 26262-9 decomposition
          rules implemented and exercised. Fixture: an ASIL-D parent
          decomposed into two children must satisfy the allowed
          pairs (ASIL-B + ASIL-B, ASIL-A + ASIL-C, …). Non-conforming
          pair -> ERROR; conforming pair -> silent.
        - `coverage-threshold` oracle: per-rule coverage % computed
          against a declared minimum; below-threshold -> ERROR
          naming (rule, observed, required).
        - `method-table-compliance` oracle: ISO 26262 Part 6
          Tables 9 / 10 / 11 encoded; missing required method at the
          declared ASIL -> ERROR.
        - Each oracle has at least one passing fixture and one
          failing fixture in `rivet-core/tests/`.
        - `rivet docs schema/...` no longer renders these as FUTURE.
    tags: [oracle, iso-26262, qualification, falsification, persona-review]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-004

  - id: REQ-094
    type: requirement
    title: "Reproducible-build oracle + sigstore-signed release verification — `rivet release-verify`"
    status: draft
    description: |
      A tool whose output IS compliance evidence needs verifiable
      releases. v0.10.x added sigstore-keyless signing of
      `SHA256SUMS` via cosign — the production side is done. The
      missing leg is the consumer side: `rivet release-verify <tag>`
      rebuilds the release artifacts from a fresh checkout, hashes
      the output, verifies the sigstore bundle against the GitHub
      OIDC identity, and refuses if any step disagrees. 10-persona
      row A4 — two of three rivet reviewers (Hess, Whittaker) flagged
      release-channel integrity as the blocking gap.

      Acceptance:
        - `rivet release-verify v0.13.0` rebuilds, hashes, fetches
          the sigstore bundle, runs `cosign verify-blob` with the
          documented identity regex, and exits 0 iff every hash
          matches and the signature verifies against the workflow's
          OIDC identity.
        - Tampered `SHA256SUMS`, missing signature, wrong identity,
          or non-reproducible build each fail loudly with the named
          mismatch.
        - A CI job runs `release-verify` on the most-recent tag
          periodically and gates merge to `main` if it fails.
        - Documented via `rivet docs release-verify`.
    tags: [release, sigstore, supply-chain, qualification, persona-review]
    fields:
      priority: must
      category: non-functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-068

  - id: REQ-095
    type: requirement
    title: "`cargo build` invokes `rivet validate` for crates declaring `rivet.yaml` — born-compliant at the compile step"
    status: draft
    description: |
      Today `rivet validate` fires on commit (pre-commit hook + CI).
      The MBSE-mandatory principle says the model drives the build,
      not sits alongside it. Bringing validation INTO the build
      closes the window between "edited an artifact" and "noticed
      the artifact is broken": today that window is one commit;
      tomorrow it is one `cargo build`. 10-persona row B1; Carrasco
      named it as the biggest MBSE-philosophy win and the realisation
      of the "born-compliant" framing.

      Acceptance:
        - A `build.rs` in `rivet-cli` (or a `cargo` subcommand
          wrapper) detects `rivet.yaml` and runs `rivet validate`.
        - Non-zero validate -> the build fails.
        - Opt-out env var (`RIVET_SKIP_VALIDATE_ON_BUILD=1`) for
          bootstrap / circular cases; default ON.
        - Build-time validation result is cached by content hash so
          incremental builds stay fast.
        - Integration test: a project with a deliberately broken
          artifact YAML fails `cargo build` with the validate
          diagnostic; fixing the artifact unblocks the build.
    tags: [mbse, build, cargo, validate, persona-review]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-004

  - id: REQ-096
    type: requirement
    title: "SACM 2.x typed safety-case schema with deductive-sufficiency mechanisation"
    status: draft
    description: |
      Rivet ships a safety-case schema today but stops at *structural*
      traceability — every workproduct has a chain of links — without
      enforcing the *deductive sufficiency* obligations a safety case
      claims. 10-persona row A2 (Dubois, safety researcher):
      "compliance trace ≠ safety case." Promoting the schema to OMG
      SACM 2.x's claim / argument / evidence shape + a mandatory
      `supported_by` cardinality lets `rivet validate` enforce "every
      hazard mitigated, every AoU discharged, every derived
      requirement traced to its analysis origin."

      Acceptance:
        - `schemas/sacm.yaml` declares `claim`, `argument`,
          `evidence` artifact types with the SACM 2.x link
          vocabulary (`supported_by`, `in_context_of`,
          `decomposed_into`, …).
        - Traceability rule: every ASIL-B+ workproduct is reachable
          from a `root_claim`-typed claim via `supported_by`.
        - Traceability rule: every `hazard` has a `mitigated_by`
          path to at least one `safety_requirement`.
        - Traceability rule: every `assumption_of_use` has a
          matching `discharged_by` link in the consumer's project
          (cross-repo via the REQ-085 mechanism).
        - Worked example under `examples/safety-case/` carrying a
          minimal but non-trivial SACM tree.
    tags: [sacm, safety-case, assurance, schema, persona-review]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: derives-from
        target: REQ-002

  - id: REQ-097
    type: requirement
    title: "DO-330 tool-qualification artifacts (TQP / TOR / TVCP / TCI / TAS) as a typed schema, dogfooded by rivet itself"
    status: draft
    description: |
      10-persona row A6 (Whittaker, DO-178C/DO-330 reviewer): "rivet's
      TCL-2 narrative is honest, but there is no DO-330 *shape* —
      TQP / TOR / TVCP / TCI / TAS are absent." A TCL-2 *tool* has
      those artifacts in typed form; a TCL-2 *narrative* does not.
      Filing them in `schemas/do-178c.yaml` and dogfooding the tool's
      own qualification dossier under that schema turns the TQL claim
      from "asserted" to "a falsifiable graph state."

      Acceptance:
        - `schemas/do-178c.yaml` declares the five artifact types:
          `tool_qualification_plan` (TQP),
          `tool_operational_requirements` (TOR),
          `tool_verification_cases_and_procedures` (TVCP),
          `tool_configuration_index` (TCI),
          `tool_accomplishment_summary` (TAS).
        - Required link cardinalities encoded:
          TQP -> TOR(+), TOR -> TVCP(+), TVCP -> TCI(1),
          TAS -> TQP(1).
        - `artifacts/qualification/` carries rivet's *own* TQP /
          TOR / TVCP / TCI / TAS — dogfood demonstrates that the
          tool's qualification artifacts validate cleanly under the
          tool itself.
        - `rivet docs do-178c` topic added with the layering
          explanation.
    tags: [do-178c, qualification, schema, dogfood, persona-review]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-004

  - id: REQ-098
    type: requirement
    title: "Independence-of-verification-layers oracle — refuse `product-of-miss-rates` claims when layers share too much code"
    status: draft
    description: |
      The single most-repeated complaint of the 10-persona panel
      (row A3, Hess + Whittaker): Kani, Verus, Rocq, proptest, and
      `rivet validate` all share the `Artifact` model and the YAML
      parser. The "product of miss rates" argument that justifies
      claiming "TCL-2 because we have N independent verification
      layers" collapses if the shared parser has a bug — common-mode
      failure. The oracle: rivet declares each verification layer's
      Rust dep graph, walks the intersection, and refuses to claim
      "independent layer" when shared-code count exceeds a
      configurable threshold.

      Acceptance:
        - `rivet validate --check-independence` reads a manifest of
          declared verification layers (Kani / Verus / Rocq /
          proptest / direct validate / salsa validate) and their
          entry-point crates.
        - Computes the transitive-dep intersection of any two
          declared-as-independent layers.
        - Emits ERROR if intersection exceeds the configured
          threshold (default: 0 shared *parsing* code; small-N
          shared *types* allowed and documented).
        - The TCL dossier's independence claim links to the oracle's
          last passing run as evidence.
    tags: [independence, verification, qualification, oracle, persona-review]
    fields:
      priority: should
      category: non-functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-051

  - id: REQ-099
    type: requirement
    title: "`rivet_apply` MCP tool — idempotency keys + atomic transactions + auto-reload + post-state diagnostics"
    status: draft
    description: |
      10-persona row A5 / C6 (Krishnamurthy, AI engineer): "MCP catalog
      is small + typed + orthogonal — good. Mutation→reload is manual,
      no batch, no idempotency = retry pain at scale." An agent fleet
      replaying a failed batch today produces duplicates; an agent
      without a reload tool sees stale state. `rivet_apply` is a
      single mutator tool that takes a JSON-patch + idempotency key,
      applies atomically, runs `rivet validate` on the post-state, and
      returns both the diff and the diagnostics — so agents don't have
      to coordinate three calls per mutation.

      Acceptance:
        - `rivet_apply(patch, idempotency_key)` MCP tool: replay with
          the same key is a verified no-op (returns the same response
          shape as the first call).
        - Atomic: failure rolls the artifact back; the YAML file is
          either fully-changed or untouched.
        - Auto-reload: the MCP server's cached state is invalidated
          on apply.
        - Response includes the post-apply `rivet validate`
          diagnostics — agents see the consequence of the change
          without a follow-up call.
        - `rivet validate` gains an audit-log check that detects
          idempotency-key violations across past applies (REQ-082
          F2 ethos extended to mutation).
    tags: [mcp, agent, idempotency, mutation, persona-review]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: FEAT-010

  - id: REQ-100
    type: requirement
    title: "`externals: kind: source` — track non-rivet upstreams (eclipse-score, sphinx-needs, DOORS exports, plain code repos) without the missing-rivet.yaml warning"
    status: draft
    description: |
      Today every external in `rivet.yaml` is assumed to be a rivet
      project, so `rivet sync` (and downstream loaders) try to read
      the external's `rivet.yaml` and emit a WARN when it is absent.
      A converter scenario like the eclipse-score import pulls 20+
      upstream repos that are deliberately not rivet projects — by
      design, they ship sphinx-needs / DOORS-exports / plain code,
      not `rivet.yaml`. The current behaviour emits 58 WARN lines of
      pure noise in that workflow.

      Generalises beyond eclipse-score: any fork tracking a DOORS
      export, a sphinx-needs project, or a plain source repo that
      feeds a converter or a source-linker wants the same shape.

      Design:
        - Add an optional `kind:` field to each entry under
          `externals:`. Two enum values:
          - `rivet` (default — current behaviour: loads the
            external's `rivet.yaml`, may emit
            assumption-of-use / discharged-by reconciliation, etc.).
          - `source` (NEW — `rivet sync` clones to the same on-disk
            location but loaders skip the `rivet.yaml` read attempt;
            no WARN; no rivet-project bookkeeping).
        - Scope: ~20 LoC in `rivet-core/src` plus the schema entry.
        - `rivet validate` rejects an unknown `kind:` value with a
          named-enum error (no silent fallback to `rivet`).

      Acceptance:
        - Fixture: a `rivet.yaml` declaring `externals: foo:
          { git: ..., kind: source }` syncs and validates cleanly,
          with ZERO `missing rivet.yaml` warnings for `foo`.
        - Fixture: omitting `kind:` defaults to `rivet` and
          preserves today's behaviour exactly — a missing
          `rivet.yaml` still surfaces (regression test).
        - Fixture: an unknown `kind:` value (e.g. `kind: roboms`)
          produces an ERROR diagnostic that names both the bad value
          and the enum domain.
        - Documented under `rivet docs externals`.

      Note: the slot REQ-100 was deliberately left free earlier
      after dropping the v1.0-readiness REQ proposal; this
      orthogonal request happily reuses the number.
    tags: [externals, sync, converter, non-rivet-upstream, ergonomics]
    fields:
      priority: should
      category: functional
      baseline: v0.13.x-track
    links:
      - type: traces-to
        target: REQ-051

  - id: REQ-101
    type: requirement
    title: "Artifact-driven verification gate — `rivet verify` matches approved artifacts to convention-named tests"
    status: draft
    description: |
      Land the artifact-driven verification-gate pattern that meld
      already runs (`tools/post_verification_comment.py`, ~50 LoC)
      as a first-class `rivet verify` subcommand.

      The gate inverts the usual "run tests, check exit code" loop:
      the artifact list is the worklist, the test is the evidence,
      and the absence of a matching test is a discrete diagnostic
      row (MISSING), not silence. Same traceability shape as
      REQ-051 (commit trailer demands link) and the schema's
      required-fields gates — the model demands a test, and CI
      falsifies the obligation mechanically.

      Pattern (per artifact A where A.status = approved and the
      schema's `verification-template` is set on A's type):

          glob  = expand(template, A.id)   # REQ-001 -> test_req_001_*
          tests = discover(glob)           # via configured runner
          if tests == []:        bucket(A) = MISSING
          elif any(t.failed):    bucket(A) = FAILED
          else:                  bucket(A) = PASSED

      Schema extension (composes on existing `ArtifactTypeDef` per
      the reuse-binding-patterns guidance, NOT a new directive):

        - id: requirement
          verification-template: "test_{id_lower_underscored}_*"

      The field is optional; types without it are excluded from the
      gate (e.g. decisions, features that have no executable
      evidence).

      Runner is shell-injectable via `rivet.yaml`
      (default `cargo test --no-fail-fast`) so pytest / nextest /
      bazel / ctest work without rivet learning each.

      Outputs:
        - `--format pr-comment` — markdown grouped by bucket with
          source-of-truth footer (byte-comparable to meld);
        - `--format json` — machine-readable, dashboard-ingestible;
        - `--format yaml --out artifacts/results/` — written into the
          existing `ResultStore` so `rivet results show` surfaces it
          without modification.

      Gate: exit code 1 if any artifact is FAILED or MISSING; 0
      otherwise.

      NOT an oracle in `rivet validate`: validate is read-only over
      YAML. `verify` executes tests and belongs in its own command.

      Acceptance:
        - `rivet verify --help` lists the subcommand.
        - Schema fixture with `verification-template:
          "test_{id_lower_underscored}_*"` on the `requirement`
          type + one approved REQ-X with a matching passing test
          makes `rivet verify` exit 0 and report 1/1 PASSED.
        - Removing the test makes `rivet verify` exit 1 with REQ-X
          in the MISSING bucket — the absence is a row, not silence.
        - Making the test fail makes `rivet verify` exit 1 with
          REQ-X in the FAILED bucket.
        - Types without `verification-template` (e.g. decisions) are
          excluded from the worklist and not counted.
        - `rivet verify --format pr-comment` emits markdown with
          PASSED / FAILED / MISSING sections + a footer naming the
          source-of-truth file(s) and the template — byte-comparable
          to meld's reference output on the meld corpus.
        - `rivet verify --format yaml --out artifacts/results/`
          writes a `ResultStore`-loadable file consumed by
          `rivet results show` without modification.
        - Runner command is overridable via `rivet.yaml`
          (`verify.runner`) — a pytest project sets it once, no
          code change.
        - Worked example under `examples/verify/` + GitHub Action
          snippet under `docs/ci/` wrapping
          `rivet verify --format pr-comment | gh pr comment`.
    tags: [verification, gate, ci, mbse, traceability, meld-parity, new-capability]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-051
      - type: traces-to
        target: REQ-004

  - id: REQ-102
    type: requirement
    title: "`gherkin` export format advertised in `rivet export --help`"
    status: approved
    description: |
      `rivet export` accepts `--format gherkin` (validate_format allows
      it, cmd_export_gherkin handles it) but the clap `--format` help
      listed only reqif / generic-yaml / html / zola. Surfaced by the
      v0.13.1 export-format end-to-end test. Help text + the
      unsupported-format error message now both list gherkin (and zola).

      Acceptance:
        - `rivet export --help` output contains "gherkin".
    tags: [export, gherkin, docs, cli]
    fields:
      priority: should
      category: functional
      baseline: v0.13.2
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-103
    type: requirement
    title: "ReqIF import reachable from the CLI (`import-results --format reqif`)"
    status: approved
    description: |
      `rivet export --format reqif` produced ReqIF, but there was no
      CLI path to bring a ReqIF file (e.g. a DOORS / Polarion export)
      back into rivet artifacts — `parse_reqif` lived in rivet-core but
      was reachable only via the supplier-pull well-formedness cache.
      The DOORS/Polarion round-trip was therefore export-only. Adds
      `import-results --format reqif <file>`: parse to artifacts, write
      generic YAML, report artifact + link counts.

      Acceptance:
        - `rivet import-results --format reqif <file> --output <dir>`
          writes `reqif-import.yaml` containing the parsed artifacts.
        - Round-trip: `export --format reqif` then
          `import-results --format reqif` preserves artifact + link
          counts (verified in rivet-core lib + a rivet-cli integration
          test).
    tags: [reqif, import, interchange, doors, polarion, round-trip]
    fields:
      priority: should
      category: interface
      baseline: v0.13.2
    links:
      - type: traces-to
        target: REQ-005

  - id: REQ-104
    type: requirement
    title: "ReqIF export emits a SPECIFICATION + SPEC-HIERARCHY document tree"
    status: approved
    description: |
      The ReqIF export carried SPEC-OBJECTs + SPEC-RELATIONs +
      SPEC-OBJECT-TYPEs but no SPECIFICATION / SPEC-HIERARCHY, so
      importers (DOORS / Polarion / codeBeamer) rendered a flat object
      pool with no navigable outline (some reject a hierarchy-less file
      outright). Export now emits one SPECIFICATION referencing a
      SPECIFICATION-TYPE, with one SPEC-HIERARCHY entry per SPEC-OBJECT
      in store order. The object/relation payload is unchanged (no
      regression: still 797 objects + 1551 relations on the rivet
      corpus).

      Acceptance:
        - Exported ReqIF contains exactly one SPECIFICATION, one
          SPECIFICATION-TYPE it references, and one SPEC-HIERARCHY per
          SPEC-OBJECT.
        - The SPECIFICATION addition does not change SPEC-OBJECT or
          SPEC-RELATION counts (round-trip preserved).
    tags: [reqif, export, interchange, doors, polarion, completeness]
    fields:
      priority: should
      category: interface
      baseline: v0.13.2
    links:
      - type: traces-to
        target: REQ-005

  - id: REQ-105
    type: requirement
    title: "HTML export must be a self-contained static site (bundle the serve-only assets)"
    status: draft
    description: |
      `cmd_export_html` reuses the `rivet serve` dashboard rendering
      (`serve::reload_state`, `serve::components`) verbatim. It does NOT
      spin up a server (reload_state is a pure offline loader), but the
      rendered HTML references serve-ONLY routes and JS that the static
      export does not bundle. Three confirmed gaps, all the same root
      cause — "export reuses serve's HTML but ships none of serve's
      runtime assets":

      1. **Absolute server-route links.** Pages link
         `href="/artifacts/CD-001"`, `href="/coverage"`, … with no
         `.html` extension. As static files these break: from
         `file:///…/html/index.html`, `/artifacts/CD-001` resolves to
         the filesystem root; the real file is `artifacts/CD-001.html`.
         Hosting under a sub-path breaks identically. (Content is all
         present; only the inter-page links are wrong.)

      2. **SVG-viewer toolbar JS not bundled.** The mermaid / graph
         toolbar buttons (`svgZoomFit`, `svgFullscreen`, `svgPopout`)
         render in the exported HTML (the `svg-viewer-toolbar` markup
         is there) but are DEAD — their handlers live in serve's
         `js.rs`, and the export's `_assets/` ships only
         `mermaid.min.js` + `styles.css`. So a static export shows
         non-functional zoom/fullscreen/popout buttons. (In `rivet
         serve` they work and are Playwright-tested.)

      3. **`/docs-asset/` images not copied.** Documentation can
         reference PNG/SVG/jpg/gif/webp/pdf images; `render::source.rs
         ::rewrite_image_paths` rewrites a relative `src` to
         `/docs-asset/<path>`, which the serve `/docs-asset/{*path}`
         route serves from the doc directories. The static export
         neither rewrites those to a relative file path nor copies the
         referenced image files, so `<img src="/docs-asset/…">` 404s in
         an exported site. (Serve path verified by unit test
         `render::source::tests::relative_image_src_rewritten_to_docs_asset`.)

      Fix direction (regression-risk — own focused cycle, not a patch):
        - Rewrite internal links to relative + `.html` on export (the
          export already computes a depth-adjusted `"../".repeat(depth)`
          prefix for `_assets/`; extend it to artifact/report links and
          to image `src`), OR add a `--base-path` option.
        - Bundle the svg-viewer toolbar JS into `_assets/` (extract the
          handlers from `js.rs` into a shared file both serve and export
          emit).
        - Copy `/docs-asset/`-referenced images into the export tree and
          rewrite their `src` to the relative copied path.
        - Longer-term: decouple export rendering from the `serve` module
          so `rivet export --format html` doesn't transitively depend on
          axum / the dashboard handlers.

      Acceptance:
        - Opening the exported `index.html` via `file://` and clicking
          any artifact / coverage / matrix / graph link lands on the
          correct local `.html` page (no absolute `/…` routes remain).
        - Hosting the export under a sub-path keeps all links working.
        - The mermaid / graph zoom-fit / fullscreen / popout buttons
          function in the exported static HTML (handler JS bundled).
        - A doc that references a relative PNG/SVG renders that image in
          the exported site (image copied + `src` relative).
        - `rivet export --format html` builds without linking the axum
          server stack (decoupling milestone).
    tags: [export, html, serve-coupling, relative-links, assets, mermaid, docs-images, regression-risk]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-106
    type: requirement
    title: "serve dashboard: variant scope resolves embedded bindings (self-reference --binding)"
    status: draft
    description: |
      User-reported (Ford variant corpus). The CLI resolves variant
      bindings correctly:

          rivet variant solve --model feature-model.yaml \
            --variant variants/ford-demo-full.yaml \
            --binding variants/ford-demo-full.yaml

      …reports the right bound-artifact counts (16 / 13 / 17). But the
      `rivet serve` dashboard calls the solver WITHOUT passing
      `--binding`, so it reports `bound_artifact_count: 0` for every
      variant. The dashboard should self-reference each variant file as
      its own binding model when the `bindings:` section is embedded in
      the variant config.

      Acceptance:
        - The serve dashboard variant view shows the same non-zero
          bound-artifact counts as the CLI `variant solve --binding`
          for variants whose binding is embedded in the variant file.
        - A serve_integration test asserts a known variant's
          bound-artifact count matches the CLI.
    tags: [serve, variant, binding, dashboard, user-reported, bug]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-107
    type: requirement
    title: "Render sequence/mapping field values, not Rust Debug output"
    status: draft
    description: |
      User-reported. A field whose value is a sequence of mappings
      renders as the Rust `Debug` form:

          Sequence [Mapping {"kind": String("e"), "page_id":
          String("e"), "version": Number(2), "section": String("eee")}]

      …instead of a readable list/table. Affects serve and likely the
      HTML export (shared rendering). Field values that are sequences
      or mappings must render as structured HTML (nested list / small
      table), never `format!("{:?}", value)`.

      Acceptance:
        - An artifact with a sequence-of-mappings field renders as a
          readable nested structure in both serve and HTML export.
        - No `Sequence [`, `Mapping {`, `String(`, `Number(` debug
          tokens appear in rendered output.
    tags: [serve, export, rendering, field-values, user-reported, bug]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-108
    type: requirement
    title: "serve: external-artifact navigation as prefix:ID + visible shortcut-link contrast"
    status: draft
    description: |
      User-reported, three coupled serve-dashboard issues for
      cross-repo (`prefix:ID`) artifacts:

      1. Clicking an externally-defined artifact navigates to
         `externals/<prefix>` rather than the
         `artifacts/<prefix>:<artifact>` detail view.
      2. On an `artifacts/<prefix>:<artifact>` page, the external's own
         references are shown but NOT as followable
         `<prefix>:<reference>` links — so the cross-repo chain can't be
         traversed (and would recurse for externals-of-externals).
      3. That same page does not show the linkage back into the
         consumer's internal artifacts.
      4. The shortcut-link color against the white background is barely
         visible (contrast/accessibility).

      Acceptance:
        - Clicking an external artifact lands on its
          `artifacts/<prefix>:<id>` detail view.
        - External references render as followable `prefix:ref` links,
          recursively.
        - The detail view shows inbound links from internal artifacts.
        - Shortcut links meet a visible contrast ratio on the light
          background.
    tags: [serve, externals, cross-repo, navigation, accessibility, user-reported, bug]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-085

  - id: REQ-109
    type: requirement
    title: "Variant scoping for documents (in/out-of-scope per variant)"
    status: draft
    description: |
      User question: a variant can scope which artifacts are in/out of
      scope (via `out-of-scope-for:` / binding), but there is no
      equivalent control for *documents* — a variant cannot currently
      declare which documents are in or out of scope. Assess whether
      document-level variant scoping is wanted, and if so design it on
      the existing variant-scope mechanism (not a new directive), so a
      variant's exported/served document set narrows with the variant.

      Acceptance:
        - Decision recorded: do documents get per-variant scoping?
        - If yes: a variant can mark a document out-of-scope, and
          serve / export under that variant omit it; `rivet validate`
          still sees the full set.
    tags: [variant, documents, scoping, design-question, user-reported]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-083

  - id: REQ-124
    type: requirement
    title: "Agent-actionable diagnostic remediation (fix-options + docs link)"
    status: approved
    description: |
      User-reported. A validation error such as

        ERROR: [TE-008] link 'traces-to' targets 'FEAT-019' (type 'feature'),
               allowed target types: ["requirement", "design-decision"]

      states *what* is wrong but not *what to do* about it, nor which of the
      competing fixes is correct. The witnessed failure: an AI agent "fixed
      forward" by inventing type-invalid `traces-to` links on sibling
      artifacts — turning 26 warnings into 8 errors — because the message
      gave no remedy and no trade-off (fix the artifact vs. widen the schema).

      Goal: least-friction remediation for an AI agent. Each remediable
      diagnostic carries structured, ordered fix options (artifact-fix first,
      schema-fix second), each tagged so a tool can tell "edit the artifact"
      from "edit the schema", plus a pointer to a per-rule explanation doc.

      Design (REQ-106 register: build on existing patterns, no new directive):
      remediation is a post-construction pass keyed by `Diagnostic.rule`
      (`rivet_core::remediation::remediation_for(diag, schema, store)`),
      re-deriving specifics from the live schema + store the renderer already
      holds — NOT a field threaded through the ~70 Diagnostic construction
      sites, keeping the hot validation path untouched. Rendered as a
      rustc-style `help:` block in text and a `remediation` object in
      `--format json`; the doc is `rivet docs diagnostics/<rule>`. Rules
      covered (8): `link-target-type` (the witnessed case), `broken-link`,
      `unknown-link-type`, `required-field`, `allowed-values`, `cardinality`,
      `unknown-field`, `known-type`. Each re-derives its specifics (offending
      link/field/value, the schema menu of allowed alternatives) faithfully
      from schema+store, matching the validator's own logic. Traceability
      forward/backlink diagnostics key on the dynamic `rule.id` (not a fixed
      string) and are a follow-up increment.

      Acceptance:
        - `rivet validate` text output on a type-invalid link shows a
          `help:` block with an artifact-fix and a schema-fix, ending with
          `see:  rivet docs diagnostics/link-target-type`.
        - `rivet validate --format json` carries a `remediation` object with
          `summary`, ordered `fix_options[].kind` (`fix-artifact` |
          `adjust-schema`), `targets`, and `doc_topic` on the offending
          diagnostic.
        - `rivet docs diagnostics/<rule>` resolves for all 8 covered rules.
        - For menu-bearing rules (unknown-link-type, allowed-values,
          unknown-field, known-type) the artifact-fix lists exactly the
          schema's declared alternatives (matching the validator's own
          known-set), and the schema-fix names the offending value/type.
        - Diagnostics for rules without guidance render unchanged (bare).
    tags: [validate, diagnostics, remediation, agent-ux, user-reported, dx]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-125
    type: requirement
    title: "validate/coverage --explain: the oracle teaches its own reasoning on every fire"
    status: draft
    description: |
      Sourced from a source-level study of garrytan/gbrain
      (`src/core/search/explain-formatter.ts` — every ranking decision is
      stamped onto the result and `--explain` replays the derivation),
      reshaped to rivet's deterministic register. The transferable asset is
      the discipline, not gbrain's fuzzy core: make the mechanical verdict
      legible.

      rivet has the analogous OPACITY problem with none of gbrain's
      fuzziness: when `rivet validate` emits a diagnostic, or
      `{{coverage}}`/`{{matrix}}` reports a number, or a traceability rule
      marks an artifact uncovered, the *why* is implicit. The gate fires red
      and a human (or agent) must INFER the reason. `--explain` closes that
      loop: the oracle stops being a wall you bump into and becomes a wall
      that says why it is there — CI-as-curriculum made explicit, and the
      key to handing the gate to a junior or an agent.

      Crucially this is a pure FORMATTER over data `validate::Diagnostic`
      and `coverage` already compute: which traceability rule fired, which
      `required-link`/`required-backlink` it checked, which `LinkGraph`
      edges were consulted, which condition failed. No new computed value,
      no scoring, no heuristic, no new failure surface — it surfaces
      computation already trusted.

      Acceptance:
        - `rivet validate --explain <ID>` prints, per diagnostic on <ID>,
          the rule id + the specific schema constraint + the edges/fields
          that produced the verdict; deterministic (same input -> byte-
          identical output, diff-checkable against a fixture).
        - `rivet coverage --explain <RULE>` shows why each artifact got its
          covered/uncovered classification.
        - `--explain` introduces NO new number; an automated check asserts
          the explained values equal the un-explained `Diagnostic`/coverage
          output.
    tags: [validate, coverage, explainability, audit, ci-as-curriculum, gbrain-derived]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-126
    type: requirement
    title: "Baseline-snapshot drift gate — with rebaselining as a gated, trailer-carrying act"
    status: draft
    description: |
      Sourced from garrytan/gbrain's two-gate eval harness
      (`src/commands/eval-gate.ts`: regression-baseline + correctness gate,
      both fail-CLOSED on any throw), reshaped to rivet's deterministic,
      non-probabilistic register. It is the general form of the
      staleness/drift problem: artifacts silently ceasing to match reality
      between releases. rivet runs `rivet validate`/`rivet commits` in CI but
      has no committed *baseline snapshot* check, so a refactor that changes
      coverage %, matrix coverage, or diagnostic counts is not caught
      mechanically. The primitive exists (`snapshot.rs`, `delta=NAME` in
      `embed.rs`); this formalizes it on existing patterns.

      THE TRAP (must be designed in, not bolted on): the baseline file is
      itself an artifact that can rot. If `.rivet/baseline.json` is
      regenerated carelessly ("tests were red, I rebaselined to green") the
      gate launders drift and reintroduces the green-because-unobserved
      failure mode. Therefore rebaselining MUST be a gated, reviewed,
      rationale-carrying act — never a convenience. The word doing the work
      is UNEXPLAINED: drift accompanied by a commit trailer/rationale is
      acceptable; drift without one fails closed. Updating the baseline must
      be harder than fixing the drift, or the gate trains people to launder
      drift into the baseline.

      Acceptance:
        - `rivet snapshot baseline` emits a deterministic JSON snapshot
          (artifact count by type, coverage %, per-rule coverage, diagnostic
          counts by severity, matrix coverage); byte-identical for identical
          input.
        - `rivet check --baseline <file>` exits non-zero on any delta,
          printing the changed metric(s); exits 0 when unchanged.
        - Fail-closed: any error computing the snapshot makes the check FAIL,
          never silently pass.
        - Rebaselining is gated: a baseline update is only accepted with an
          explicit rationale (commit trailer or `--reason`), and CI can
          distinguish an explained rebaseline from an unexplained one.
    tags: [validate, snapshot, regression-gate, ci, fail-closed, anti-laundering, gbrain-derived]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-101

  - id: REQ-127
    type: requirement
    title: "MCP fail-closed invariant: lock that no tool can override schema/externals resolution"
    status: draft
    description: |
      Sourced from garrytan/gbrain's `src/core/schema-pack/op-trust-gate.ts`
      (a remote caller is refused a per-call schema override unless the
      context is explicitly trusted — fail-closed). gbrain needed this
      because multi-user/remote callers forced it; rivet has not been forced
      yet because it has been single-operator.

      AUDIT (performed 2026-05-30 against rivet-cli/src/mcp.rs): the hole
      does NOT exist today. Schema/store/graph are resolved ONCE at
      `RivetServer::new -> load_project(project_dir)` and held in an RwLock;
      `project_dir` is a fixed `Arc<PathBuf>`. `rivet_validate` takes no
      params. Every `*Params` struct carries only data fields (ids, filters,
      titles, query strings) — NONE exposes a schema, externals, path,
      project, or config override. `rivet_reload` re-runs `load_project` on
      the SAME fixed dir. So rivet's MCP gate is currently fail-closed —
      but BY OMISSION, not by an explicit, tested invariant.

      The risk is latent: the day someone adds a convenience param (e.g.
      "validate against this schema" / "use this externals set") to an MCP
      tool, the hole opens silently with no guard. This REQ LOCKS the
      property as an explicit invariant + regression test, so the gate can
      never be told to look the other way by whoever drives the agent.
      Directly reinforces REQ-051 ("only CI enforcement provides
      traceability assurance"). Scope: hardening, not an urgent fix.

      Acceptance:
        - A documented invariant: no MCP tool parameter may alter schema or
          externals resolution for a validate/query/coverage/get operation.
        - A regression test asserts that an MCP-context validate cannot be
          pointed at an alternate schema/externals to suppress a diagnostic
          the default resolution emits (the test fails if such a param is
          ever introduced).
        - If a per-call override is ever genuinely needed, it is gated to
          trusted/local context with a fail-closed default (untrusted unless
          explicitly local), mirroring gbrain's `ctx.remote ?? true`.
    tags: [mcp, security, fail-closed, validate, audited, REQ-051, gbrain-derived]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-051

  - id: REQ-128
    type: requirement
    title: "rivet list --orphans: mechanical detection of asserted-but-unanchored claims"
    status: draft
    description: |
      Sourced from garrytan/gbrain's `src/core/search/graph-signals.ts`
      (inbound-link / cross-source signals), taking ONLY the deterministic
      structural-graph part and rejecting the ML/boost layer entirely.

      In rivet's domain an orphan is not a lonely note — it is a requirement
      with no verifying test, a design decision linked to no hazard, a
      safety-case node nothing traces to: an asserted-but-unjustified claim,
      the exact knowledge-graph failure mode rivet's epistemics exist to
      prevent. So `--orphans` is not a nice-to-have report; it is the
      standing query that answers "does the graph contain any claim that
      isn't anchored?" — mechanical enforcement of the thing the toolchain
      rests on. rivet already computes the signal
      (`links::LinkGraph::backlinks_to`, `reachable-from`/`reachable-to` in
      `sexpr_eval.rs`); the gap is surfacing it. No scoring, no fuzziness —
      ordering is by exact integer link counts only.

      Acceptance:
        - `rivet list --orphans` lists artifacts with no inbound AND no
          outbound links (the existing orphan diagnostic as a queryable
          view).
        - A structural report (CLI and/or a `{{group:backlinks}}` embed)
          ranks artifacts by inbound-link count using `LinkGraph`,
          deterministic and reproducible.
        - No relevance/semantic ranking is introduced; ordering is exact
          integer counts only.
    tags: [query, graph, backlinks, orphans, epistemics, gbrain-derived]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-129
    type: requirement
    title: "validate --review-candidates: report the unclassifiable without accepting it"
    status: draft
    description: |
      Sourced from garrytan/gbrain's `src/core/schema-pack/candidate-audit.ts`
      (when a page's type isn't in the active pack, gbrain neither silently
      accepts nor silently drops it — it appends a SHA-redacted record to a
      rotating audit and surfaces `schema review-candidates`). The adaptation
      keeps gbrain's CAUTION and drops its ACCEPTANCE.

      rivet must NOT accept unknown types — that stays a hard validation error
      (loud-fail default preserved). But a structured "what did validation see
      that it could not classify" REPORT is legitimately useful for schema
      evolution: unknown-type artifacts, fields not in any loaded schema, and
      unresolved `externals` `prefix:ID` references. This is a reporting view,
      never an ingest/accept path.

      SAFETY CONSTRAINT (must be designed in): the redaction must be genuinely
      one-way (e.g. SHA of content, names of frontmatter keys but never their
      values) and the report must NOT become a side channel that leaks
      artifact content past the classification boundary. For most rivet
      content this is moot, but if any artifact carries a constraint that must
      never be named publicly, the candidate report is a place that constraint
      has to hold. Cheap to get right, expensive to get wrong.

      Acceptance:
        - `rivet validate --review-candidates` prints a deterministic report
          of unknown-type artifacts, unknown fields, and unresolved externals
          refs — WITHOUT changing exit status semantics (unknown type is still
          an error; the report is additive).
        - The report emits only redacted identifiers (hashes / key names), never
          field values or descriptions; a test asserts no artifact body content
          appears in the output.
        - Default behavior is unchanged (loud-fail); the report is opt-in.
    tags: [validate, schema-evolution, audit, redaction, fail-closed, gbrain-derived]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-130
    type: requirement
    title: "Supersession as an additive typed link that preserves the node (not deletion)"
    status: draft
    description: |
      Sourced from garrytan/gbrain's `src/core/facts-fence.ts` (supersession is
      an explicit, in-record, reasoned act that survives in git — `~~claim~~`
      + `superseded by #N`), reshaped onto an existing rivet pattern rather
      than a new directive or a markdown fence parser.

      Standardize a `superseded-by:` + `superseded-reason:` field convention on
      the requirement schema (via schema `extends`), validated deterministically.

      SAFETY CONSTRAINT (the whole point): in a safety domain supersession is
      NOT deletion. A requirement that was live for three release cycles and is
      then superseded is part of the certification evidence trail, not garbage
      to hide. gbrain's git-delete -> soft-delete model is WRONG here.
      `superseded-by` MUST be an additive typed graph edge that PRESERVES the
      node — the superseded artifact stays in the store and in coverage, just
      marked. It is never a soft-delete and never drops the node from the
      traceability graph.

      Acceptance:
        - `superseded-by: REQ-NNN` + `superseded-reason:` validate
          deterministically: target must exist; an artifact cannot supersede
          itself; reason is required when `superseded-by` is present.
        - A superseded artifact remains in the store, the link graph, and
          coverage reporting (marked superseded, not removed).
        - Built on schema `extends` + existing id-resolution (`prefix:ID`),
          introducing no new config surface or directive.
    tags: [schema, lifecycle, supersession, traceability, safety, gbrain-derived]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-010
