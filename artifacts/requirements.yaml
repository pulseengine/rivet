artifacts:
  - id: REQ-001
    type: requirement
    title: Text-file-first artifact management
    status: approved
    description: >
      All lifecycle artifacts are stored as YAML files in git repositories.
      No database or external service required for core operation.
    tags: [core, hook-test]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-002
    type: requirement
    title: STPA artifact support
    status: approved
    description: >
      Support STPA methodology artifacts: losses, hazards, sub-hazards,
      system constraints, control structure, UCAs, controller constraints,
      and loss scenarios with proper link semantics.
    tags: [stpa, safety]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-003
    type: requirement
    title: Automotive SPICE V-model traceability
    status: approved
    description: >
      Support full ASPICE V-model artifact types from stakeholder requirements
      through system/software requirements, architecture, design, and testing
      with bidirectional traceability rules.
    tags: [aspice, automotive]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-004
    type: requirement
    title: Validation engine
    status: approved
    description: >
      Validate link integrity, coverage completeness, required fields,
      allowed values, and traceability rules. Report diagnostics with
      severity levels (error, warning, info).
    tags: [core, validation]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-1
      - type: satisfies
        target: SC-3
      - type: satisfies
        target: SC-15
      - type: constraint-satisfies
        target: SC-1
      - type: constraint-satisfies
        target: SC-3
      - type: constraint-satisfies
        target: SC-15
      - type: constraint-satisfies
        target: SC-LSP-002
      - type: constraint-satisfies
        target: SC-LSP-004
      - type: constraint-satisfies
        target: SC-IMPL-001
      - type: constraint-satisfies
        target: SC-IMPL-004
      - type: constraint-satisfies
        target: SC-IMPL-006
      - type: constraint-satisfies
        target: CC-C-1
      - type: constraint-satisfies
        target: CC-C-2
      - type: constraint-satisfies
        target: CC-C-3
      - type: constraint-satisfies
        target: CC-C-4
      - type: constraint-satisfies
        target: CC-C-5
      - type: constraint-satisfies
        target: CC-C-6
      - type: constraint-satisfies
        target: CC-C-7
      - type: constraint-satisfies
        target: CC-C-8
      - type: constraint-satisfies
        target: CC-C-9
      - type: constraint-satisfies
        target: CC-C-24
      - type: constraint-satisfies
        target: CC-C-25
      - type: constraint-satisfies
        target: CC-D-3
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-005
    type: requirement
    title: ReqIF 1.2 import/export
    status: approved
    description: >
      Support OMG ReqIF 1.2 XML format for cross-organization requirements
      interchange. Import ReqIF into trace artifacts, export trace artifacts
      to ReqIF.
    tags: [interchange, reqif]
    fields:
      priority: should
      category: interface
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-4
      - type: satisfies
        target: SC-9
      - type: constraint-satisfies
        target: SC-4
      - type: constraint-satisfies
        target: SC-9
      - type: constraint-satisfies
        target: CC-Q-1
      - type: constraint-satisfies
        target: CC-Q-2
      - type: constraint-satisfies
        target: CC-Q-3
      - type: constraint-satisfies
        target: CC-Q-4
      - type: constraint-satisfies
        target: CC-Q-5
      - type: constraint-satisfies
        target: CC-Q-6
      - type: constraint-satisfies
        target: CC-Q-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-006
    type: requirement
    title: OSLC-based tool synchronization
    status: approved
    description: >
      Bidirectional sync with Polarion, DOORS, and codebeamer via OSLC
      (Open Services for Lifecycle Collaboration) rather than per-tool
      REST adapters. Support RM, QM, CM domains.
    tags: [oslc, sync, interchange]
    fields:
      priority: should
      category: interface
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-5
      - type: satisfies
        target: SC-8
      - type: constraint-satisfies
        target: SC-5
      - type: constraint-satisfies
        target: SC-8
      - type: constraint-satisfies
        target: CC-O-1
      - type: constraint-satisfies
        target: CC-O-2
      - type: constraint-satisfies
        target: CC-O-3
      - type: constraint-satisfies
        target: CC-O-4
      - type: constraint-satisfies
        target: CC-O-5
      - type: constraint-satisfies
        target: CC-O-6
      - type: constraint-satisfies
        target: CC-O-7
      - type: constraint-satisfies
        target: CC-O-8
      - type: constraint-satisfies
        target: CC-O-9
      - type: constraint-satisfies
        target: CC-O-10
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-007
    type: requirement
    title: CLI and serve pattern
    status: approved
    description: >
      Provide a CLI binary for validation, querying, and reporting.
      Support an HTTP serve mode (axum + HTMX) for dashboard visualization.
      No Tauri or desktop GUI.
    tags: [core, ui]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0
    links:
      - type: satisfies
        target: SC-18
      - type: constraint-satisfies
        target: SC-18
      - type: constraint-satisfies
        target: SC-22
      - type: constraint-satisfies
        target: SC-IMPL-003
      - type: constraint-satisfies
        target: CC-L-1
      - type: constraint-satisfies
        target: CC-L-2
      - type: constraint-satisfies
        target: CC-L-3
      - type: constraint-satisfies
        target: CC-L-4
      - type: constraint-satisfies
        target: CC-L-5
      - type: constraint-satisfies
        target: CC-D-1
      - type: constraint-satisfies
        target: CC-D-2
      - type: constraint-satisfies
        target: CC-D-4
      - type: constraint-satisfies
        target: CC-C-27
      - type: constraint-satisfies
        target: CC-M-1
      - type: constraint-satisfies
        target: CC-M-2
      - type: constraint-satisfies
        target: CC-M-3
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-008
    type: requirement
    title: WASM component adapters
    status: approved
    description: >
      Support pluggable adapters as WASM components via WIT interface.
      Adapters can import/export artifacts for custom formats.
    tags: [extensibility, wasm]
    fields:
      priority: should
      category: functional
      baseline: v0.1.0
    links:
      - type: satisfies
        target: SC-16
      - type: constraint-satisfies
        target: SC-16
      - type: constraint-satisfies
        target: CC-C-21
      - type: constraint-satisfies
        target: CC-C-22
      - type: constraint-satisfies
        target: CC-C-23
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-009
    type: requirement
    title: Test results as release evidence
    status: approved
    description: >
      Tie test execution results (JUnit XML, coverage reports) to
      GitHub releases as per-version evidence. Support fetching and
      caching from release assets.
    tags: [testing, evidence]
    fields:
      priority: should
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-6
      - type: constraint-satisfies
        target: SC-6
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-010
    type: requirement
    title: Schema-driven validation
    status: approved
    description: >
      Artifact types, fields, link constraints, and traceability rules
      are defined in mergeable YAML schemas. The tool validates artifacts
      against loaded schemas.
    tags: [core, schema]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-1
      - type: constraint-satisfies
        target: SC-IMPL-005
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-011
    type: requirement
    title: Rust edition 2024 with MSRV 1.85
    status: approved
    description: >
      Use Rust edition 2024. Maintain minimum supported Rust version of 1.85.
    tags: [toolchain]
    fields:
      priority: must
      category: constraint
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-012
    type: requirement
    title: Comprehensive CI quality gates
    status: approved
    description: >
      CI pipeline must include fmt, clippy -D warnings, tests, security audit,
      cargo-deny, code coverage, Miri, proptest, supply chain verification,
      and MSRV check.
    tags: [ci, quality]
    fields:
      priority: must
      category: non-functional
      baseline: v0.1.0
    links:
      - type: constraint-satisfies
        target: CC-I-1
      - type: constraint-satisfies
        target: CC-I-2
      - type: constraint-satisfies
        target: CC-I-3
      - type: constraint-satisfies
        target: CC-I-4
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-013
    type: requirement
    title: Performance benchmarks with KPI baselines
    status: approved
    description: >
      Criterion benchmarks for core operations (store, link graph, validation,
      matrix) at multiple artifact scales (100, 1000, 10000). Results serve
      as performance KPI baselines and regression detection.
    tags: [testing, performance]
    fields:
      priority: must
      category: non-functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-014
    type: requirement
    title: ASPICE SWE.4/5/6 test structure
    status: approved
    description: >
      Test suite organized to mirror ASPICE verification levels.
      SWE.4 unit verification (proptest, Miri), SWE.5 integration
      verification (cross-module tests), SWE.6 qualification testing
      (full pipeline validation, benchmark KPIs).
    tags: [testing, aspice]
    fields:
      priority: must
      category: non-functional
      baseline: v0.1.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-015
    type: requirement
    title: ASPICE 4.0 aligned schemas
    status: approved
    description: >
      Schema definitions must align with Automotive SPICE v4.0 process
      reference model. SWE.5 is "Software Component Verification and
      Integration Verification", SWE.6 is "Software Verification".
      Use "verification measure" terminology instead of "test".
      Expanded method values (static-analysis, formal-verification,
      simulation, inspection, walkthrough). Verification-criteria field
      on requirement types.
    tags: [aspice, schema]
    fields:
      priority: must
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-4
      - type: constraint-satisfies
        target: SC-4
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-016
    type: requirement
    title: Cybersecurity schema (ISO 21434 / ASPICE SEC.1-4)
    status: approved
    description: >
      Support automotive cybersecurity engineering artifacts aligned with
      ISO/SAE 21434 and ASPICE v4.0 SEC.1-4 processes. Cover TARA
      (assets, threats, risk assessment), cybersecurity goals/requirements,
      cybersecurity design, implementation, and verification.
    tags: [cybersecurity, aspice, schema]
    fields:
      priority: should
      category: functional
      baseline: v0.1.0

    links:
      - type: satisfies
        target: SC-4
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-017

    type: requirement
    title: Commit-to-artifact traceability
    status: approved
    description: >
      The system must parse git commit trailers and link them to artifacts
      at runtime, injecting ephemeral commit nodes into the link graph.
      Must support configurable trailer-to-link-type mapping and produce
      five report types: linked commits, broken references, orphan commits,
      artifact commit coverage, and unimplemented artifacts.
    tags: [core, traceability, git]
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-7
      - type: satisfies
        target: SC-17
      - type: constraint-satisfies
        target: SC-7
      - type: constraint-satisfies
        target: SC-17
      - type: constraint-satisfies
        target: CC-C-18
      - type: constraint-satisfies
        target: CC-C-19
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-018
    type: requirement
    title: Commit message validation at commit time
    status: approved
    description: >
      The system must validate commit messages via a pre-commit hook,
      ensuring non-exempt commits reference at least one valid artifact
      ID in git trailers. Must support conventional-commit type exemptions
      and an explicit skip trailer for opt-out.
    tags: [core, validation, git]
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-019
    type: requirement
    title: Orphan commit detection
    status: approved
    description: >
      The system must identify commits that modify files in configured
      traced paths without referencing any artifact. Must support
      path-based configuration for traced vs. exempt directories and
      an artifact whitelist for known-unimplemented items.
    tags: [core, traceability, git]
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-020
    type: requirement
    title: Cross-repository artifact linking via prefixed IDs
    status: approved
    description: >
      Rivet projects must be able to declare external dependencies on other
      rivet repositories and reference their artifacts using prefix:ID syntax.
    tags: [cross-repo, traceability]
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-10
      - type: satisfies
        target: SC-19
      - type: constraint-satisfies
        target: SC-10
      - type: constraint-satisfies
        target: SC-19
      - type: constraint-satisfies
        target: CC-C-20
      - type: constraint-satisfies
        target: CC-L-6
      - type: constraint-satisfies
        target: CC-L-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-021
    type: requirement
    title: Distributed baselining via convention tags
    status: approved
    description: >
      Multiple rivet repositories must be able to form consistent baselines
      using git tags without requiring a central platform repository.
    tags: [cross-repo, baseline]
    fields:
      priority: should
      category: functional
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-10
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-022
    type: requirement
    title: Single-binary WASM asset embedding
    status: draft
    description: >
      The rivet binary must optionally embed all WASM and JavaScript assets
      so it can be distributed as a single self-contained executable.
    tags: [packaging, wasm]
    fields:
      priority: should
      category: functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-023
    type: requirement
    title: Conditional validation rules
    status: approved
    description: >
      The validation engine must support conditional rules where field
      requirements or link cardinality depend on the value of another field.
      For example, an artifact with status "approved" must have a non-empty
      verification-criteria field, or an artifact with safety level ASIL_B
      must have mitigated_by links. This enables safety-critical constraint
      enforcement that static per-type validation cannot express.
      Contradictory conditional rules must be detected at schema load time.
    tags: [validation, schema, safety]
    links:
      - type: satisfies
        target: SC-12
      - type: constraint-satisfies
        target: SC-12
      - type: constraint-satisfies
        target: CC-C-12
    fields:
      priority: should
      category: functional
      upstream-ref: "eclipse-score/docs-as-code#180"
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-024
    type: requirement
    title: Change impact analysis
    status: approved
    description: >
      The system must detect which artifacts changed between two baselines
      or commits and compute the transitive set of downstream artifacts
      affected via the link graph. This supports change management workflows
      required by ASPICE SUP.10 and ISO 26262 part 8.
    tags: [traceability, baseline, safety]
    fields:
      priority: should
      category: functional
      upstream-ref: "eclipse-score/docs-as-code#314, eclipse-score/process_description#535"
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-3
      - type: constraint-satisfies
        target: SC-IMPL-002
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-025
    type: requirement
    title: sphinx-needs JSON import
    status: approved
    description: >
      The system must import artifacts from the sphinx-needs needs.json
      export format, mapping sphinx-needs types, links, and fields to
      rivet schema types via configurable mappings. This provides a
      migration path for projects using sphinx-needs-based toolchains.
    tags: [interchange, adapter, migration]
    fields:
      priority: should
      category: interface
      upstream-ref: "eclipse-score/score#1695"
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-4
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-026
    type: requirement
    title: Test-to-requirement traceability extraction
    status: approved
    description: >
      The system must extract traceability markers from test source code
      and test results, linking test cases to requirements without requiring
      manual YAML maintenance. Must support language-specific markers
      (attributes, decorators, comments) and test result formats (JUnit XML,
      cargo test JSON).
    tags: [testing, traceability, automation]
    fields:
      priority: should
      category: functional
      upstream-ref: "eclipse-score/score#2521, eclipse-score/score#2619"
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-7
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-027

    type: requirement
    title: Build-system-aware cross-repo discovery
    status: approved
    description: >
      The system must discover cross-repo dependencies from build system
      manifests (Bazel MODULE.bazel, Nix flake.lock, or custom manifests)
      rather than requiring manual external declarations. This includes
      resolving pinned commits, workspace paths, and source code locations
      across the dependency graph for traceability validation and source
      code linking.
    tags: [cross-repo, bazel, nix, traceability]
    fields:
      priority: should
      category: functional
      upstream-ref: "eclipse-score/reference_integration (known_good.json)"
      baseline: v0.2.0-dev

    links:
      - type: satisfies
        target: SC-10
      - type: constraint-satisfies
        target: SC-20
      - type: constraint-satisfies
        target: CC-C-15
      - type: constraint-satisfies
        target: CC-C-16
      - type: constraint-satisfies
        target: CC-C-17
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z
  - id: REQ-028
    type: requirement
    title: Diagnostic-quality parsing with lossless syntax trees
    status: approved
    description: >
      All parsers for build-system manifests and configuration files must
      produce lossless concrete syntax trees (CST) with full span information
      for byte-exact error reporting. Parsers must recover from errors and
      produce partial results rather than failing completely. Uses rowan
      for CST representation, consistent with the spar AADL toolchain.
    tags: [parsing, rowan, diagnostics]
    links:
      - type: satisfies
        target: SC-13
      - type: constraint-satisfies
        target: SC-13
      - type: constraint-satisfies
        target: SC-LSP-001
      - type: constraint-satisfies
        target: SC-LSP-005
    fields:
      priority: must
      category: non-functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-029
    type: requirement
    title: Incremental validation via dependency-tracked computation
    status: approved
    description: >
      The validation pipeline must support incremental recomputation where
      changing a single artifact file only re-evaluates affected validation
      rules, link graph edges, and coverage computations. Uses salsa for
      dependency tracking, consistent with the spar AADL toolchain. This
      enables sub-millisecond revalidation for IDE integration and efficient
      conditional rule evaluation. Incremental results must be identical
      to full validation results for the same inputs.
    tags: [validation, salsa, incremental, performance]
    links:
      - type: satisfies
        target: SC-11
      - type: constraint-satisfies
        target: SC-11
      - type: constraint-satisfies
        target: SC-21
      - type: constraint-satisfies
        target: SC-LSP-003
      - type: constraint-satisfies
        target: SC-LSP-006
      - type: constraint-satisfies
        target: SC-LSP-007
      - type: constraint-satisfies
        target: CC-C-10
      - type: constraint-satisfies
        target: CC-C-11
      - type: constraint-satisfies
        target: CC-C-13
      - type: constraint-satisfies
        target: CC-C-14
      - type: constraint-satisfies
        target: CC-C-26
    fields:
      priority: must
      category: non-functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-031
    type: requirement
    title: Schema-validated artifact mutation from CLI
    status: approved
    description: >
      The CLI must provide commands to create, modify, remove, link, and
      unlink artifacts directly, with full schema validation at write time.
      All mutations must validate the artifact ID is unique (or exists for
      modify), the type exists in the loaded schema, required fields are
      present, link targets exist, link types are valid for the source and
      target types, and status values are in the allowed set. The CLI must
      write valid YAML that preserves existing file formatting and comments
      where possible. This eliminates the need for external agents or manual
      YAML editing to maintain artifacts correctly.
    tags: [cli, mutation, validation, safety]
    links:
      - type: satisfies
        target: SC-1
      - type: satisfies
        target: SC-2
      - type: constraint-satisfies
        target: SC-2
    fields:
      priority: must
      category: functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-030
    type: requirement
    title: Formal correctness guarantees for validation engine
    status: draft
    description: >
      Core validation algorithms must have formal correctness proofs at
      three levels. Bounded model checking (Kani) for panic freedom.
      Functional correctness proofs (Verus) for validation soundness
      and completeness. Metamodel semantic proofs (Rocq/coq-of-rust)
      for schema satisfiability and rule consistency. These proofs
      serve as ISO 26262 tool qualification evidence at TCL 1.
      Proofs must verify the actual implementation, not a separate model.
    tags: [formal-verification, safety, tool-qualification]
    links:
      - type: satisfies
        target: SC-14
      - type: constraint-satisfies
        target: SC-14
    fields:
      priority: should
      category: non-functional
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-032
    type: requirement
    title: Markdown rendering in artifact descriptions
    status: approved
    description: >
      Artifact descriptions rendered as CommonMark HTML via pulldown-cmark with tables, strikethrough, task lists, and code blocks.
    tags: [rendering, markdown]
    fields:
      category: functional
      priority: should
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-033
    type: requirement
    title: Rich artifact embedding in documents with schema-driven link traversal
    status: approved
    description: >
      Document embeds support modifiers (full, links, upstream, downstream, chain, table) with schema-driven link traversal.
    tags: [documents, embedding, traceability]
    fields:
      category: functional
      priority: should
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-034
    type: requirement
    title: YAML 1.2.2 spec-compliant artifact file editing
    status: approved
    description: >
      YAML artifact file editing uses indentation-aware parser per YAML 1.2.2 spec for lossless modification.
    tags: [yaml, parsing, spec-compliance]
    fields:
      category: functional
      priority: must
      baseline: v0.2.0-dev
    links:
      - type: satisfies
        target: SC-2
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-035
    type: requirement
    title: HTML export includes rendered documents with resolved embeds
    status: approved
    description: >
      HTML export includes rendered documents with resolved wiki-links and artifact embeds as static pages.
    tags: [export, documents]
    fields:
      category: functional
      priority: must
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-036
    type: requirement
    title: HTML export supports version switcher and homepage link
    status: approved
    description: >
      HTML export supports runtime config.js for version switcher dropdown and homepage back-link.
    tags: [export, navigation, versioning]
    fields:
      category: functional
      priority: should
      baseline: v0.2.0-dev
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-037
    type: requirement
    title: SysML v2 artifact import
    status: draft
    description: >
      Import requirement and constraint elements from SysML v2 textual notation
      files (.sysml) as rivet artifacts. Parse SysML v2 using a rowan-based
      lossless parser (same pattern as spar's AADL parser). Extract requirement
      elements with their attributes, rationale, and satisfy/verify relationships,
      mapping them to rivet artifact YAML.
    tags: [sysml, adapter, future]
    fields:
      priority: should
      category: functional
      baseline: v0.3.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-038
    type: requirement
    title: SysML v2 to AADL model lowering
    status: draft
    description: >
      Lower SysML v2 system models to AADL execution platform models where
      timing, scheduling, and resource binding semantics exist. Implements
      the mapping rules specified by SEI's "Extending SysML v2 with AADL
      Concepts" research. Enables multi-level architecture analysis:
      SysML v2 (system-level) to AADL (deployment-level) to implementation.
    tags: [sysml, aadl, architecture, future]
    fields:
      priority: could
      category: functional
      baseline: v0.3.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-040
    type: requirement
    title: Test report lifecycle integration
    status: draft
    description: >
      CI test runs produce structured results (JUnit XML) that are imported
      into rivet as test result artifacts. The workflow is: run tests, import
      results via rivet import --format junit, create a test report artifact
      in results/, conformance review, commit report as PR, include in
      release. Test evidence becomes part of the traceability chain — rivet
      dogfoods its own test tracking.
    tags: [testing, ci, dogfood, future]
    fields:
      priority: should
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-039
    type: requirement
    title: Draft-aware validation severity
    status: draft
    description: >
      Validation diagnostics for traceability rules (missing required links,
      incomplete coverage) must respect artifact maturity. Artifacts with
      status "draft" produce info-level diagnostics for unfulfilled links
      instead of errors. Only "active" or "approved" artifacts are held to
      full traceability enforcement. This distinguishes "not yet done" from
      "broken" in projects with work-in-progress artifacts.
    tags: [validation, workflow, core]
    fields:
      priority: must
      category: functional
      baseline: v0.3.0
    provenance:
      created-by: ai-assisted
      timestamp: 2026-04-07T03:40:26Z

  - id: REQ-041
    type: requirement
    title: S-expression query and constraint language
    status: draft
    description: >
      The system must provide a single s-expression-based language for
      artifact filtering, traceability queries, feature model constraints,
      and variant-scoped validation. The language must support predicates
      over artifact fields (type, status, tags, custom fields), logical
      connectives (and, or, not, implies, excludes), quantifiers (forall,
      exists) over artifact sets, link traversal predicates (linked-by,
      linked-from, reachable-from), and aggregation (count, ratio). The
      same syntax must be usable in CLI --filter flags, schema traceability
      rules, feature model constraints, and binding validation rules. No
      alternative syntax or sugar — one canonical form.
    tags: [core, query, s-expression]
    links:
      - type: constraint-satisfies
        target: SC-VAR-004
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-042
    type: requirement
    title: Feature model for product line engineering
    status: draft
    description: >
      The system must support FODA-style feature models that describe the
      logical problem space of a product line. Feature models define a
      tree of features with group types (mandatory, optional, alternative,
      or) and cross-tree constraints expressed as s-expressions (implies,
      excludes, requires). Feature models are stored as YAML artifacts
      separate from implementation artifacts, maintaining the separation
      between logical (problem space) and implementation (solution space)
      concerns. Feature models must be validatable independently — the
      constraint set must be satisfiable.
    tags: [variant, ple, feature-model]
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-043
    type: requirement
    title: Variant configuration with constraint solving
    status: draft
    description: >
      The system must support named variant configurations that select a
      subset of features from a feature model. A constraint solver must
      propagate implications (selecting feature A forces feature B via
      constraints), detect conflicts (mutually exclusive selections), and
      validate that the final feature set satisfies all cross-tree
      constraints. Invalid configurations must produce clear diagnostics
      identifying which constraints are violated. The solver must use
      propositional logic (boolean SAT) at minimum, with optional
      first-order extensions (forall/exists over feature sets) for
      advanced constraints.
    tags: [variant, ple, constraint-solver]
    links:
      - type: constraint-satisfies
        target: SC-VAR-002
      - type: constraint-satisfies
        target: SC-VAR-006
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-044
    type: requirement
    title: Feature-to-artifact binding model
    status: draft
    description: >
      The system must support a binding model that maps features from the
      logical feature model to implementation artifacts and source code
      globs. Bindings are stored in separate YAML files, maintaining the
      three-layer separation (feature model / variant config / binding).
      Each feature can bind to zero or more artifact IDs and zero or more
      source file glob patterns. Validation must detect: unbound features
      (feature with no artifacts), unbound artifacts (artifacts not mapped
      to any feature), and stale bindings (referencing deleted artifacts
      or non-existent source paths).
    tags: [variant, ple, binding]
    links:
      - type: constraint-satisfies
        target: SC-VAR-003
      - type: constraint-satisfies
        target: SC-VAR-005
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-045
    type: requirement
    title: Per-variant validation and coverage
    status: draft
    description: >
      The system must validate traceability and coverage independently for
      each variant configuration. Given a variant, the system derives the
      effective feature set, collects all bound artifacts, and validates
      that traceability chains are complete within that subset. Coverage
      reports must be per-variant. An --all-variants mode must validate
      every defined variant independently and report per-variant results.
      A variant that passes validation when the union of all artifacts
      passes must still fail if its own subset has broken traceability.
    tags: [variant, validation, coverage]
    links:
      - type: constraint-satisfies
        target: SC-VAR-001
      - type: constraint-satisfies
        target: SC-VAR-007
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-046
    type: requirement
    title: Variant-scoped CLI and API commands
    status: draft
    description: >
      All relevant CLI commands must accept --variant flags to scope
      operations to a specific product configuration. This includes
      validate, coverage, matrix, list, stats, impact, export, and the
      dashboard API endpoints. The --variant flag composes with existing
      --type and --status filters. rivet validate without --variant
      validates the union (all artifacts). rivet validate --variant X
      validates only the subset bound to variant X's features.
    tags: [variant, cli, api]
    fields:
      priority: should
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-12T12:00:00Z

  - id: REQ-047
    type: requirement
    title: MCP mutation audit logging
    status: draft
    description: >
      Every MCP write operation (rivet_add, rivet_modify, rivet_link,
      rivet_unlink, rivet_remove) must produce a structured JSON log entry
      containing: tool name, artifact ID, fields changed, old value,
      new value, timestamp, and MCP session ID. The log must be append-only
      and written to a configurable path (default: .rivet/mcp-audit.jsonl).
      This enables forensic analysis of AI agent activity and rollback of
      unintended changes. Without audit logging, a prompt-injected agent's
      modifications cannot be detected or reversed.
    tags: [mcp, security, audit, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-005
      - type: constraint-satisfies
        target: SSC-TQ-002
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-048
    type: requirement
    title: Regex complexity bounds in s-expression evaluator
    status: draft
    description: >
      The matches predicate in the s-expression evaluator must enforce
      a size limit on compiled regex patterns (via regex::RegexBuilder::
      size_limit) and a match timeout to prevent ReDoS attacks. Invalid
      or oversized patterns must produce a clear diagnostic rather than
      hanging. Default size limit: 1MB. Default match timeout: 1 second.
    tags: [security, s-expression, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-001
      - type: constraint-satisfies
        target: SSC-TQ-001
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-049
    type: requirement
    title: Export validation embedding
    status: draft
    description: >
      The Zola and HTML export commands must embed the validation result
      (PASS/FAIL, error count, warning count, timestamp, git commit) into
      the exported output. For Zola: written to data/<prefix>/validation.json.
      For HTML: embedded in the index page. Consumers can verify the export
      was generated from a validated project state. A --clean flag must
      remove stale content/<prefix>/ before writing to prevent deleted
      artifacts from persisting as published pages.
    tags: [export, validation, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-003
      - type: constraint-satisfies
        target: SSC-TQ-004
    fields:
      priority: must
      category: functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-050
    type: requirement
    title: Import link-target verification
    status: draft
    description: >
      After importing artifacts from needs-json or ReqIF, the import
      command must verify that all link targets exist in the combined
      store (existing project + imported artifacts). Unresolved targets
      must be reported as warnings with the specific artifact ID and
      link type. A --strict flag must promote unresolved warnings to
      errors, blocking the import. This prevents crafted imports from
      creating traceability links to non-existent artifacts.
    tags: [import, validation, security, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-004
      - type: constraint-satisfies
        target: SSC-TQ-003
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-051
    type: requirement
    title: CI-enforced traceability (hooks are convenience only)
    status: draft
    description: >
      The CI pipeline must include rivet commits and rivet validate as
      required checks, independent of local git hooks. Git hooks (commit-msg,
      pre-commit) are convenience tooling that can be bypassed via
      --no-verify. The system documentation must explicitly state that
      hooks do not provide security assurance — only CI enforcement does.
      A rivet validate --check-hooks flag should report whether hooks are
      installed, but hook absence must not block local development.

      Status (2026-06-06): the in-CI gating MECHANISM is now wired — see
      REQ-212, which added a `traceability` job to ci.yml running `rivet
      validate` (fail on errors) + `rivet commits` (orphan/broken-ref gate on
      PRs). The hook-security documentation clause is met (CLAUDE.md "Hook
      Security Model"). REMAINING before this can flip to implemented:
      (1) the job must be added to branch protection's REQUIRED status checks
      so it actually blocks merges (operator/repo-settings action, can't live
      in-repo; the empty required-checks set is tracked in #436); and (2) the
      `rivet validate --check-hooks` flag does not yet exist. Kept `draft`
      until both are done — the job running but not blocking is exactly the
      advisory-gate trap #436 describes.
    tags: [ci, hooks, security, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-006
      - type: constraint-satisfies
        target: SSC-TQ-005
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-052
    type: requirement
    title: Variant solver fuzz testing
    status: draft
    description: >
      The feature model constraint solver must be tested with randomly
      generated feature models (proptest) to verify: (a) solver never
      panics, (b) solver result is consistent with brute-force enumeration
      for small models (≤10 features), (c) every resolved variant satisfies
      all constraints when re-checked. This addresses the gap between the
      12 hand-written unit tests and the combinatorial space of real
      feature models.
    tags: [variant, testing, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-002
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-053
    type: requirement
    title: Quantifier scope correctness testing
    status: draft
    description: >
      The forall/exists evaluator must have tests that verify: adding an
      artifact to the store changes quantifier results, removing an artifact
      changes results, and the store parameter is the actual iteration set
      (not a cached or global set). These tests must use different store
      contents to confirm scoping, not just check the happy path.
    tags: [s-expression, testing, tool-qualification]
    links:
      - type: constraint-satisfies
        target: SC-TQ-007
    fields:
      priority: must
      category: non-functional
      baseline: v0.4.0
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-6
      timestamp: 2026-04-13T12:00:00Z

  - id: REQ-054
    type: requirement
    title: Independent verification for AI-generated tests
    status: draft
    description: >
      Every safety-critical module with AI-generated tests must also have proptest properties and/or Kani proofs verifying mathematical invariants.

    tags: [ai, testing, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: must
    links:
      - type: constraint-satisfies
        target: SC-AI-001
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-055
    type: requirement
    title: Human review gate for AI-generated STPA
    status: draft
    description: >
      AI STPA must remain draft until human domain expert reviews.
    tags: [ai, stpa, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: must
    links:
      - type: constraint-satisfies
        target: SC-AI-002
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-056
    type: requirement
    title: Semantic review checklist for bulk AI artifact creation
    status: draft
    description: >
      More than 5 artifacts in one session needs semantic review checklist.
    tags: [ai, review, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: should
    links:
      - type: constraint-satisfies
        target: SC-AI-003
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-057
    type: requirement
    title: Assumption documentation in AI-generated modules
    status: draft
    description: >
      AI-written modules must document runtime assumptions in headers.
    tags: [ai, documentation, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: should
    links:
      - type: constraint-satisfies
        target: SC-AI-004
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-058
    type: requirement
    title: Risk-proportional PR review for AI-generated code
    status: draft
    description: >
      Safety-critical paths need manual edge case checks.
    tags: [ai, review, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: must
    links:
      - type: constraint-satisfies
        target: SC-AI-005
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-059
    type: requirement
    title: AI provenance with model ID and session context
    status: draft
    description: >
      Provenance must include model ID, Co-Authored-By, and session refs.
    tags: [ai, provenance, tool-qualification]
    fields:
      baseline: v0.4.0
      category: non-functional
      priority: must
    links:
      - type: constraint-satisfies
        target: SC-AI-006
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-7
      timestamp: 2026-04-22T20:29:48Z

  - id: REQ-084
    type: requirement
    title: "Verus proof CI must fail loudly when its Nix install fails — a continue-on-error proof job that verifies nothing is a silent failure"
    status: draft
    description: |
      The Verus Proofs CI job (.github/workflows/ci.yml) installs Nix
      via DeterminateSystems/nix-installer-action, then runs
      `bazel test //verus:rivet_specs_verify`. The job is
      `continue-on-error: true` — intentionally soft-gated, because
      Verus has an open SMT proof gap (ci.yml lines 555-558).

      On the self-hosted runner (`[self-hosted, linux, x64, lean-mem]`,
      NoNewPrivileges=true, no sudo) the `nix-installer` binary
      escalates via `sudo` and fails; the `Install Nix` step errors and
      `Verify Verus specs` is then SKIPPED. Because the job is
      `continue-on-error`, the workflow stays green. Net effect: the
      Verus proof job has been verifying nothing while reporting no
      blocking failure — the F2 silent-failure class. A proof job that
      is not proving anything is worse than a red one.

      Confirmed 2026-05-21 on PR #311 run 26246893086: the Verus job
      step sequence was `failure Install Nix` -> `skipped Verify Verus
      specs`.

      Pinning the action + `determinate: false` (shipped in v0.11.1)
      fixed the Rocq job on ubuntu-latest but cannot fix Verus: no
      version of `nix-installer-action` installs Nix without root. The
      durable fix is to run the Verus (and Rocq) Bazel/Nix jobs in a
      rootless `nixos/nix` container — Nix pre-installed in the image,
      no installer, no daemon, no sudo, no unpinned-action drift
      surface.

      Unblocker (2026-05-23): smithy runner9 is now podman-capable —
      `podman_userns: true`, `NoNewPrivileges=0`,
      `ProtectKernelTunables=false` deployed and verified live
      (`podman run nixos/nix@sha256:fd7a5c67... nix --version` returns
      `nix (Nix) 2.24.9`). GHA label set is
      `[self-hosted, Linux, X64, hetzner, rust-cpu, podman]`. The
      Verus job can switch `runs-on:` to include the `podman` label
      and drive the Bazel/Nix work through rootless podman against
      `docker.io/nixos/nix`; per the smithy note, "the flag is one
      line per runner — trivial to expand" if contention on runner9
      becomes a concern. The rivet implementation should follow the
      `spar` validation of the same pattern (sequential, so we inherit
      a tested approach rather than co-debug two repos in parallel).

      Acceptance:
        - The Verus CI job's `Verify Verus specs` step actually
          executes — assert it is not `skipped` in the job step list.
        - Environment breakage (Nix/toolchain unavailable) makes the
          job FAIL visibly, distinct from a soft-gated proof result:
          `continue-on-error` must not be able to mask a job that
          never ran the verifier.
        - A completed Verus job's `verus-test-log` artifact contains a
          real Verus solver result.
        - The Verus job's `runs-on:` targets the `podman` label
          (scoped to a podman-capable runner) and Nix work runs inside
          a `nixos/nix` rootless container — no `nix-installer-action`
          and no host-installed Nix.
    tags: [ci, verus, silent-failure, f2-family, nix]
    fields:
      priority: should
      category: non-functional
      baseline: v0.12.0-track
    links:
      - type: derives-from
        target: FEAT-135

  - id: REQ-087
    type: requirement
    title: "rivet export --single-page and --filter must actually narrow output for large projects, not silently emit the full artifact set"
    status: draft
    description: |
      Empirical observation on a 5120-artifact rivet project (2026-05-23):
      `rivet export --html --single-page` still produces all per-artifact
      pages instead of a single document, and `--filter '<s-expr>'`
      emits the full artifact set rather than the filtered subset. The
      flag help text claims semantics the implementation does not
      deliver — the F2 silent-failure class catalogued by
      REQ-078..082.

      Verified plausible in the code: `--single-page` is wired through
      `cmd_export` -> `cmd_export_html` (rivet-cli/src/main.rs:7976,
      8000-8017, calling `rivet_core::export::render_single_page`); but
      something in the path either ignores the flag for the multi-page
      generator that also runs, or the renderer falls back to per-page
      output for input sizes above a threshold. The user observed both
      flags non-functional on the 5120-artifact project.

      Acceptance:
        - `rivet export --html --single-page --out single.html` on a
          5000+ artifact project produces ONE HTML file at the given
          path, not a directory tree of per-artifact pages.
        - `rivet export --html --filter '<s-expr>' --out tree/` on a
          5000+ artifact project produces only the artifacts that match
          the s-expr (verified by counting files in `tree/artifacts/`).
        - If a requested flag combination cannot be honoured on a given
          input, the exporter errors loudly with a clear message — it
          must not silently emit "default" output that contradicts the
          user's flags.
        - Regression test in `rivet-cli/tests/` driving the real binary
          against a small fixture exercising both flags.
    tags: [export, html, silent-failure, f2-family]
    fields:
      priority: should
      category: functional
      baseline: v0.13.0-track
    links:
      - type: derives-from
        target: FEAT-135
      - type: traces-to
        target: REQ-078

  - id: REQ-088
    type: requirement
    title: "rivet export HTML must not embed the full CSS/JS framework per page — extract to shared assets so large exports are usable"
    status: draft
    description: |
      Each rendered per-artifact HTML page embeds the full CSS + JS
      framework inline: `<style>{fonts_css}{css}</style>` and
      `<script>{mermaid_js}</script>` (rivet-cli/src/main.rs:8142-8143),
      with `MERMAID_JS = include_str!("../assets/mermaid.min.js")` —
      mermaid.min.js alone is ~3MB. 4000+ artifact pages times ~3MB
      = ~13 GB by construction; the user measured exactly that.

      The fix is well-bounded: extract the framework into a single
      `_assets/` directory under the export root and reference it from
      each page via relative `<link rel="stylesheet" href="...">` /
      `<script src="...">`. Per-page payload drops to ~50KB
      (artifact-specific markup only); total export of a 5000-artifact
      project should land at ~50-100 MB uncompressed.

      Acceptance:
        - `rivet export --html` on a 5000-artifact project produces
          exactly one copy of every shared asset (mermaid.min.js, base
          CSS, fonts) under `<out>/_assets/`, and per-artifact pages
          reference them by relative URL.
        - Total uncompressed export size on a 5000-artifact project
          is ≤ 100 MB.
        - A randomly-sampled per-artifact page renders correctly when
          opened directly from disk (the relative paths resolve).
        - Regression test asserts the per-page byte budget (e.g.
          `assert!(per_page_bytes < 100_000)` for a representative
          artifact).
    tags: [export, html, performance, bloat, audit]
    fields:
      priority: should
      category: non-functional
      baseline: v0.13.0-track
    links:
      - type: traces-to
        target: REQ-087

  - id: REQ-089
    type: requirement
    title: "Diagnostics must be identical across the VSIX extension, `rivet serve`, and `rivet validate` — no per-frontend divergence"
    status: draft
    description: |
      Empirical observation against v0.11.1: the VS Code extension
      (vscode-rivet) surfaced different warnings than `rivet serve` or
      `rivet validate` for the same project. There is no integration
      test asserting the three rendering paths agree on the diagnostic
      set. A user fixing what the VSIX shows may still have unsurfaced
      `rivet validate` errors that gate CI — itself an F2-class
      silent failure on the QA surface.

      The structural fix: all three frontends consume the same
      diagnostic-producing function (`validate::validate` and friends);
      no frontend re-implements or filters diagnostics independently.
      An integration test that runs the three paths over a fixture
      project and diffs the diagnostic SET (rule, severity,
      artifact_id, message-prefix) enforces this.

      Acceptance:
        - For a fixture project with a known mix of errors / warnings /
          infos, an integration test extracts diagnostics from:
          (a) `rivet validate --format json`,
          (b) `rivet serve` and scraping the validate page, and
          (c) the VSIX extension's published diagnostics (LSP-style
          test or a headless VS Code extension test).
          The three sets must match by (rule, severity, artifact_id,
          message-prefix).
        - Any per-frontend filtering (e.g. severity threshold display)
          is explicit and documented in the diagnostic-emission API,
          not buried in the frontend.
        - The same shared diagnostic function powers all three.
    tags: [vsix, serve, validate, silent-failure, f2-family, qa, consistency]
    fields:
      priority: should
      category: non-functional
      baseline: v0.13.0-track
    links:
      - type: derives-from
        target: FEAT-135

  - id: REQ-090
    type: requirement
    title: "GitHub Release attaches a compliance bundle (~51 MB: documents + coverage + matrix + validate + ReqIF), not the per-artifact HTML drilldown"
    status: draft
    description: |
      The v0.12.0 GitHub Release attached HTML navigation shells
      (~7 MB, useless without the per-artifact pages) and not the
      rendered specifications + machine-importable ReqIF auditors
      actually need. The audit content map for a 5120-artifact project:

      - 51 MB bundle = `documents/` (44 rendered specs with resolved
        artifact tables — ID/Type/Title/Status per reference) +
        `coverage/` + `matrix/` + `validate/` + `stpa/` +
        `eu-ai-act/` + `graph/` + `index.html` + `ReqIF` (XML, OMG
        standard, importable into DOORS / Polarion / codeBeamer).
        Auditors get full review material; fits a GitHub release
        artifact.
      - Per-artifact HTML drilldown (~13 GB once REQ-088's bloat is
        fixed, ~50 MB) is overkill for compliance review — the doc
        tables already show ID/Type/Title/Status for every
        referenced artifact.

      Acceptance:
        - The release workflow produces a single bundled tarball
          (e.g. `rivet-compliance-bundle-vX.Y.Z.tar.gz`) containing
          documents/, coverage/, matrix/, validate/, stpa/,
          eu-ai-act/, graph/, index.html, plus the project's
          ReqIF + generic-yaml export.
        - The tarball is attached to the GitHub Release.
        - The per-artifact `artifacts/` HTML tree is NOT in the
          tarball (until REQ-088's CSS/JS dedup lands, including it
          balloons the bundle; even after REQ-088 it's still optional
          drilldown, not compliance evidence).
        - A README in the tarball documents what each directory is and
          how to import the ReqIF.
        - The release workflow change is gated on REQ-088 landing
          first — until per-page assets are de-duplicated, the
          per-artifact pages can't be cleanly included anyway.
    tags: [release, packaging, compliance, audit, reqif]
    fields:
      priority: should
      category: non-functional
      baseline: v0.13.0-track
    links:
      - type: traces-to
        target: REQ-088
      - type: traces-to
        target: REQ-005

  - id: REQ-091
    type: requirement
    title: "rowan-yaml salsa parser silently drops flush-left artifacts from the default validate path"
    status: draft
    description: |
      Default `rivet validate` (the salsa-incremental path with the
      `rowan-yaml` feature enabled, the default) drops artifacts
      written in YAML's flush-left list style from its grading. The
      artifact is loaded by other paths (`rivet list`, lifecycle
      coverage, validate `--direct`) and the artifact file parses
      cleanly via `parse_generic_yaml`, but the rowan parser
      (`yaml_hir::extract_schema_driven`, the default salsa load
      path) returns ZERO artifacts and ZERO diagnostics for the same
      file. The link graph the validator should grade is invisible.

      Discovered via clean-room verification of a parallel agent's
      bug report (sphinx-needs importer corpus — Python tool 2507
      warnings, Rust port 488 — the 2019 gap is exactly the lost
      grading). The agent attributed the bug to
      `GenericYamlAdapter::export` / `parse_generic_yaml`; probe
      reproduced their fixture and FALSIFIED that attribution —
      `parse_generic_yaml` returns 1 link for both fixtures,
      identical link_type/target. The real failure is in
      `yaml_hir::extract_schema_driven`:

        Fixture (flush.yaml, 7 lines):
          artifacts:
          - id: COMP_REQ__FLUSH__A
            type: comp-req
            title: Flush-left
            status: approved
            links:
            - type: satisfies
              target: FEAT_REQ__NOPE__DANGLE

        extract_schema_driven(...) -> artifacts: 0, diagnostics: 0   ← bug
        parse_generic_yaml(...)    -> 1 artifact, 1 link             ← fine
        rivet list                 -> 1 artifact, 1 link             ← fine
        rivet validate (default)   -> no link errors for FLUSH       ← bug
        rivet validate --direct    -> link errors for both           ← fine

      A contributing factor: `extract_links_via_serde` (the rowan
      parser's fallback) silently returns `Vec::new()` on
      `serde_yaml::from_str` parse error — F2 silent-failure ethos
      violation. Even the fallback can't surface "I tried to parse
      links and couldn't."

      Impact: any project using generic-yaml whose artifacts use
      flush-left lists (which is exactly what serde_yaml's
      `to_string` emits — so rivet's own
      `GenericYamlAdapter::export` produces files rivet's default
      validate cannot fully grade) silently loses link-graph
      validation in CI.

      Bug-doc + 2-file fixture authored by the parallel agent:
      `docs/design/rivet-link-parse-bug.md` +
      `docs/design/rivet-link-parse-bug-fixture/` (on the
      `feat/import-results-sphinx-needs` branch). Description is
      attribution-corrected here.

      Acceptance:
        - `yaml_hir::extract_schema_driven` returns 1 artifact + 1
          link for the flush.yaml fixture (identical to indent.yaml).
        - Default `rivet validate` (salsa + rowan-yaml) emits the
          broken-link ERROR for COMP_REQ__FLUSH__A — same as
          `rivet validate --direct` does today.
        - `extract_links_via_serde`'s parse-error branch emits a
          parse diagnostic instead of `Vec::new()` (loud-fail).
        - Regression test in `rivet-core/tests/` driving
          `extract_schema_driven` directly with both fixture shapes,
          asserting identical artifact + link counts.
        - The parallel agent's sphinx-needs port corpus, re-graded
          after the fix, matches the Python reference's ~2500
          warnings (the falsification gate the agent's handoff
          described).
    tags: [yaml, rowan, salsa, validate, silent-failure, f2-family]
    fields:
      priority: must
      category: functional
      baseline: v0.13.1-track
    links:
      - type: derives-from
        target: FEAT-135

  - id: REQ-092
    type: requirement
    title: "Per-source-line traceability linker — Eclipse S-CORE source_code_linker equivalent"
    status: draft
    description: |
      Eclipse S-CORE ships `score_source_code_linker` — a tool that
      scans source files for inline comments of the shape
      `// req-Id: <id>` and emits per-line traceability records
      linking that source location to the named requirement (need).
      Rivet has `rivet commits` (REQ-051), which links per-commit via
      trailers (`Implements:`, `Refs:`, `Fixes:`, `Verifies:`).
      Different granularity: per-line vs per-commit; both auditable.

      For projects converging from S-CORE → rivet (the Eclipse S-CORE
      sphinx-needs importer landing in the parallel agent's PR is one
      such case), a per-source-line linker closes the granularity gap
      and lets one tool surface both kinds of evidence under the
      existing traceability rules.

      Proposed shape (subject to design):

      - `rivet source-link` — new subcommand. Scans source paths
        configured in `rivet.yaml` for `// rivet: <req-id>`
        comments (marker configurable; default `// rivet:` to avoid
        colliding with S-CORE's `// req-Id:` until a migration story
        exists). Emits a per-line traceability artifact set: file,
        line range, linked artifact-id, comment text.
      - Output integrates with existing traceability — the per-line
        evidence shows up in `rivet validate`'s coverage metrics and
        the matrix view, indistinguishable from a per-commit link
        once aggregated by target.
      - Optional `--marker '// req-Id:'` switch lets a downstream
        project consume S-CORE-style comments without migration.

      Acceptance:
        - `rivet source-link` on a project with `// rivet: <req-id>`
          comments in tracked source files emits artifacts naming
          file / line / linked artifact-id.
        - `rivet validate` after `source-link` counts those per-line
          links toward the same coverage metrics as commit-trailer
          links (REQ-051 family).
        - `--marker '// req-Id:'` ingests S-CORE-style comments.
        - Malformed markers (`// rivet: NOTASHAPE`, missing ID,
          duplicate per line) emit diagnostics — never silent skip.
        - A worked example added under `examples/source-link/`.
    tags: [traceability, source-link, eclipse-score, parity, new-capability]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-051

  - id: REQ-093
    type: requirement
    title: "Implement the FUTURE oracles — asil-decomposition, coverage-threshold, method-table-compliance — turn the ISO 26262 narrative into mechanical checks"
    status: draft
    description: |
      The schema declares three oracles today (`asil-decomposition`,
      `coverage-threshold`, `method-table-compliance`) but their
      evaluators are FUTURE stubs — `rivet validate` parses the
      pipeline declaration and never fires. 10-persona review row A1
      (Hess, ISO 26262 assessor): "your schema parses pipelines that
      don't fire." Until they fire, the per-ASIL rigour rivet claims
      is narrative, not mechanical.

      Acceptance:
        - `asil-decomposition` oracle: ISO 26262-9 decomposition
          rules implemented and exercised. Fixture: an ASIL-D parent
          decomposed into two children must satisfy the allowed
          pairs (ASIL-B + ASIL-B, ASIL-A + ASIL-C, …). Non-conforming
          pair -> ERROR; conforming pair -> silent.
        - `coverage-threshold` oracle: per-rule coverage % computed
          against a declared minimum; below-threshold -> ERROR
          naming (rule, observed, required).
        - `method-table-compliance` oracle: ISO 26262 Part 6
          Tables 9 / 10 / 11 encoded; missing required method at the
          declared ASIL -> ERROR.
        - Each oracle has at least one passing fixture and one
          failing fixture in `rivet-core/tests/`.
        - `rivet docs schema/...` no longer renders these as FUTURE.
    tags: [oracle, iso-26262, qualification, falsification, persona-review]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-004

  - id: REQ-094
    type: requirement
    title: "Reproducible-build oracle + sigstore-signed release verification — `rivet release-verify`"
    status: draft
    description: |
      A tool whose output IS compliance evidence needs verifiable
      releases. v0.10.x added sigstore-keyless signing of
      `SHA256SUMS` via cosign — the production side is done. The
      missing leg is the consumer side: `rivet release-verify <tag>`
      rebuilds the release artifacts from a fresh checkout, hashes
      the output, verifies the sigstore bundle against the GitHub
      OIDC identity, and refuses if any step disagrees. 10-persona
      row A4 — two of three rivet reviewers (Hess, Whittaker) flagged
      release-channel integrity as the blocking gap.

      Acceptance:
        - `rivet release-verify v0.13.0` rebuilds, hashes, fetches
          the sigstore bundle, runs `cosign verify-blob` with the
          documented identity regex, and exits 0 iff every hash
          matches and the signature verifies against the workflow's
          OIDC identity.
        - Tampered `SHA256SUMS`, missing signature, wrong identity,
          or non-reproducible build each fail loudly with the named
          mismatch.
        - A CI job runs `release-verify` on the most-recent tag
          periodically and gates merge to `main` if it fails.
        - Documented via `rivet docs release-verify`.
    tags: [release, sigstore, supply-chain, qualification, persona-review]
    fields:
      priority: must
      category: non-functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-068

  - id: REQ-095
    type: requirement
    title: "`cargo build` invokes `rivet validate` for crates declaring `rivet.yaml` — born-compliant at the compile step"
    status: draft
    description: |
      Today `rivet validate` fires on commit (pre-commit hook + CI).
      The MBSE-mandatory principle says the model drives the build,
      not sits alongside it. Bringing validation INTO the build
      closes the window between "edited an artifact" and "noticed
      the artifact is broken": today that window is one commit;
      tomorrow it is one `cargo build`. 10-persona row B1; Carrasco
      named it as the biggest MBSE-philosophy win and the realisation
      of the "born-compliant" framing.

      Acceptance:
        - A `build.rs` in `rivet-cli` (or a `cargo` subcommand
          wrapper) detects `rivet.yaml` and runs `rivet validate`.
        - Non-zero validate -> the build fails.
        - Opt-out env var (`RIVET_SKIP_VALIDATE_ON_BUILD=1`) for
          bootstrap / circular cases; default ON.
        - Build-time validation result is cached by content hash so
          incremental builds stay fast.
        - Integration test: a project with a deliberately broken
          artifact YAML fails `cargo build` with the validate
          diagnostic; fixing the artifact unblocks the build.
    tags: [mbse, build, cargo, validate, persona-review]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-004

  - id: REQ-096
    type: requirement
    title: "SACM 2.x typed safety-case schema with deductive-sufficiency mechanisation"
    status: draft
    description: |
      Rivet ships a safety-case schema today but stops at *structural*
      traceability — every workproduct has a chain of links — without
      enforcing the *deductive sufficiency* obligations a safety case
      claims. 10-persona row A2 (Dubois, safety researcher):
      "compliance trace ≠ safety case." Promoting the schema to OMG
      SACM 2.x's claim / argument / evidence shape + a mandatory
      `supported_by` cardinality lets `rivet validate` enforce "every
      hazard mitigated, every AoU discharged, every derived
      requirement traced to its analysis origin."

      Acceptance:
        - `schemas/sacm.yaml` declares `claim`, `argument`,
          `evidence` artifact types with the SACM 2.x link
          vocabulary (`supported_by`, `in_context_of`,
          `decomposed_into`, …).
        - Traceability rule: every ASIL-B+ workproduct is reachable
          from a `root_claim`-typed claim via `supported_by`.
        - Traceability rule: every `hazard` has a `mitigated_by`
          path to at least one `safety_requirement`.
        - Traceability rule: every `assumption_of_use` has a
          matching `discharged_by` link in the consumer's project
          (cross-repo via the REQ-085 mechanism).
        - Worked example under `examples/safety-case/` carrying a
          minimal but non-trivial SACM tree.
    tags: [sacm, safety-case, assurance, schema, persona-review]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: derives-from
        target: REQ-002

  - id: REQ-097
    type: requirement
    title: "DO-330 tool-qualification artifacts (TQP / TOR / TVCP / TCI / TAS) as a typed schema, dogfooded by rivet itself"
    status: draft
    description: |
      10-persona row A6 (Whittaker, DO-178C/DO-330 reviewer): "rivet's
      TCL-2 narrative is honest, but there is no DO-330 *shape* —
      TQP / TOR / TVCP / TCI / TAS are absent." A TCL-2 *tool* has
      those artifacts in typed form; a TCL-2 *narrative* does not.
      Filing them in `schemas/do-178c.yaml` and dogfooding the tool's
      own qualification dossier under that schema turns the TQL claim
      from "asserted" to "a falsifiable graph state."

      Acceptance:
        - `schemas/do-178c.yaml` declares the five artifact types:
          `tool_qualification_plan` (TQP),
          `tool_operational_requirements` (TOR),
          `tool_verification_cases_and_procedures` (TVCP),
          `tool_configuration_index` (TCI),
          `tool_accomplishment_summary` (TAS).
        - Required link cardinalities encoded:
          TQP -> TOR(+), TOR -> TVCP(+), TVCP -> TCI(1),
          TAS -> TQP(1).
        - `artifacts/qualification/` carries rivet's *own* TQP /
          TOR / TVCP / TCI / TAS — dogfood demonstrates that the
          tool's qualification artifacts validate cleanly under the
          tool itself.
        - `rivet docs do-178c` topic added with the layering
          explanation.
    tags: [do-178c, qualification, schema, dogfood, persona-review]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-004

  - id: REQ-098
    type: requirement
    title: "Independence-of-verification-layers oracle — refuse `product-of-miss-rates` claims when layers share too much code"
    status: draft
    description: |
      The single most-repeated complaint of the 10-persona panel
      (row A3, Hess + Whittaker): Kani, Verus, Rocq, proptest, and
      `rivet validate` all share the `Artifact` model and the YAML
      parser. The "product of miss rates" argument that justifies
      claiming "TCL-2 because we have N independent verification
      layers" collapses if the shared parser has a bug — common-mode
      failure. The oracle: rivet declares each verification layer's
      Rust dep graph, walks the intersection, and refuses to claim
      "independent layer" when shared-code count exceeds a
      configurable threshold.

      Acceptance:
        - `rivet validate --check-independence` reads a manifest of
          declared verification layers (Kani / Verus / Rocq /
          proptest / direct validate / salsa validate) and their
          entry-point crates.
        - Computes the transitive-dep intersection of any two
          declared-as-independent layers.
        - Emits ERROR if intersection exceeds the configured
          threshold (default: 0 shared *parsing* code; small-N
          shared *types* allowed and documented).
        - The TCL dossier's independence claim links to the oracle's
          last passing run as evidence.
    tags: [independence, verification, qualification, oracle, persona-review]
    fields:
      priority: should
      category: non-functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-051

  - id: REQ-099
    type: requirement
    title: "`rivet_apply` MCP tool — idempotency keys + atomic transactions + auto-reload + post-state diagnostics"
    status: draft
    description: |
      10-persona row A5 / C6 (Krishnamurthy, AI engineer): "MCP catalog
      is small + typed + orthogonal — good. Mutation→reload is manual,
      no batch, no idempotency = retry pain at scale." An agent fleet
      replaying a failed batch today produces duplicates; an agent
      without a reload tool sees stale state. `rivet_apply` is a
      single mutator tool that takes a JSON-patch + idempotency key,
      applies atomically, runs `rivet validate` on the post-state, and
      returns both the diff and the diagnostics — so agents don't have
      to coordinate three calls per mutation.

      Acceptance:
        - `rivet_apply(patch, idempotency_key)` MCP tool: replay with
          the same key is a verified no-op (returns the same response
          shape as the first call).
        - Atomic: failure rolls the artifact back; the YAML file is
          either fully-changed or untouched.
        - Auto-reload: the MCP server's cached state is invalidated
          on apply.
        - Response includes the post-apply `rivet validate`
          diagnostics — agents see the consequence of the change
          without a follow-up call.
        - `rivet validate` gains an audit-log check that detects
          idempotency-key violations across past applies (REQ-082
          F2 ethos extended to mutation).
    tags: [mcp, agent, idempotency, mutation, persona-review]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: FEAT-010

  - id: REQ-100
    type: requirement
    title: "`externals: kind: source` — track non-rivet upstreams (eclipse-score, sphinx-needs, DOORS exports, plain code repos) without the missing-rivet.yaml warning"
    status: draft
    description: |
      Today every external in `rivet.yaml` is assumed to be a rivet
      project, so `rivet sync` (and downstream loaders) try to read
      the external's `rivet.yaml` and emit a WARN when it is absent.
      A converter scenario like the eclipse-score import pulls 20+
      upstream repos that are deliberately not rivet projects — by
      design, they ship sphinx-needs / DOORS-exports / plain code,
      not `rivet.yaml`. The current behaviour emits 58 WARN lines of
      pure noise in that workflow.

      Generalises beyond eclipse-score: any fork tracking a DOORS
      export, a sphinx-needs project, or a plain source repo that
      feeds a converter or a source-linker wants the same shape.

      Design:
        - Add an optional `kind:` field to each entry under
          `externals:`. Two enum values:
          - `rivet` (default — current behaviour: loads the
            external's `rivet.yaml`, may emit
            assumption-of-use / discharged-by reconciliation, etc.).
          - `source` (NEW — `rivet sync` clones to the same on-disk
            location but loaders skip the `rivet.yaml` read attempt;
            no WARN; no rivet-project bookkeeping).
        - Scope: ~20 LoC in `rivet-core/src` plus the schema entry.
        - `rivet validate` rejects an unknown `kind:` value with a
          named-enum error (no silent fallback to `rivet`).

      Acceptance:
        - Fixture: a `rivet.yaml` declaring `externals: foo:
          { git: ..., kind: source }` syncs and validates cleanly,
          with ZERO `missing rivet.yaml` warnings for `foo`.
        - Fixture: omitting `kind:` defaults to `rivet` and
          preserves today's behaviour exactly — a missing
          `rivet.yaml` still surfaces (regression test).
        - Fixture: an unknown `kind:` value (e.g. `kind: roboms`)
          produces an ERROR diagnostic that names both the bad value
          and the enum domain.
        - Documented under `rivet docs externals`.

      Note: the slot REQ-100 was deliberately left free earlier
      after dropping the v1.0-readiness REQ proposal; this
      orthogonal request happily reuses the number.
    tags: [externals, sync, converter, non-rivet-upstream, ergonomics]
    fields:
      priority: should
      category: functional
      baseline: v0.13.x-track
    links:
      - type: traces-to
        target: REQ-051

  - id: REQ-101
    type: requirement
    title: "Artifact-driven verification gate — `rivet verify` matches approved artifacts to convention-named tests"
    status: draft
    description: |
      Land the artifact-driven verification-gate pattern that meld
      already runs (`tools/post_verification_comment.py`, ~50 LoC)
      as a first-class `rivet verify` subcommand.

      The gate inverts the usual "run tests, check exit code" loop:
      the artifact list is the worklist, the test is the evidence,
      and the absence of a matching test is a discrete diagnostic
      row (MISSING), not silence. Same traceability shape as
      REQ-051 (commit trailer demands link) and the schema's
      required-fields gates — the model demands a test, and CI
      falsifies the obligation mechanically.

      Pattern (per artifact A where A.status = approved and the
      schema's `verification-template` is set on A's type):

          glob  = expand(template, A.id)   # REQ-001 -> test_req_001_*
          tests = discover(glob)           # via configured runner
          if tests == []:        bucket(A) = MISSING
          elif any(t.failed):    bucket(A) = FAILED
          else:                  bucket(A) = PASSED

      Schema extension (composes on existing `ArtifactTypeDef` per
      the reuse-binding-patterns guidance, NOT a new directive):

        - id: requirement
          verification-template: "test_{id_lower_underscored}_*"

      The field is optional; types without it are excluded from the
      gate (e.g. decisions, features that have no executable
      evidence).

      Runner is shell-injectable via `rivet.yaml`
      (default `cargo test --no-fail-fast`) so pytest / nextest /
      bazel / ctest work without rivet learning each.

      Outputs:
        - `--format pr-comment` — markdown grouped by bucket with
          source-of-truth footer (byte-comparable to meld);
        - `--format json` — machine-readable, dashboard-ingestible;
        - `--format yaml --out artifacts/results/` — written into the
          existing `ResultStore` so `rivet results show` surfaces it
          without modification.

      Gate: exit code 1 if any artifact is FAILED or MISSING; 0
      otherwise.

      NOT an oracle in `rivet validate`: validate is read-only over
      YAML. `verify` executes tests and belongs in its own command.

      Acceptance:
        - `rivet verify --help` lists the subcommand.
        - Schema fixture with `verification-template:
          "test_{id_lower_underscored}_*"` on the `requirement`
          type + one approved REQ-X with a matching passing test
          makes `rivet verify` exit 0 and report 1/1 PASSED.
        - Removing the test makes `rivet verify` exit 1 with REQ-X
          in the MISSING bucket — the absence is a row, not silence.
        - Making the test fail makes `rivet verify` exit 1 with
          REQ-X in the FAILED bucket.
        - Types without `verification-template` (e.g. decisions) are
          excluded from the worklist and not counted.
        - `rivet verify --format pr-comment` emits markdown with
          PASSED / FAILED / MISSING sections + a footer naming the
          source-of-truth file(s) and the template — byte-comparable
          to meld's reference output on the meld corpus.
        - `rivet verify --format yaml --out artifacts/results/`
          writes a `ResultStore`-loadable file consumed by
          `rivet results show` without modification.
        - Runner command is overridable via `rivet.yaml`
          (`verify.runner`) — a pytest project sets it once, no
          code change.
        - Worked example under `examples/verify/` + GitHub Action
          snippet under `docs/ci/` wrapping
          `rivet verify --format pr-comment | gh pr comment`.
    tags: [verification, gate, ci, mbse, traceability, meld-parity, new-capability]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: derives-from
        target: REQ-051
      - type: traces-to
        target: REQ-004

  - id: REQ-102
    type: requirement
    title: "`gherkin` export format advertised in `rivet export --help`"
    status: approved
    description: |
      `rivet export` accepts `--format gherkin` (validate_format allows
      it, cmd_export_gherkin handles it) but the clap `--format` help
      listed only reqif / generic-yaml / html / zola. Surfaced by the
      v0.13.1 export-format end-to-end test. Help text + the
      unsupported-format error message now both list gherkin (and zola).

      Acceptance:
        - `rivet export --help` output contains "gherkin".
    tags: [export, gherkin, docs, cli]
    fields:
      priority: should
      category: functional
      baseline: v0.13.2
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-103
    type: requirement
    title: "ReqIF import reachable from the CLI (`import-results --format reqif`)"
    status: approved
    description: |
      `rivet export --format reqif` produced ReqIF, but there was no
      CLI path to bring a ReqIF file (e.g. a DOORS / Polarion export)
      back into rivet artifacts — `parse_reqif` lived in rivet-core but
      was reachable only via the supplier-pull well-formedness cache.
      The DOORS/Polarion round-trip was therefore export-only. Adds
      `import-results --format reqif <file>`: parse to artifacts, write
      generic YAML, report artifact + link counts.

      Acceptance:
        - `rivet import-results --format reqif <file> --output <dir>`
          writes `reqif-import.yaml` containing the parsed artifacts.
        - Round-trip: `export --format reqif` then
          `import-results --format reqif` preserves artifact + link
          counts (verified in rivet-core lib + a rivet-cli integration
          test).
    tags: [reqif, import, interchange, doors, polarion, round-trip]
    fields:
      priority: should
      category: interface
      baseline: v0.13.2
    links:
      - type: traces-to
        target: REQ-005

  - id: REQ-104
    type: requirement
    title: "ReqIF export emits a SPECIFICATION + SPEC-HIERARCHY document tree"
    status: approved
    description: |
      The ReqIF export carried SPEC-OBJECTs + SPEC-RELATIONs +
      SPEC-OBJECT-TYPEs but no SPECIFICATION / SPEC-HIERARCHY, so
      importers (DOORS / Polarion / codeBeamer) rendered a flat object
      pool with no navigable outline (some reject a hierarchy-less file
      outright). Export now emits one SPECIFICATION referencing a
      SPECIFICATION-TYPE, with one SPEC-HIERARCHY entry per SPEC-OBJECT
      in store order. The object/relation payload is unchanged (no
      regression: still 797 objects + 1551 relations on the rivet
      corpus).

      Acceptance:
        - Exported ReqIF contains exactly one SPECIFICATION, one
          SPECIFICATION-TYPE it references, and one SPEC-HIERARCHY per
          SPEC-OBJECT.
        - The SPECIFICATION addition does not change SPEC-OBJECT or
          SPEC-RELATION counts (round-trip preserved).
    tags: [reqif, export, interchange, doors, polarion, completeness]
    fields:
      priority: should
      category: interface
      baseline: v0.13.2
    links:
      - type: traces-to
        target: REQ-005

  - id: REQ-105
    type: requirement
    title: "HTML export must be a self-contained static site (bundle the serve-only assets)"
    status: implemented
    description: |
      `cmd_export_html` reuses the `rivet serve` dashboard rendering
      (`serve::reload_state`, `serve::components`) verbatim. It does NOT
      spin up a server (reload_state is a pure offline loader), but the
      rendered HTML references serve-ONLY routes and JS that the static
      export does not bundle. Three confirmed gaps, all the same root
      cause — "export reuses serve's HTML but ships none of serve's
      runtime assets":

      1. **Absolute server-route links.** Pages link
         `href="/artifacts/CD-001"`, `href="/coverage"`, … with no
         `.html` extension. As static files these break: from
         `file:///…/html/index.html`, `/artifacts/CD-001` resolves to
         the filesystem root; the real file is `artifacts/CD-001.html`.
         Hosting under a sub-path breaks identically. (Content is all
         present; only the inter-page links are wrong.)

      2. **SVG-viewer toolbar JS not bundled.** The mermaid / graph
         toolbar buttons (`svgZoomFit`, `svgFullscreen`, `svgPopout`)
         render in the exported HTML (the `svg-viewer-toolbar` markup
         is there) but are DEAD — their handlers live in serve's
         `js.rs`, and the export's `_assets/` ships only
         `mermaid.min.js` + `styles.css`. So a static export shows
         non-functional zoom/fullscreen/popout buttons. (In `rivet
         serve` they work and are Playwright-tested.)

      3. **`/docs-asset/` images not copied.** Documentation can
         reference PNG/SVG/jpg/gif/webp/pdf images; `render::source.rs
         ::rewrite_image_paths` rewrites a relative `src` to
         `/docs-asset/<path>`, which the serve `/docs-asset/{*path}`
         route serves from the doc directories. The static export
         neither rewrites those to a relative file path nor copies the
         referenced image files, so `<img src="/docs-asset/…">` 404s in
         an exported site. (Serve path verified by unit test
         `render::source::tests::relative_image_src_rewritten_to_docs_asset`.)

      Fix direction (regression-risk — own focused cycle, not a patch):
        - Rewrite internal links to relative + `.html` on export (the
          export already computes a depth-adjusted `"../".repeat(depth)`
          prefix for `_assets/`; extend it to artifact/report links and
          to image `src`), OR add a `--base-path` option.
        - Bundle the svg-viewer toolbar JS into `_assets/` (extract the
          handlers from `js.rs` into a shared file both serve and export
          emit).
        - Copy `/docs-asset/`-referenced images into the export tree and
          rewrite their `src` to the relative copied path.
        - Longer-term: decouple export rendering from the `serve` module
          so `rivet export --format html` doesn't transitively depend on
          axum / the dashboard handlers.

      Acceptance:
        - Opening the exported `index.html` via `file://` and clicking
          any artifact / coverage / matrix / graph link lands on the
          correct local `.html` page (no absolute `/…` routes remain).
        - Hosting the export under a sub-path keeps all links working.
        - The mermaid / graph zoom-fit / fullscreen / popout buttons
          function in the exported static HTML (handler JS bundled).
        - A doc that references a relative PNG/SVG renders that image in
          the exported site (image copied + `src` relative).
        - `rivet export --format html` builds without linking the axum
          server stack (decoupling milestone).
    tags: [export, html, serve-coupling, relative-links, assets, mermaid, docs-images, regression-risk]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-106
    type: requirement
    title: "serve dashboard: variant scope resolves embedded bindings (self-reference --binding)"
    status: draft
    description: |
      User-reported (Ford variant corpus). The CLI resolves variant
      bindings correctly:

          rivet variant solve --model feature-model.yaml \
            --variant variants/ford-demo-full.yaml \
            --binding variants/ford-demo-full.yaml

      …reports the right bound-artifact counts (16 / 13 / 17). But the
      `rivet serve` dashboard calls the solver WITHOUT passing
      `--binding`, so it reports `bound_artifact_count: 0` for every
      variant. The dashboard should self-reference each variant file as
      its own binding model when the `bindings:` section is embedded in
      the variant config.

      Acceptance:
        - The serve dashboard variant view shows the same non-zero
          bound-artifact counts as the CLI `variant solve --binding`
          for variants whose binding is embedded in the variant file.
        - A serve_integration test asserts a known variant's
          bound-artifact count matches the CLI.
    tags: [serve, variant, binding, dashboard, user-reported, bug]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-107
    type: requirement
    title: "Render sequence/mapping field values, not Rust Debug output"
    status: draft
    description: |
      User-reported. A field whose value is a sequence of mappings
      renders as the Rust `Debug` form:

          Sequence [Mapping {"kind": String("e"), "page_id":
          String("e"), "version": Number(2), "section": String("eee")}]

      …instead of a readable list/table. Affects serve and likely the
      HTML export (shared rendering). Field values that are sequences
      or mappings must render as structured HTML (nested list / small
      table), never `format!("{:?}", value)`.

      Acceptance:
        - An artifact with a sequence-of-mappings field renders as a
          readable nested structure in both serve and HTML export.
        - No `Sequence [`, `Mapping {`, `String(`, `Number(` debug
          tokens appear in rendered output.
    tags: [serve, export, rendering, field-values, user-reported, bug]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-108
    type: requirement
    title: "serve: external-artifact navigation as prefix:ID + visible shortcut-link contrast"
    status: draft
    description: |
      User-reported, three coupled serve-dashboard issues for
      cross-repo (`prefix:ID`) artifacts:

      1. Clicking an externally-defined artifact navigates to
         `externals/<prefix>` rather than the
         `artifacts/<prefix>:<artifact>` detail view.
      2. On an `artifacts/<prefix>:<artifact>` page, the external's own
         references are shown but NOT as followable
         `<prefix>:<reference>` links — so the cross-repo chain can't be
         traversed (and would recurse for externals-of-externals).
      3. That same page does not show the linkage back into the
         consumer's internal artifacts.
      4. The shortcut-link color against the white background is barely
         visible (contrast/accessibility).

      Acceptance:
        - Clicking an external artifact lands on its
          `artifacts/<prefix>:<id>` detail view.
        - External references render as followable `prefix:ref` links,
          recursively.
        - The detail view shows inbound links from internal artifacts.
        - Shortcut links meet a visible contrast ratio on the light
          background.
    tags: [serve, externals, cross-repo, navigation, accessibility, user-reported, bug]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-085

  - id: REQ-109
    type: requirement
    title: "Variant scoping for documents (in/out-of-scope per variant)"
    status: draft
    description: |
      User question: a variant can scope which artifacts are in/out of
      scope (via `out-of-scope-for:` / binding), but there is no
      equivalent control for *documents* — a variant cannot currently
      declare which documents are in or out of scope. Assess whether
      document-level variant scoping is wanted, and if so design it on
      the existing variant-scope mechanism (not a new directive), so a
      variant's exported/served document set narrows with the variant.

      DESIGN DECISION (recorded 2026-05-30):
      YES — documents should get optional per-variant scoping, designed
      on the EXISTING binding mechanism rather than a new directive
      (per the reuse-binding-patterns principle). Mechanism:

        - A feature-binding entry's per-feature payload, today
          `{feature: {artifacts: [...]}}`, gains an optional sibling
          `documents: [DOC-ID, ...]`. A document bound to a feature is
          in-scope exactly when that feature is effective in the active
          variant — identical to how `collect_bound_ids` resolves
          artifacts (rivet-cli/src/serve/variant.rs). No new file kind,
          no new top-level directive.

        - DEFAULT POLICY: opt-in narrowing, NOT opt-out. A document
          NOT bound to any feature stays in scope for EVERY variant.
          Rationale: most documents (README, getting-started, glossary)
          are variant-agnostic; mirroring artifacts (which require an
          explicit binding to be scoped) keeps the model consistent and
          avoids silently hiding global docs.

        - `rivet validate` always sees the full document set (scoping is
          a serve/export view concern, never a validation gate) — same
          contract as artifact variant scope.

      Implementation is a follow-up (priority `could`); this REQ records
      the decision + mechanism so the build has no open product question.

      Acceptance:
        - [DONE] Decision recorded: documents DO get per-variant scoping,
          opt-in via the binding's `documents:` list, default-in for
          unbound docs.
        - [follow-up] A variant binding with a `documents:` list narrows
          the served/exported document set under that variant; unbound
          docs remain; `rivet validate` still sees the full set.
    tags: [variant, documents, scoping, design-question, user-reported, decision-recorded]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-083

  - id: REQ-110
    type: requirement
    title: "Coverage HTML display counts per-rule totals, not distinct artifacts, mislabeled as 'artifacts covered'"
    status: implemented
    description: |
      Bug-hunt finding (cross-command-consistency, 3/3 lens-
      confirmed).

      The line '{total_covered} / {total_items} artifacts covered
      across {} rules' uses per-rule sums (line 238: `e.covered`
      summed, line 238: `e.total` summed). When the same artifact
      appears in multiple traceability rules, the HTML overview counts
      it multiple times. This differs fundamentally from the
      'Artifacts' stat card (line 115) which shows store.len() — each
      artifact counted once.

      Evidence: Lines 237-238 sum coverage entry counts: `let
      total_covered: usize = cov_report.entries.iter().map(|e|
      e.covered).sum(); let total_items: usize =
      cov_report.entries.iter().map(|e| e.total).sum();`. Each
      CoverageEntry.total is computed per-rule from store.by_type(),
      so an artifact satisfying multiple rules increments total_items
      multiple times. But the label on line 252 implies these are
      artifact counts: '{total_covered} / {total_items} artifacts
      covered'. By contrast, rivet stats (lines 5919-5923) sums per-
      type counts ensuring each artifact is counted once.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      cli/src/render/stats.rs:237-252

      Acceptance:
        - In a project where an artifact type appears in 2+
      traceability rules, run `rivet serve` and view the overview
      card. The 'N/M artifacts covered' denominator (M) will not match
      the total artifacts count when rules overlap on source type.
      Compare against `rivet coverage` text output (line 6172) which
      shows only rule-level totals, not distinct artifacts.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, cross-command-consistency, oracle-verified]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-111
    type: requirement
    title: "Coverage JSON 'total' field aggregates per-rule denominators, not distinct artifacts, creating semantic ambiguity with stats 'total'"
    status: implemented
    description: |
      Bug-hunt finding (cross-command-consistency, 3/3 lens-
      confirmed).

      The 'overall.total' in coverage JSON (line 6094: `let total:
      usize = report.entries.iter().map(|e| e.total).sum();`) sums
      per-rule source-type totals, which can count the same artifact
      multiple times if it appears in multiple rules. By contrast,
      stats JSON (line 5867: `"total": stats.total`) uses the sum of
      per-type store counts (line 5923), ensuring each artifact is
      counted exactly once. The same JSON key 'total' has two
      different cardinality semantics.

      Evidence: Lines 6094-6106: coverage JSON includes `"total":
      total` where total is the sum of rule-level totals (line 6094).
      Lines 5867-5875: stats JSON includes `"total": stats.total`
      where stats.total is sum(store.count_by_type()) (line 5923). A
      10-artifact project where all artifacts satisfy both of 2 rules
      would report coverage total=20, stats total=10.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      cli/src/main.rs:6094

      Acceptance:
        - Run `rivet coverage --format json` and `rivet stats --format
      json` on a project where artifact types overlap in multiple
      traceability rules. Compare the 'total' field —
      coverage.overall.total will exceed stats.total.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, cross-command-consistency, oracle-verified]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-112
    type: requirement
    title: "cmd_commits excludes externals while cmd_validate includes them, creating inconsistent artifact counts"
    status: draft
    description: |
      Bug-hunt finding (git-remote-semantics, 3/3 lens-confirmed).

      cmd_commits loads ONLY local artifacts from config.sources and
      does not load externals, while cmd_validate (via ProjectContext)
      loads both local and external artifacts when configured. This
      means artifact counts, link validation, and unimplemented/orphan
      classification differ between `rivet commits` and `rivet
      validate` for projects with externals.

      Evidence: cmd_commits (line 10329-10335): loops through
      config.sources ONLY and builds store with local artifacts. Does
      not call load_all_externals() like cmd_validate does (line 4965,
      5029, 5498). The known_ids set (line 10337) thus excludes all
      cross-repo prefix:ID artifacts, so any commit referencing those
      IDs will be classified as 'broken' by commits but valid by
      validate.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      cli/src/main.rs:10328-10335

      Acceptance:
        - In a project with externals declared in rivet.yaml: (1) Run
      `rivet commits` on a range where commits reference `prefix:ID`
      artifacts from externals. (2) Commits will report these as
      'broken refs' because the externals were never loaded. (3) Run
      `rivet validate` on the same source—it loads externals and
      validates the same cross-repo references successfully. The two
      commands count differently.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, git-remote-semantics, oracle-verified]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-113
    type: requirement
    title: "resolve_external_dir returns unsynced cache_dir path for git externals without checking if sync has been performed"
    status: draft
    description: |
      Bug-hunt finding (git-remote-semantics, 3/3 lens-confirmed).

      resolve_external_dir returns cache_dir/<prefix> for git
      externals (line 315) without verifying the directory has been
      synced (i.e., .git exists). If sync has not been run, code that
      calls resolve_external_dir will silently use a non-existent
      path. Additionally, when --local is used during sync (setting
      use_path=true, line 104), the symlink/copy at cache_dir is
      created, but resolve_external_dir has no way to know if --local
      was used, so downstream code cannot distinguish between a git
      clone (current HEAD) vs a local symlink (working-tree state).

      Evidence: Line 315 returns cache_dir.join(&ext.prefix)
      unconditionally for git externals. No check for .git or error if
      the path doesn't exist. Compare to verify_baseline() (line 778)
      which explicitly checks ext_dir.exists() before proceeding.
      Additionally, sync_external with use_path=true (line 106)
      creates a symlink or copy, but the SEMANTICS are never
      recorded—downstream code using resolve_external_dir cannot tell
      if it's looking at a cached clone or an uncommitted working
      tree.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      core/src/externals.rs:302-317

      Acceptance:
        - (1) Declare a git external without running `rivet sync`. (2)
      Call any code that uses resolve_external_dir, e.g.,
      detect_version_conflicts (line 660). It will return a cache_dir
      path that doesn't exist. (3) Try to read an artifact from that
      path—fails with ENOENT or similar. Additionally: (1) Run `rivet
      sync --local` to use a local path external. (2) Later, code
      calls resolve_external_dir—it returns the same symlink target
      regardless of whether --local was used. (3) The resolved path
      points to a working tree (uncommitted changes visible) but this
      semantic is not exposed to callers, so they don't know if the
      artifacts they loaded include unstaged changes.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, git-remote-semantics, oracle-verified]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-114
    type: requirement
    title: "sync_external uses --local heuristic (use_path = ext.path.is_some() && local_only) but remote-sourced code ignores local_only context, reporting state inconsistently"
    status: draft
    description: |
      Bug-hunt finding (git-remote-semantics, 2/3 lens-confirmed).

      sync_external silently prefers ext.path over ext.git when
      local_only=true (line 104), skipping the git fetch/clone
      entirely. But the lockfile (rivet.lock) records the git URL and
      commit SHA for all externals, regardless of whether they were
      synced locally. This means rivet.lock may claim an external is
      at commit ABC, but it was actually synced from a local --local
      path that is ahead/behind ABC. The dashboard/audit sees what's
      in rivet.lock (git SHAs) and reports that as the truth, not
      realizing the resolved directory may have diverged.

      Evidence: Line 104: use_path = ext.path.is_some() && (local_only
      || ext.git.is_none()). When true, git fetch/clone is skipped
      entirely (lines 168-291 are not executed). But generate_lockfile
      (line 579) always calls git_head_sha on the resolved directory,
      which for a --local-synced path points to a symlink/copy—if the
      underlying working tree has unpushed commits, the lockfile
      records a local-only SHA, not the remote upstream. Then any tool
      reading rivet.lock thinks that SHA is canonical, when really
      it's just the working tree state.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      core/src/externals.rs:104 + rivet-cli/src/main.rs:10934

      Acceptance:
        - (1) Have a git external with
      ext.git='https://github.com/org/repo' and ext.path='../local-
      checkout'. (2) Make a commit in ../local-checkout that's not
      pushed. (3) Run `rivet sync --local`. (4) Run `rivet lock` (or
      sync records it)—it captures the local-only commit SHA into
      rivet.lock. (5) Clone the repo elsewhere; run `rivet sync`
      without --local—it fetches from the remote, which doesn't have
      that local commit. Now the two clones have different SHAs in
      their lockfiles for the same external, both created by the same
      rivet workflow.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, git-remote-semantics, oracle-verified]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-115
    type: requirement
    title: "Zola export emits absolute artifact links that break in subdirectories"
    status: implemented
    description: |
      Bug-hunt finding (path-url-leakage, 3/3 lens-confirmed).

      Artifact links in Zola markdown export begin with `/` which
      assumes root-level deployment. In a subdirectory (e.g.,
      /docs/rivet), these links resolve to wrong paths.

      Evidence: Line 7543 formats artifact links as: `format!("-
      **{}** → [{}](/{prefix}/artifacts/{target_slug}/)\n",
      l.link_type, l.target)`. The leading `/` makes the link absolute
      to site root, not relative to current page.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      cli/src/main.rs:7543

      Acceptance:
        - Run `rivet export --format zola --output /tmp/site --prefix
      myproject`. Then deploy the site to a subdirectory (e.g.,
      server.com/docs/myproject/) and click an artifact link. It
      resolves to server.com/myproject/artifacts instead of
      server.com/docs/myproject/artifacts.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, path-url-leakage, oracle-verified]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-116
    type: requirement
    title: "Zola export shortcode emits absolute href in artifact card links"
    status: implemented
    description: |
      Bug-hunt finding (path-url-leakage, 3/3 lens-confirmed).

      The rivet_artifact shortcode template emits `href="/{{ prefix
      }}/artifacts/..."` which is an absolute link to site root,
      breaking if Zola site is deployed in a subdirectory.

      Evidence: Line 7713 in the shortcode writes: `<strong><a
      href="/{{ prefix }}/artifacts/{{ art.id | lower |
      replace(from=".", to="-") }}/">`. The leading `/` makes this
      absolute.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      cli/src/main.rs:7713

      Acceptance:
        - Export with shortcodes enabled, deploy to
      server.com/docs/rivet/, and use the shortcode. The artifact card
      link will resolve to server.com/artifacts instead of
      server.com/docs/rivet/artifacts.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, path-url-leakage, oracle-verified]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-117
    type: requirement
    title: "HTML export embeds hardcoded localhost in oEmbed discovery tag"
    status: implemented
    description: |
      Bug-hunt finding (path-url-leakage, 3/3 lens-confirmed).

      The oEmbed discovery link references http://localhost:{port}
      unconditionally, even when rendering HTML export which is static
      and has no server running. The tag becomes broken metadata in
      exported HTML.

      Evidence: Lines 540-546 format: `http://localhost:{port}/oembed?
      url=http://localhost:{port}/artifacts/...` using
      ctx.context.port. This tag appears in every artifact page's
      <head> regardless of whether it's served or exported.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      cli/src/render/artifacts.rs:540-546

      Acceptance:
        - Run `rivet export --format html --output /tmp/dist`. Open
      any artifact HTML file, inspect <head>, and find the oEmbed link
      pointing to http://localhost:8080. This breaks any tool trying
      to use oEmbed for embeds, and is invalid in static exports.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, path-url-leakage, oracle-verified]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-118
    type: requirement
    title: "Document wiki-link and artifact-ref resolution emits absolute paths in exported markdown"
    status: implemented
    description: |
      Bug-hunt finding (path-url-leakage, 2/3 lens-confirmed).

      Wiki-link replacement in Zola export (lines 7774-7785 in
      main.rs) calls into document processing which converts [[ID]] to
      Markdown links with absolute paths. Zola export document wiki-
      links also use absolute `/` prefixes in line 7780.

      Evidence: Line 1131-1135 in document.rs format artifact refs as:
      `<a ... href="/artifacts/{id}" ...>` and `<a ...
      href="/documents/{id}" ...>`. These render in exported documents
      and have absolute paths. Line 7780 in main.rs also shows:
      `format!("[{id}](/{prefix}/artifacts/{target_slug}/)")` for Zola
      doc links.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      core/src/document.rs:1131,1135

      Acceptance:
        - Create a document with [[REQ-001]] wiki-link. Export with
      `rivet export --format zola`. Check the generated markdown in
      content/docs/. The link will be rendered as
      `[REQ-001](/{prefix}/artifacts/req-001/)` (absolute). Or export
      to HTML and inspect embedded artifact cards in docs — they will
      have hx-get="/artifacts/{id}" which are absolute HTMX routes.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, path-url-leakage, oracle-verified]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-119
    type: requirement
    title: "ReqIF enumeration value resolution silently drops unresolved references"
    status: implemented
    description: |
      Bug-hunt finding (f2-silent-failure, 3/3 lens-confirmed).

      When importing ReqIF ATTRIBUTE-VALUE-ENUMERATION elements, the
      code silently filters out enum-value references that don't exist
      in the enum_value_names lookup, producing an incomplete/degraded
      field value without warning.

      Evidence: Line 915 uses `.filter_map(|r|
      enum_value_names.get(r.as_str()).copied())` which drops any
      references not found; if an ENUM-VALUE-REF points to an
      undefined identifier, it vanishes silently. Line 920 then joins
      the resolved set, but a user has no diagnostic showing that some
      original values were omitted. Example: a STATUS field with refs
      [val-1, val-2, val-3] where val-2 is undefined produces
      status="val-1, val-3" with no error or warning.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      core/src/reqif.rs:909-918

      Acceptance:
        - Create a ReqIF file with SPEC-OBJECT/VALUES/ATTRIBUTE-VALUE-
      ENUMERATION pointing to an ENUM-VALUE-REF that doesn't match any
      DATATYPE-DEFINITION-ENUMERATION/SPECIFIED-VALUES/ENUM-
      VALUE/@IDENTIFIER. Import it; the field will be silently
      incomplete.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, f2-silent-failure, oracle-verified]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-120
    type: requirement
    title: "ReqIF directory import swallows parse errors to log::warn; no count/report of failures"
    status: implemented
    description: |
      Bug-hunt finding (f2-silent-failure, 3/3 lens-confirmed).

      import_reqif_directory silently skips malformed ReqIF files (XML
      parse errors, invalid structure) to stderr via log::warn. No
      count of skipped files is returned, and no mechanism signals to
      the caller how many files failed to parse.

      Evidence: Line 703-705: `match parse_reqif(&content, type_map) {
      Ok(arts) => artifacts.extend(arts), Err(e) =>
      log::warn!("skipping {}: {e}", path.display()), }` — errors are
      swallowed and logged; the caller receives only a successful Vec
      with whatever was parsed. If 10 out of 20 files fail, the report
      shows a total equal to only the 10 that succeeded.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      core/src/reqif.rs:700-706

      Acceptance:
        - Place a corrupted ReqIF file alongside valid ones in a
      directory, run import_reqif_directory. Check that total artifact
      count doesn't include the corrupted file, and only a stderr log
      message is produced—no diagnostic in the return value.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, f2-silent-failure, oracle-verified]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-121
    type: requirement
    title: "Git checkout failure in external sync is silently ignored on fallback attempt"
    status: draft
    description: |
      Bug-hunt finding (f2-silent-failure, 3/3 lens-confirmed).

      When syncing an existing git external, if checkout of the
      requested git_ref fails, the code attempts a fallback checkout
      as origin/{git_ref} but silently swallows the error with .ok()
      without verifying the fallback succeeded.

      Evidence: Lines 204-209: Initial checkout is error-checked (`if
      !output.status.success() { ... }`); but lines 212-217 show the
      fallback checkout wrapped in `.output().ok()` — any failure is
      discarded. If both checkouts fail, the external symlink points
      to a git repo in an unknown/inconsistent state, but no error is
      returned to the caller.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      core/src/externals.rs:210-217

      Acceptance:
        - Create an external with git_ref pointing to a ref that
      doesn't exist locally or on origin (e.g. 'nonexistent-branch').
      First checkout fails, fallback to 'origin/nonexistent-branch'
      also fails. The function returns Ok(dest) pointing to a repo in
      an inconsistent state.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, f2-silent-failure, oracle-verified]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-122
    type: requirement
    title: "External symlink/directory removal failures are silently ignored"
    status: draft
    description: |
      Bug-hunt finding (f2-silent-failure, 2/3 lens-confirmed).

      When removing an existing destination symlink or directory
      before creating a new one, removal errors are silently swallowed
      with .ok(). If removal fails (permissions, in-use file), the
      code proceeds to create the new external, potentially creating a
      mixed/corrupted state.

      Evidence: Lines 148 and 150 use `.ok()` on remove_file and
      remove_dir_all respectively. If either fails (e.g., permission
      denied, file in use), the subsequent symlink() or
      copy_dir_recursive() call may fail or create a
      partial/inconsistent state without the original cleanup issue
      being surfaced to the caller.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      core/src/externals.rs:147-151

      Acceptance:
        - Create an external with path pointing to a directory. Create
      a subdirectory in that external's destination with a file held
      open by another process. Re-run sync_external. The
      remove_dir_all will fail and be swallowed; the subsequent
      symlink/copy may fail cryptically or silently produce a
      corrupted state.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, f2-silent-failure, oracle-verified]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-123
    type: requirement
    title: "ReqIF title/description default fallbacks mask missing required fields"
    status: implemented
    description: |
      Bug-hunt finding (f2-silent-failure, 3/3 lens-confirmed).

      When constructing a Rivet artifact from a ReqIF SPEC-OBJECT,
      title and description are populated with fallbacks
      (unwrap_or_default, or_else chains) that mask the absence of
      required fields, reporting an artifact with empty title rather
      than raising an error.

      Evidence: Line 943: `let title = reqif_name.or_else(||
      obj.long_name.clone()).unwrap_or_default()` — if both reqif_name
      and obj.long_name are None, title becomes an empty string. A
      Rivet artifact with an empty title is semantically invalid
      (required field), but no validation error is raised during
      import.

      Location: /Users/r/git/pulseengine/rivet/rivet-
      core/src/reqif.rs:939-945

      Acceptance:
        - Create a ReqIF SPEC-OBJECT without @LONG-NAME and no
      ReqIF.Name attribute value. The imported artifact will have an
      empty title field. This violates the Rivet schema (title is
      required) but is not detected during ReqIF import.
        - the inconsistency/incorrectness above is resolved and a
      regression test pins the corrected behaviour.
    tags: [bug-hunt, report-consistency, f2-silent-failure, oracle-verified]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-124
    type: requirement
    title: "Agent-actionable diagnostic remediation (fix-options + docs link)"
    status: implemented
    description: |
      User-reported. A validation error such as

        ERROR: [TE-008] link 'traces-to' targets 'FEAT-019' (type 'feature'),
               allowed target types: ["requirement", "design-decision"]

      states *what* is wrong but not *what to do* about it, nor which of the
      competing fixes is correct. The witnessed failure: an AI agent "fixed
      forward" by inventing type-invalid `traces-to` links on sibling
      artifacts — turning 26 warnings into 8 errors — because the message
      gave no remedy and no trade-off (fix the artifact vs. widen the schema).

      Goal: least-friction remediation for an AI agent. Each remediable
      diagnostic carries structured, ordered fix options (artifact-fix first,
      schema-fix second), each tagged so a tool can tell "edit the artifact"
      from "edit the schema", plus a pointer to a per-rule explanation doc.

      Design (REQ-106 register: build on existing patterns, no new directive):
      remediation is a post-construction pass keyed by `Diagnostic.rule`
      (`rivet_core::remediation::remediation_for(diag, schema, store)`),
      re-deriving specifics from the live schema + store the renderer already
      holds — NOT a field threaded through the ~70 Diagnostic construction
      sites, keeping the hot validation path untouched. Rendered as a
      rustc-style `help:` block in text and a `remediation` object in
      `--format json`; the doc is `rivet docs diagnostics/<rule>`. Rules
      covered (8): `link-target-type` (the witnessed case), `broken-link`,
      `unknown-link-type`, `required-field`, `allowed-values`, `cardinality`,
      `unknown-field`, `known-type`. Each re-derives its specifics (offending
      link/field/value, the schema menu of allowed alternatives) faithfully
      from schema+store, matching the validator's own logic. Traceability
      forward/backlink diagnostics key on the dynamic `rule.id` (not a fixed
      string) and are a follow-up increment.

      Acceptance:
        - `rivet validate` text output on a type-invalid link shows a
          `help:` block with an artifact-fix and a schema-fix, ending with
          `see:  rivet docs diagnostics/link-target-type`.
        - `rivet validate --format json` carries a `remediation` object with
          `summary`, ordered `fix_options[].kind` (`fix-artifact` |
          `adjust-schema`), `targets`, and `doc_topic` on the offending
          diagnostic.
        - `rivet docs diagnostics/<rule>` resolves for all 8 covered rules.
        - For menu-bearing rules (unknown-link-type, allowed-values,
          unknown-field, known-type) the artifact-fix lists exactly the
          schema's declared alternatives (matching the validator's own
          known-set), and the schema-fix names the offending value/type.
        - Diagnostics for rules without guidance render unchanged (bare).
    tags: [validate, diagnostics, remediation, agent-ux, user-reported, dx]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-125
    type: requirement
    title: "validate/coverage --explain: the oracle teaches its own reasoning on every fire"
    status: implemented
    description: |
      Sourced from a source-level study of garrytan/gbrain
      (`src/core/search/explain-formatter.ts` — every ranking decision is
      stamped onto the result and `--explain` replays the derivation),
      reshaped to rivet's deterministic register. The transferable asset is
      the discipline, not gbrain's fuzzy core: make the mechanical verdict
      legible.

      rivet has the analogous OPACITY problem with none of gbrain's
      fuzziness: when `rivet validate` emits a diagnostic, or
      `{{coverage}}`/`{{matrix}}` reports a number, or a traceability rule
      marks an artifact uncovered, the *why* is implicit. The gate fires red
      and a human (or agent) must INFER the reason. `--explain` closes that
      loop: the oracle stops being a wall you bump into and becomes a wall
      that says why it is there — CI-as-curriculum made explicit, and the
      key to handing the gate to a junior or an agent.

      Crucially this is a pure FORMATTER over data `validate::Diagnostic`
      and `coverage` already compute: which traceability rule fired, which
      `required-link`/`required-backlink` it checked, which `LinkGraph`
      edges were consulted, which condition failed. No new computed value,
      no scoring, no heuristic, no new failure surface — it surfaces
      computation already trusted.

      Acceptance:
        - `rivet validate --explain <ID>` prints, per diagnostic on <ID>,
          the rule id + the specific schema constraint + the edges/fields
          that produced the verdict; deterministic (same input -> byte-
          identical output, diff-checkable against a fixture).
        - `rivet coverage --explain <RULE>` shows why each artifact got its
          covered/uncovered classification.
        - `--explain` introduces NO new number; an automated check asserts
          the explained values equal the un-explained `Diagnostic`/coverage
          output.
    tags: [validate, coverage, explainability, audit, ci-as-curriculum, gbrain-derived]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-126
    type: requirement
    title: "Baseline-snapshot drift gate — with rebaselining as a gated, trailer-carrying act"
    status: draft
    description: |
      Sourced from garrytan/gbrain's two-gate eval harness
      (`src/commands/eval-gate.ts`: regression-baseline + correctness gate,
      both fail-CLOSED on any throw), reshaped to rivet's deterministic,
      non-probabilistic register. It is the general form of the
      staleness/drift problem: artifacts silently ceasing to match reality
      between releases. rivet runs `rivet validate`/`rivet commits` in CI but
      has no committed *baseline snapshot* check, so a refactor that changes
      coverage %, matrix coverage, or diagnostic counts is not caught
      mechanically. The primitive exists (`snapshot.rs`, `delta=NAME` in
      `embed.rs`); this formalizes it on existing patterns.

      THE TRAP (must be designed in, not bolted on): the baseline file is
      itself an artifact that can rot. If `.rivet/baseline.json` is
      regenerated carelessly ("tests were red, I rebaselined to green") the
      gate launders drift and reintroduces the green-because-unobserved
      failure mode. Therefore rebaselining MUST be a gated, reviewed,
      rationale-carrying act — never a convenience. The word doing the work
      is UNEXPLAINED: drift accompanied by a commit trailer/rationale is
      acceptable; drift without one fails closed. Updating the baseline must
      be harder than fixing the drift, or the gate trains people to launder
      drift into the baseline.

      Acceptance:
        - `rivet snapshot baseline` emits a deterministic JSON snapshot
          (artifact count by type, coverage %, per-rule coverage, diagnostic
          counts by severity, matrix coverage); byte-identical for identical
          input.
        - `rivet check --baseline <file>` exits non-zero on any delta,
          printing the changed metric(s); exits 0 when unchanged.
        - Fail-closed: any error computing the snapshot makes the check FAIL,
          never silently pass.
        - Rebaselining is gated: a baseline update is only accepted with an
          explicit rationale (commit trailer or `--reason`), and CI can
          distinguish an explained rebaseline from an unexplained one.
    tags: [validate, snapshot, regression-gate, ci, fail-closed, anti-laundering, gbrain-derived]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-101

  - id: REQ-127
    type: requirement
    title: "MCP fail-closed invariant: lock that no tool can override schema/externals resolution"
    status: draft
    description: |
      Sourced from garrytan/gbrain's `src/core/schema-pack/op-trust-gate.ts`
      (a remote caller is refused a per-call schema override unless the
      context is explicitly trusted — fail-closed). gbrain needed this
      because multi-user/remote callers forced it; rivet has not been forced
      yet because it has been single-operator.

      AUDIT (performed 2026-05-30 against rivet-cli/src/mcp.rs): the hole
      does NOT exist today. Schema/store/graph are resolved ONCE at
      `RivetServer::new -> load_project(project_dir)` and held in an RwLock;
      `project_dir` is a fixed `Arc<PathBuf>`. `rivet_validate` takes no
      params. Every `*Params` struct carries only data fields (ids, filters,
      titles, query strings) — NONE exposes a schema, externals, path,
      project, or config override. `rivet_reload` re-runs `load_project` on
      the SAME fixed dir. So rivet's MCP gate is currently fail-closed —
      but BY OMISSION, not by an explicit, tested invariant.

      The risk is latent: the day someone adds a convenience param (e.g.
      "validate against this schema" / "use this externals set") to an MCP
      tool, the hole opens silently with no guard. This REQ LOCKS the
      property as an explicit invariant + regression test, so the gate can
      never be told to look the other way by whoever drives the agent.
      Directly reinforces REQ-051 ("only CI enforcement provides
      traceability assurance"). Scope: hardening, not an urgent fix.

      Acceptance:
        - A documented invariant: no MCP tool parameter may alter schema or
          externals resolution for a validate/query/coverage/get operation.
        - A regression test asserts that an MCP-context validate cannot be
          pointed at an alternate schema/externals to suppress a diagnostic
          the default resolution emits (the test fails if such a param is
          ever introduced).
        - If a per-call override is ever genuinely needed, it is gated to
          trusted/local context with a fail-closed default (untrusted unless
          explicitly local), mirroring gbrain's `ctx.remote ?? true`.
    tags: [mcp, security, fail-closed, validate, audited, REQ-051, gbrain-derived]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-051

  - id: REQ-128
    type: requirement
    title: "rivet list --orphans: mechanical detection of asserted-but-unanchored claims"
    status: implemented
    description: |
      Sourced from garrytan/gbrain's `src/core/search/graph-signals.ts`
      (inbound-link / cross-source signals), taking ONLY the deterministic
      structural-graph part and rejecting the ML/boost layer entirely.

      In rivet's domain an orphan is not a lonely note — it is a requirement
      with no verifying test, a design decision linked to no hazard, a
      safety-case node nothing traces to: an asserted-but-unjustified claim,
      the exact knowledge-graph failure mode rivet's epistemics exist to
      prevent. So `--orphans` is not a nice-to-have report; it is the
      standing query that answers "does the graph contain any claim that
      isn't anchored?" — mechanical enforcement of the thing the toolchain
      rests on. rivet already computes the signal
      (`links::LinkGraph::backlinks_to`, `reachable-from`/`reachable-to` in
      `sexpr_eval.rs`); the gap is surfacing it. No scoring, no fuzziness —
      ordering is by exact integer link counts only.

      Acceptance:
        - `rivet list --orphans` lists artifacts with no inbound AND no
          outbound links (the existing orphan diagnostic as a queryable
          view).
        - A structural report (CLI and/or a `{{group:backlinks}}` embed)
          ranks artifacts by inbound-link count using `LinkGraph`,
          deterministic and reproducible.
        - No relevance/semantic ranking is introduced; ordering is exact
          integer counts only.
    tags: [query, graph, backlinks, orphans, epistemics, gbrain-derived]
    fields:
      priority: should
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-129
    type: requirement
    title: "validate --review-candidates: report the unclassifiable without accepting it"
    status: draft
    description: |
      Sourced from garrytan/gbrain's `src/core/schema-pack/candidate-audit.ts`
      (when a page's type isn't in the active pack, gbrain neither silently
      accepts nor silently drops it — it appends a SHA-redacted record to a
      rotating audit and surfaces `schema review-candidates`). The adaptation
      keeps gbrain's CAUTION and drops its ACCEPTANCE.

      rivet must NOT accept unknown types — that stays a hard validation error
      (loud-fail default preserved). But a structured "what did validation see
      that it could not classify" REPORT is legitimately useful for schema
      evolution: unknown-type artifacts, fields not in any loaded schema, and
      unresolved `externals` `prefix:ID` references. This is a reporting view,
      never an ingest/accept path.

      SAFETY CONSTRAINT (must be designed in): the redaction must be genuinely
      one-way (e.g. SHA of content, names of frontmatter keys but never their
      values) and the report must NOT become a side channel that leaks
      artifact content past the classification boundary. For most rivet
      content this is moot, but if any artifact carries a constraint that must
      never be named publicly, the candidate report is a place that constraint
      has to hold. Cheap to get right, expensive to get wrong.

      Acceptance:
        - `rivet validate --review-candidates` prints a deterministic report
          of unknown-type artifacts, unknown fields, and unresolved externals
          refs — WITHOUT changing exit status semantics (unknown type is still
          an error; the report is additive).
        - The report emits only redacted identifiers (hashes / key names), never
          field values or descriptions; a test asserts no artifact body content
          appears in the output.
        - Default behavior is unchanged (loud-fail); the report is opt-in.
    tags: [validate, schema-evolution, audit, redaction, fail-closed, gbrain-derived]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-130
    type: requirement
    title: "Supersession as an additive typed link that preserves the node (not deletion)"
    status: draft
    description: |
      Sourced from garrytan/gbrain's `src/core/facts-fence.ts` (supersession is
      an explicit, in-record, reasoned act that survives in git — `~~claim~~`
      + `superseded by #N`), reshaped onto an existing rivet pattern rather
      than a new directive or a markdown fence parser.

      Standardize a `superseded-by:` + `superseded-reason:` field convention on
      the requirement schema (via schema `extends`), validated deterministically.

      SAFETY CONSTRAINT (the whole point): in a safety domain supersession is
      NOT deletion. A requirement that was live for three release cycles and is
      then superseded is part of the certification evidence trail, not garbage
      to hide. gbrain's git-delete -> soft-delete model is WRONG here.
      `superseded-by` MUST be an additive typed graph edge that PRESERVES the
      node — the superseded artifact stays in the store and in coverage, just
      marked. It is never a soft-delete and never drops the node from the
      traceability graph.

      Acceptance:
        - `superseded-by: REQ-NNN` + `superseded-reason:` validate
          deterministically: target must exist; an artifact cannot supersede
          itself; reason is required when `superseded-by` is present.
        - A superseded artifact remains in the store, the link graph, and
          coverage reporting (marked superseded, not removed).
        - Built on schema `extends` + existing id-resolution (`prefix:ID`),
          introducing no new config surface or directive.
    tags: [schema, lifecycle, supersession, traceability, safety, gbrain-derived]
    fields:
      priority: could
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-010

  - id: REQ-132
    type: requirement
    title: "Completeness diagnostics name the required chain, not just the terminal artifact"
    status: draft
    description: |
      User-reported via GitHub issue #350 (pulseengine/synth, aspice
      schema). Flipping a `sw-req` to `status: implemented` produces
      `missing: unit-verification, sw-integration-verification`, which
      names the TERMINAL artifacts but not the required intermediate
      CHAIN (`sw-req -> sw-arch-component -> sw-detail-design ->
      {unit,integration}-verification`), and does not say a verification
      cannot link a sw-req directly. The reporter only discovered the
      layered model by trying a direct `verifies` link and reading the
      target-type rejection — the wrong message taught the right thing.

      This is the traceability/completeness-rule analogue of REQ-124
      (which covers link-target-type / broken-link / etc. but
      deliberately deferred the traceability rules, since those key on a
      dynamic `rule.id`). Extend the remediation pass to completeness/
      traceability diagnostics: emit a "how to satisfy" line naming the
      intermediate artifact chain and the link types that connect it,
      derived deterministically from the schema's link-fields +
      traceability rules.

      Acceptance:
        - The completeness diagnostic for an implemented req whose
          required terminal verification cannot link directly includes a
          help line naming the intermediate chain (e.g. "needs a
          sw-detail-design that `satisfies` <id>, verified by a
          unit-verification").
        - Derivation is deterministic from the schema (no hardcoded
          ASPICE knowledge); `rivet docs diagnostics/<rule>` resolves.
    tags: [validate, diagnostics, remediation, agent-ux, user-reported, issue-350]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-133
    type: requirement
    title: "rivet implement/scaffold-verification: guided stubs for the verification chain"
    status: draft
    description: |
      User-reported via GitHub issue #350. Marking shipped work as
      `implemented` + verified currently means hand-authoring the full
      ASPICE chain (`sw-arch-component`, `sw-detail-design`, the
      verification artifacts) wired with exactly the right link types —
      reverse-engineered from link-target-type rejections. There is no
      shortcut for "this requirement is implemented and here are the
      tests that cover it."

      Provide a guided command (e.g. `rivet implement <id>` /
      `rivet scaffold-verification <id>`) that, from the schema's
      traceability rules, stubs the minimal intermediate + verification
      artifacts wired with the correct link types and prints what it
      created, so marking shipped work implemented is a guided step. Built
      on the existing mutation/add path; the scaffold must itself pass
      `rivet validate`.

      Acceptance:
        - `rivet implement <req-id>` (name TBD) creates the minimal chain
          of artifacts the schema requires to mark <req-id> verified,
          wired with correct link types, and the result validates clean.
        - A dry-run mode prints the plan without writing.
    tags: [cli, mutation, scaffold, agent-ux, user-reported, issue-350]
    fields:
      priority: could
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-134
    type: requirement
    title: "Per-project field extension for built-in schema types (without forking the schema)"
    status: implemented
    description: |
      User-reported via GitHub issue #350. A project's `sw-req` artifacts
      carry a `crate:` field the built-in aspice `sw-req` schema does not
      declare, producing 37+ `INFO: field 'crate' is not defined in
      schema` diagnostics on every `rivet validate` — drowning the
      actionable errors. There is currently no easy, documented way to
      add a project-specific field to a built-in type without forking the
      whole schema.

      Provide a documented per-project field extension that builds on the
      existing schema `extends` mechanism (REQ-106 register: reuse, don't
      invent a directive): a project schema declares additional fields on
      a built-in type, and validate treats them as known. This both
      silences the noise and keeps the field schema-validated.

      RESOLUTION: the mechanism already exists — `Schema::merge` /
      `ArtifactTypeDef::merge_in_place` union `fields` by name across
      schemas that share a type name, so a project-local schema
      re-declaring the built-in type with the extra field (registered
      after the built-in in `project.schemas`) adds it without forking.
      Verified empirically (the `crate` INFO disappears with the
      extension, is present without it) and documented in
      docs/schemas.md via REQ-149 / PR #395 — which was an accidental
      duplicate of this REQ (filed before noticing REQ-134; see the
      duplicate-on-add friction filed this iteration).

      Acceptance:
        - A project can declare extra fields on a built-in artifact type
          via `extends` (or equivalent) so those fields no longer raise
          `unknown-field`, without copying the upstream schema.
        - Documented in `rivet docs` with a copy-pasteable example.
    tags: [schema, extends, validate, dx, user-reported, issue-350]
    fields:
      priority: could
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-010

  - id: REQ-135
    type: requirement
    title: "Validate and enforce the status enum (typo'd statuses must not pass silently)"
    status: draft
    description: |
      User-reported cluster: GitHub issues #352, #354, #355, #353
      (multiple downstream projects: relay/falcon, sigil, meld). Verified
      against source.

      Three converging root causes:
      1. `common.yaml` declares the `status` base-field as `type: enum`
         but with NO `allowed-values` — an empty enum, so there is
         nothing to validate against and nothing for `schema show` to
         list.
      2. `validate` never checks `status` at all: the allowed-values
         pass iterates `artifact.fields`, but `status` is the top-level
         `artifact.status`, so even a declared enum would be ignored.
      3. `mutate.rs` explicitly accepts any status string
         ("generally freeform, but we'll accept it").

      Live dogfood evidence (rivet's OWN repo): `grep status: artifacts/`
      yields 246 approved, 140 draft, 20 implemented — plus 4 `accepted`,
      2 `active`, 1 `proposed`, 1 `partial`: ad-hoc statuses passing
      validate with no diagnostic. A fat-fingered status silently drops
      an artifact out of every status-based query/gate — the silent-error
      class that erodes trace trust.

      ROOT CAUSE (found iter-8, verified): the merged `Schema` struct
      (schema.rs) does NOT retain `base-fields` at all — they are parsed
      onto `SchemaFile` and dropped by `Schema::merge`, and `.base_fields`
      is read nowhere outside parsing. So `status` (a base field) can never
      be enum-validated: the validator never has its definition, regardless
      of whether `allowed-values` is declared. The mechanism fix is
      therefore two-part and NON-breaking:
        (a) retain `base_fields: Vec<FieldDef>` on the merged `Schema`
            (populate in `Schema::merge`; ~10 explicit constructors need a
            `base_fields: Vec::new()`);
        (b) in validate, check `artifact.status` against the `status`
            base-field's `allowed-values` when declared (with REQ-124
            remediation). With no values declared, behavior is unchanged.
      Only AFTER the mechanism lands does declaring the canonical set in
      common.yaml activate enforcement — keeping the taxonomy a separate,
      explicit maintainer act.

      DESIGN DECISION (maintainer): the canonical lifecycle value set.
      De-facto is `draft -> approved -> implemented` (+ `obsolete`). The
      built-in `accepted`/`active`/`proposed`/`partial` are drift to
      reconcile.

      Acceptance:
        - The `status` base-field in `common.yaml` declares
          `allowed-values` (the canonical lifecycle).
        - `rivet validate` flags a status outside the enum
          (loud-fail, with REQ-124 remediation: "did you mean
          'implemented'?"); a shell test sets a typo'd status and asserts
          a diagnostic.
        - `rivet modify --set-status <bad>` rejects or warns on values
          outside the enum.
        - `rivet schema show <type>` lists the allowed `status` values.
        - rivet's own artifacts are reconciled to the canonical set so
          validate stays green.
    tags: [validate, schema, status, lifecycle, enum, bug, user-reported, cluster]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-136
    type: requirement
    title: "rivet modify ergonomics: --set-description flag + positional-status hint"
    status: implemented
    description: |
      User-reported via GitHub issues #359 and #360 (status-audit work on
      multiple projects). Two small `rivet modify` friction points:

      - #360: there is no `--set-description` flag; updating a
        description requires `--set-field description=...`, which is
        non-obvious (description is a top-level base field, not a
        domain field). The documented path does not match the field's
        first-class status.
      - #359: the positional form `rivet modify <ID> status <value>`
        fails with a bare clap error and no hint that the flag is
        `--set-status`. The error should guide toward the flag.

      Acceptance:
        - `rivet modify <ID> --set-description "<text>"` works (and
          `--set-title` for symmetry if not already present).
        - Misusing a positional where a `--set-*` flag is expected
          produces an error that names the correct flag.
    tags: [cli, modify, dx, agent-ux, user-reported]
    fields:
      priority: could
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-131
    type: requirement
    title: "required-backlink rules must match the inverse link-type name"
    status: implemented
    description: |
      User-reported via GitHub issue #349 (found on rivet 0.13.3 while
      bringing pulseengine/loom's safety case green; verified against
      source before fixing). Any traceability rule using
      `required-backlink` (e.g. a GSN `goal-has-support` rule whose
      `required-backlink: supported-by`) failed for EVERY artifact even
      when the supporting link was correctly present.

      Root cause: `Backlink.link_type` holds the FORWARD link name and
      `Backlink.inverse_type` the inverse, but `validate.rs` and
      `coverage.rs` compared the schema's `required-backlink` value (the
      INVERSE name) against `Backlink.link_type` (the FORWARD name) — they
      never matched: a false-positive error, the worst class for a
      traceability tool.

      Fixed in PR #351: match on EITHER name at both sites; the same fix
      additionally evaluates `rule.alternate_backlinks` in both engines
      (previously a goal satisfied only via an alternate backlink still
      errored). Regression test `required_backlink_matches_inverse_name`.

      (Re-filed: this REQ artifact was dropped when #351 was reworked to
      add alternate-backlink support; the code shipped without it.)

      Acceptance:
        - A `required-backlink` rule whose value is the INVERSE link name
          is satisfied by a forward link of the corresponding type.
        - validate and coverage agree on the same data; the forward-name
          convention still passes.
    tags: [validate, coverage, traceability, backlink, bug, user-reported, issue-349]
    fields:
      priority: must
      category: functional
      baseline: v0.14.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-137
    type: requirement
    title: "validate --min-severity: filter displayed diagnostics to cut advisory noise"
    status: implemented
    description: |
      Self-reported via GitHub issue #357 (dogfooding). `rivet validate`
      prints every diagnostic regardless of severity — on rivet's own repo
      that is ~160 warnings (lifecycle aspirations, prose-mention notes)
      for 0 errors, burying any actionable diagnostic. `--fail-on`
      controls the exit-code threshold but still prints everything; there
      was no way to say "show me only what I must act on."

      Add `rivet validate --min-severity <error|warning|info>` (text
      output) that DISPLAYS only diagnostics at or above the floor. Counts
      and exit code are unchanged (computed from the full set); when the
      filter hides anything, a one-line note reports how many of how many
      were shown, so nothing is silently dropped (loud-fail register).
      Reuses the existing `--fail-on` severity parser.

      Acceptance:
        - `rivet validate --min-severity error` on a warning-only repo
          prints no `WARN:`/`INFO:` lines and notes "(showing N of M …
          at or above 'error')".
        - Exit code and the summary counts are identical with and without
          `--min-severity` (display-only).
    tags: [validate, diagnostics, dx, agent-ux, signal-to-noise, user-reported, issue-357]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-138
    type: requirement
    title: "Zola export must produce a buildable site — end-to-end build smoke check"
    status: implemented
    description: |
      Self-reported via GitHub issue #378 (dogfooding). The Zola export
      tests assert on generated *strings*, so a whole class of bug ships
      undetected: the export exits 0 but the output is broken downstream —
      absolute links that 404 on a sub-directory deploy (REQ-115/116/118),
      a dangling `@/` internal link that aborts `zola build`, a missing
      shortcode template, a missing taxonomy. "Export succeeded" does not
      today imply "the site is usable" (loud-fail register).

      Two complementary mechanisms:
        1. An optional CI smoke job (gated on `zola` availability, like the
           continue-on-error Kani/Verus jobs): export the dogfood corpus,
           run `zola build` under a sub-directory `base_url`, fail on a
           build error or on any generated link that drops the sub-path.
        2. A documented one-command repro recipe — or `rivet export
           --format zola --scaffold` emitting a minimal *buildable*
           config.toml + required templates — so verifying export
           correctness does not require hand-rolling a Zola site.

      Acceptance:
        - A script (committed under e.g. scripts/ or a gated CI job) runs
          `rivet export --format zola --shortcodes` into a scaffolded site
          with a sub-directory `base_url`, runs `zola build`, and exits
          non-zero if the build fails OR if `grep -r 'href="/<prefix>/'`
          finds any generated absolute artifact link in the built HTML.
        - The check passes on the current dogfood corpus (post-#377) and
          would have failed on the pre-#377 absolute-link export.
    tags: [export, zola, ci, loud-fail, self-validation, dogfooding, issue-378]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-115

  - id: REQ-139
    type: requirement
    title: "Directory import: only warn on malformed artifact files, stay silent for legitimate non-artifact YAML"
    status: implemented
    description: |
      Self-reported via GitHub issue #353 part 4 (dogfooding meld). The
      generic-YAML directory import warned `[WARN] skipping <file>: …` for
      EVERY file it declined to load — including legitimate non-artifact YAML
      that lives under the artifacts source path (bindings.yaml,
      feature-model.yaml, variants/*.yaml). On a real project that is a WARN
      line on every single command, burying the signal (loud-fail register:
      noise that cries wolf is as bad as silence).

      The REQ-062 `SkipKind` classification already distinguishes a malformed
      artifact file (`ParseError`) from expected non-artifact YAML
      (`NotArtifactFile`), but the load-path WARN ignored it. Gate the WARN
      on `classify_skip(...) == ParseError`: a real problem still warns (and
      `validate` still surfaces it as a hard `artifact-parse-error`); the
      expected case is silent.

      Acceptance:
        - `rivet list` on a project whose artifacts/ dir contains
          bindings.yaml / variants/*.yaml prints no `skipping …` WARN lines.
        - A genuinely malformed artifact file (e.g. `id:`+`type:` at top
          level, or `artifacts:` plus an unknown sibling key) still prints a
          `skipping …` WARN at load and a `artifact-parse-error` ERROR under
          `rivet validate`.
    tags: [dx, agent-ux, signal-to-noise, loud-fail, user-reported, issue-353]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-062

  - id: REQ-140
    type: requirement
    title: "Generic-YAML load is inconsistent across commands: validate loads a file that list/get/export silently drop"
    status: draft
    description: |
      Surfaced while verifying issue #353 against current `main` (the silent
      file-skip of #353 part 1 is already fixed by REQ-062). A file with an
      `artifacts:` list AND an unknown sibling top-level key (the reporter's
      `traceability.yaml` with `loss-coverage:`) is handled inconsistently:

        * `rivet list` / `get` / `export` build the store via the strict
          `load_artifacts` adapter (serde `deny_unknown_fields`) and DROP the
          whole file — its artifacts are invisible to these commands.
        * `rivet validate` builds its store via the lenient
          `yaml_hir::extract_schema_driven` path, which tolerates the unknown
          key and LOADS the artifacts — then ALSO reports the same file as
          `artifact-parse-error` "failed to parse".

      So the same project yields different artifact sets depending on the
      command, and `validate` simultaneously validates an artifact (REQ-2)
      and declares its file unparseable — a contradictory signal. Verified
      empirically: `rivet list` shows only REQ-1; `rivet validate` shows
      REQ-2 as a loaded (mis-typed) artifact plus the parse-error.

      This is a design decision (which parser is canonical, and is the
      tolerant behaviour intended?) — flagging for the maintainer rather than
      unilaterally picking. The fix should make every command agree, on the
      loud-fail side: a file that can't be cleanly parsed as artifacts should
      be treated identically everywhere (either all load-and-warn, or all
      drop-and-error), never load in one command and vanish in another.

      Acceptance:
        - For a fixture file with `artifacts:` + an unknown sibling key,
          `rivet list --format json` and the set `rivet validate` type-checks
          contain the SAME artifact IDs.
        - `rivet validate` does not both load an artifact and report its
          source file as failed-to-parse.
    tags: [bug, consistency, loud-fail, parser, user-reported, issue-353, needs-maintainer-decision]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-062

  - id: REQ-141
    type: requirement
    title: "rivet modify --where <filter>: query-driven bulk status/field set in one race-free pass"
    status: implemented
    description: |
      Feature ask from GitHub issue #353 (dogfooding meld). Bringing a real
      project's requirement statuses in line with shipped code meant a bulk
      `draft`/`planned` -> `implemented` flip, and the only tool was a shell
      loop of per-ID `rivet modify` invocations — which the reporter found
      silently no-op'd under redirection (a suspected reload/rewrite race
      between rapid-succession subprocesses).

      Add `rivet modify --where '<s-expr>' --set-status … ` (and any other
      `--set-*`): select every artifact matching the same s-expression filter
      `rivet query` uses, and apply the change in a SINGLE in-process pass —
      load once, validate every target up front (all-or-nothing), then write
      each affected file once. Because it never re-spawns a subprocess per ID,
      it cannot race the way the shell loop could. `--where` is mutually
      exclusive with a positional `<ID>` (clap-enforced). `--dry-run` previews
      the match set without writing. An empty match set is a loud no-op
      ("no artifacts match the --where filter") so silence is never read as
      success. Reuses the existing `sexpr_eval` engine (design on existing
      patterns, not a new filter dialect).

      Acceptance:
        - `rivet modify --where '(= status "draft")' --set-status implemented
          --dry-run` lists the matching IDs and leaves every file byte-identical.
        - Without `--dry-run` the same command flips exactly the matching
          artifacts (non-matches untouched) and prints "modified N artifacts".
        - `rivet modify <ID> --where '…'` is rejected (mutually exclusive).
        - A `--where` matching nothing prints "no artifacts match" and exits 0.
    tags: [cli, modify, bulk, agent-ux, dx, loud-fail, user-reported, issue-353]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-142
    type: requirement
    title: "s-expr filter parse error shows the common field-equality example"
    status: implemented
    description: |
      Self-reported via GitHub issue #381 (dogfooding). The s-expression
      filter dialect (shared by `query`, `list --filter`, `export --filter`,
      `modify --where`) puts an OPERATOR in head position, but the single most
      common first attempt is `(status "draft")` — field name in head — which
      fails with `unknown form 'status'`. The error listed every supported
      head form but showed no example of the correct `(= field "value")`
      shape, costing every first-time author (human or agent) a failed
      round-trip + a grep through the test files to discover it.

      The `unknown head symbol` note now states the head is an operator (not a
      field name) and shows the field-equality form inline:
      `(= status "draft")` / `(and (= type "requirement") (has-tag "safety"))`.
      Because the note is generated once in `sexpr_eval`, the improvement
      reaches every command that parses a filter. The `export --filter` help
      also gained an example (it previously had none, unlike `list`/`query`).

      Acceptance:
        - `rivet list --filter '(status "draft")'` (or any command) prints a
          note containing `(= status "draft")`.
        - `cargo test -p rivet-core --lib
          sexpr_eval::tests::parse_error_unknown_head_surfaces_note` passes
          (asserts the example is present).
    tags: [dx, agent-ux, discoverability, sexpr, user-reported, issue-381]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-143
    type: requirement
    title: "External artifact refs with a hyphenated prefix (kebab project slug) must resolve, not 404"
    status: implemented
    description: |
      User-reported (0.14.0). Browsing `/artifacts/linc-mesh:A-AVTP-STREAM` in
      serve returned "Artifact does not exist", and external `prefix:ID` refs
      with a hyphenated prefix were unresolved in document rendering, even
      though the artifact existed in the external project.

      Root cause: `externals::parse_artifact_ref` required the prefix to be
      PURELY `is_ascii_lowercase()` — no hyphens. A kebab-case project slug
      like `linc-mesh` failed the check, so the ref fell through to
      `ArtifactRef::Local("linc-mesh:A-AVTP-STREAM")` and was looked up as a
      local id (404). But externals are stored as `<prefix>:<id>` with that
      same hyphenated prefix, so the parse no longer round-tripped its own
      stored form. Every external whose prefix contained a hyphen was
      unreachable in serve detail view and in document link resolution.

      Fix: accept a kebab-case slug prefix — starts with a lowercase letter,
      then lowercase letters / digits / hyphens. `parse_artifact_ref` is the
      single shared gate (serve detail handler + document link rendering both
      delegate to it), so one fix restores both surfaces. The Kani
      round-trip proof was updated to the new contract.

      Acceptance:
        - `parse_artifact_ref("linc-mesh:A-AVTP-STREAM")` returns
          `External { prefix: "linc-mesh", id: "A-AVTP-STREAM" }`.
        - A ref whose prefix does not start with a lowercase letter (e.g.
          `H-1:2`) stays `Local`.
        - `cargo test -p rivet-core --lib externals::tests` passes.
    tags: [bug, externals, serve, regression, user-reported, v0.14.0]
    fields:
      priority: must
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-085

  - id: REQ-144
    type: requirement
    title: "Source-view artifact-id linking must match whole tokens, not substrings"
    status: implemented
    description: |
      User-reported. In the source view, an artifact id that is a SUBSTRING of
      another id gets wrongly linked. Example: a doc carrying
      `doc-id: SDV-BCM-SWR-001` also has an artifact `SWR-001` in the store;
      because matching uses `str::contains`, `SWR-001` is detected inside
      `SDV-BCM-SWR-001`, producing a spurious link. For a traceability tool
      this is a correctness bug — a phantom trace edge.

      Location: `rivet-cli/src/render/source.rs` lines ~841 and ~856 use
      `line.contains(id.as_str())`. The fix is a whole-token match: an id only
      matches when it is delimited by non-`[A-Za-z0-9-]` characters (ids
      contain hyphens, so hyphen is part of the token, not a delimiter).

      Acceptance:
        - A source line containing `SDV-BCM-SWR-001` does NOT produce a link
          to a shorter artifact `SWR-001` that is merely a substring.
        - A line containing the exact id `SWR-001` (surrounded by whitespace /
          punctuation) still links to it.
        - Regression test in source.rs covers the substring vs whole-token case.
    tags: [bug, source-view, traceability, correctness, user-reported]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-092

  - id: REQ-145
    type: requirement
    title: "Test-results import: honor a rivet_tc_id property for the testcase artifact id"
    status: implemented
    description: |
      User-reported. When importing JUnit-style test results, rivet derives
      the testcase artifact id from `classname` + `name` (the
      `format!("{}.{}", classname, name)` fallback in
      `rivet-core/src/junit.rs::artifact_id_for`), so the link to the intended
      artifact does not resolve. The user records an explicit id per testcase:
      a `rivet_tc_id` property (e.g. value `SYS-VER-058`) and wants rivet to
      use it.

      The JUnit parser does not currently read `<property>` children of
      `<testcase>` (the `ParsedCase` struct has no properties field; the
      element is silently ignored). Add: parse `<property>` children, and in
      `artifact_id_for` add a highest-priority tier that returns the
      `rivet_tc_id` property value when present, before the existing
      classname/name heuristics.

      Acceptance:
        - A testcase (classname `com.x.Y`, name `t`) carrying a `rivet_tc_id`
          property of `SYS-VER-058` imports with artifact id `SYS-VER-058`,
          not `com.x.Y.t`.
        - A testcase with no such property keeps the current classname/name
          behaviour (back-compatible).
        - Parser test covers the property-present and property-absent cases.
    tags: [feature, test-results, junit, import, traceability, user-reported]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-101

  - id: REQ-146
    type: requirement
    title: "Salsa validate path must evaluate status-gate validation-rules (parity with the direct path)"
    status: implemented
    description: |
      Surfaced verifying issue #355 Finding 3 ("rivet validate and rivet check
      gaps-json disagree"). `rivet validate` runs the incremental salsa path
      (`db::validate_all`), which evaluated structural rules (phases 1-7) and
      conditional rules (phase 8) but NOT the status-gate `validation-rules`
      (phase 9 — the `(implies …)` V-model promotion gates). The direct
      `validate::validate*` path (used by `rivet check gaps-json` and
      `rivet validate --direct`) DID evaluate them. Result: the default
      `rivet validate` silently PASSED a project with a status-gate violation
      that `gaps-json`/`--direct` correctly FAILED — a validation soundness
      gap, since the default surface under-reported.

      Fix: `db::validate_all` and `validate_all_with_extras` now also call
      `validate::evaluate_validation_rules` against the same materialized
      store/schema/graph, so every validation surface agrees.

      Acceptance:
        - On a project whose schema declares a `validation-rules` status gate
          that an artifact violates, `rivet validate` (default/salsa) and
          `rivet validate --direct` report the SAME error count, both
          including the gate diagnostic.
        - `cargo test -p rivet-core --lib
          db::tests::validation_rules_evaluated_in_validate_all` passes.
    tags: [bug, validation, salsa, soundness, consistency, issue-355]
    fields:
      priority: must
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-029

  - id: REQ-147
    type: requirement
    title: "Coverage diagnostics say HOW to satisfy a missing backlink, not just what's missing"
    status: implemented
    description: |
      User-reported via #350. Marking a sw-req `implemented` then running
      `rivet validate` produced "should be verified..." / "missing:
      <types>" diagnostics that named WHAT was missing but not HOW to satisfy
      it — which link type connects the artifacts, and that the listed types
      attach via a specific incoming link. The reporter reverse-engineered the
      required shape from an unrelated link-target rejection ("a whole
      detour").

      Two complementary, schema-agnostic improvements:
        1. The `required-backlink` coverage diagnostic now appends the
           incoming link type and the allowed source types, e.g. "… — needs an
           incoming `verifies` link from one of [sw-verification,
           unit-verification, sw-integration-verification]".
        2. The `Lifecycle coverage gaps` summary now points at
           `rivet validate --explain <ID>`, which lists each rule's
           satisfied/MISSING state with the exact incoming link + source types
           (and any alternates).

      Acceptance:
        - On a sw-req with no verification backlink, `rivet validate` output
          for the coverage rule contains "needs an incoming `verifies` link
          from one of [...]".
        - When lifecycle gaps are present, the validate output prints a
          "run `rivet validate --explain <ID>`" pointer.
        - `cargo test -p rivet-core --lib validate::tests` passes.
    tags: [dx, agent-ux, diagnostics, guidance, traceability, user-reported, issue-350]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-125

  - id: REQ-148
    type: requirement
    title: "Schema-consistency check: flag a required-backlink rule whose from-types cannot form the link"
    status: implemented
    description: |
      Surfaced verifying #350 against the aspice schema. The coverage rule
      `swe1-has-verification` declares `source-type: sw-req`,
      `required-backlink: verifies`, `from-types: [sw-verification,
      unit-verification, sw-integration-verification]` — but the link-target
      rules only permit `sw-verification` to `verifies` a `sw-req`
      (unit-verification verifies sw-detail-design; sw-integration-verification
      verifies sw-arch-component/sw-detail-design). So two of the three
      advertised satisfiers can never actually form the backlink, which is
      exactly why the reporter authored a forbidden unit-verification→sw-req
      link and got rejected.

      Add a schema-consistency meta-check (sibling of
      `conditional-rule-consistency`) that flags a `required-backlink` rule
      whose `from-types` include an artifact type that no link-type / rule
      permits to form `<required_backlink>` to the rule's `source-type`. This
      catches the "advertises an unsatisfiable satisfier" class in ANY schema.
      Care needed: target constraints live in both link-type defs and per-type
      link-fields, and the inverse-name convention applies — implement
      conservatively to avoid false positives (prefer false-negatives).

      Note: whether the aspice `from-types` is itself wrong (ASPICE intent:
      SWE.6 sw-verification verifies sw-reqs; unit/integration verify design
      layers) is a maintainer/compliance-semantics call, tracked on #350/#355,
      separate from this generic check.

      Acceptance:
        - A schema with a `required-backlink` rule listing a from-type that
          cannot form the link emits a `coverage-rule-consistency` diagnostic
          naming the rule, the unsatisfiable from-type, and the link.
        - A schema whose from-types are all linkable emits no such diagnostic.
        - Unit test covers both cases.
    tags: [validation, schema-consistency, meta-check, dx, issue-350, issue-355]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-149
    type: requirement
    title: "Document the recipe for extending a built-in artifact type with project fields"
    status: implemented
    description: |
      User-reported via #350 (suggestion 4): a project's artifacts carried a
      `crate:` field the built-in ASPICE `sw-req` doesn't declare, producing
      37+ `INFO: field 'crate' is not defined in schema` advisories that
      drowned the actionable diagnostics. The reporter asked for "an easy
      per-project field extension... without forking the schema."

      Verified the mechanism already exists: `Schema::merge` /
      `ArtifactTypeDef::merge_in_place` union `fields` by name across schemas
      sharing a type name, so a project-local schema re-declaring the built-in
      type with only the extra field — registered AFTER the built-in in
      `rivet.yaml` `project.schemas` — adds the field without forking.
      Confirmed empirically: with such an extension the `crate` INFO
      disappears; without it the INFO is present. It was simply undocumented.

      Documented in docs/schemas.md ("Extending a built-in artifact type with
      project fields"), with the copy-pasteable recipe and the
      `--min-severity warning` alternative; also corrected the
      "Schema merging behavior" note (artifact types merge fields by name,
      they don't replace wholesale).

      Acceptance:
        - docs/schemas.md contains the "Extending a built-in artifact type"
          recipe (project-local schema re-declaring the type + registering it
          after the built-in).
        - `rivet docs check` PASS.
    tags: [docs, dx, schema, user-reported, issue-350, duplicate-of-req-134]
    fields:
      priority: should
      category: documentation
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-010
      - type: traces-to
        target: REQ-134

  - id: REQ-150
    type: requirement
    title: "rivet add should warn when a new artifact closely duplicates an existing one"
    status: draft
    description: |
      Self-found while dogfooding (this loop). `mutate::validate_add`
      rejects a duplicate `id` but does nothing about a duplicate
      *intent*: an author can file a requirement whose title/description
      restates an existing one and rivet accepts it silently. I did
      exactly this — REQ-149 ("Document the recipe for extending a
      built-in artifact type…") duplicated the pre-existing REQ-134
      ("Per-project field extension for built-in schema types…"); only an
      `rivet list` audit a day later surfaced it. For an agent-driven
      workflow (the explicit use case — agents file findings as
      artifacts) this is a recurring trap: the backlog silently accretes
      near-duplicates.

      Add a discovery aid at add time (and/or a `validate` advisory): when
      a new artifact's title/keywords strongly overlap an existing
      artifact of the same type, surface it — "similar to REQ-134: '…'
      — file anyway?" / an INFO diagnostic listing near-duplicate pairs.
      Design questions to settle: the similarity signal (shared title
      tokens vs an edit-distance threshold — must avoid noise on
      legitimately-distinct short titles), advisory vs blocking, and
      whether it runs in `add`, `validate`, or both. Conservative default:
      non-blocking advisory; never reject.

      Acceptance:
        - Filing an artifact whose title shares a high fraction of
          significant tokens with an existing same-type artifact prints a
          "similar to <ID>" advisory (non-blocking).
        - Distinct artifacts (low token overlap) produce no advisory.
        - A unit test covers the duplicate-detected and distinct cases.
    tags: [dx, agent-ux, mutate, duplicate-detection, dogfooding, self-found]
    fields:
      priority: could
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-151
    type: requirement
    title: "Global --quiet flag: suppress the WARN log preamble for scripted/agent use"
    status: implemented
    description: |
      User-reported via #353 (feature ask): every rivet invocation can emit
      WARN-level log lines before a command's real output (e.g. "could not
      load externals: …", and previously per-file skip notices), which is
      noise for scripted/agent consumption — especially combined with
      `--format json`. There was a `-v/--verbose` flag to raise the log
      level but no way to lower it below the default `warn`.

      Add a global `-q/--quiet` flag (mutually exclusive with `--verbose`)
      that sets the log filter to `error`, suppressing the WARN preamble
      while leaving a command's own stdout and hard-error reporting intact.
      Pairs with `--format json` for clean machine-consumable output.

      Acceptance:
        - `rivet --quiet <cmd>` emits no `WARN`-level log lines on stderr for
          a project that otherwise would (e.g. a misconfigured external),
          while the command's stdout is unchanged.
        - Hard errors (e.g. an unparseable rivet.yaml) are still reported
          under `--quiet`.
        - `cargo test -p rivet-cli --test cli_commands quiet_suppresses` passes.
    tags: [cli, dx, agent-ux, logging, quiet, user-reported, issue-353]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-152
    type: requirement
    title: "rivet matrix must not silently render an all-empty matrix as 'no traceability'"
    status: implemented
    description: |
      Self-found while dogfooding. `rivet matrix --from <X> --to <Y>` defaults
      to `--direction backward` and auto-detects the link type
      (backward→`verifies`). For a FORWARD relationship like
      `design-decision --satisfies--> requirement`, the defaults produce an
      all-`(none)` matrix — falsely reading as "no traceability" — even though
      every design-decision satisfies a requirement. For a traceability tool,
      a silent empty matrix that contradicts reality is a dangerous
      false-negative (loud-fail register).

      Verified: `matrix --from design-decision --to requirement` (defaults)
      shows every row `(none)`; `--link satisfies --direction forward` shows
      the real `DD-001 -> REQ-006` … links.

      Fix (additive, no behaviour change to valid matrices): when the matrix
      is all-empty (sources exist, zero links), emit an actionable hint on
      stderr. If the opposite direction would surface links, name it
      precisely ("… 0 via 'satisfies' (backward), but 67 do with
      `--direction forward` — the relationship runs the other way"); otherwise
      point at `rivet schema show <from>` / `--link`. A matrix with links
      emits no hint.

      Acceptance:
        - `rivet matrix --from design-decision --to requirement --link
          satisfies` (default backward) prints a stderr hint naming
          `--direction forward`.
        - The same with `--direction forward` (which has links) prints no hint.
        - `cargo test -p rivet-cli --test cli_commands
          matrix_empty_emits_direction_hint` passes.

      Follow-up (filed separately): inferring the direction from the link
      type's declared source/target so the default just works, rather than
      hinting after an empty result.
    tags: [cli, matrix, traceability, loud-fail, dx, dogfooding, self-found]
    fields:
      priority: should
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-153
    type: requirement
    title: "rivet impact --since must load the baseline through the canonical loader (not a bespoke parser)"
    status: implemented
    description: |
      Self-found while dogfooding (GitHub issue #403). `rivet impact --since
      HEAD~5` on the rivet repo reported 503 changed + 354 added (1207 total)
      when the only real change was ~7 new REQs in requirements.yaml — the
      output was misleading to the point of useless.

      Root cause: `load_baseline_from_git` reconstructed the baseline with a
      bespoke per-file parser (`parse_yaml_content` + `GenericYamlWrapper`)
      that (a) only handled a couple of source formats and silently skipped
      the rest (→ spurious "added") and (b) represented artifacts differently
      from the live `load_artifacts` adapter, so `impact::content_hash`
      differed for unchanged artifacts (→ every loaded baseline artifact
      "changed"). A third, inconsistent parse path — the REQ-140 family.

      Fix: materialise the ref's tracked tree via `git archive <ref> | tar`
      into a temp dir and load it through `rivet_core::load_project_full`
      (the SAME `load_artifacts`-per-source path the live store uses), then
      diff. The bespoke `parse_yaml_content` / `git_show_file` /
      `git_ls_tree_files` helpers + `GenericYamlWrapper` were deleted.

      Verified: `impact --since HEAD~5` now reports 3 changed (the real
      requirements.yaml edits) + 7 added (3 new REQs + 4 externals) instead
      of 503/354.

      Acceptance:
        - On a 2-commit fixture (one artifact edited, one added, one
          untouched), `rivet impact --since HEAD~1 --format json` reports
          exactly the edited id under `changed` and the new id under `added`;
          the untouched artifact appears in neither.
        - `cargo test -p rivet-cli --test cli_commands
          impact_since_reports_only_real_changes` passes.

      Residual (separate, smaller): externals are in the live store but not
      loaded into the baseline by `load_project_full`, so external `prefix:id`
      artifacts still show as "added". Tracked for follow-up.
    tags: [bug, impact, parser-consistency, loud-fail, dogfooding, self-found, issue-403]
    fields:
      priority: must
      category: functional
      baseline: v0.15.0-track
    links:
      - type: traces-to
        target: REQ-140

  - id: REQ-154
    type: requirement
    title: "Enumeration commands (list/stats) must loudly report sources skipped by parse errors, not silently return fewer artifacts"
    status: implemented
    description: |
      Self-found while triaging GitHub issue #353 (an agent driving rivet on
      the `meld` project). A single stray top-level key in an artifact file
      (`unknown field 'loss-coverage', expected 'artifacts'`) causes the
      ENTIRE file — including its valid `artifacts:` list — to be dropped from
      the graph, and the only signal is a `log::warn!` preamble line.

      `rivet validate` already handles this correctly: it surfaces the file as
      a hard `artifact-parse-error` ERROR diagnostic (verified). The gap is the
      enumeration commands: `rivet list` prints "0 artifacts" and `rivet stats`
      its counts with NO indication that a whole source was dropped. An agent
      that pipes the WARN preamble to /dev/null (or runs under a future
      `--quiet`) silently operates on a partial graph — a loud-fail (F2)
      violation for the read path. Issue #353 explicitly asks to "surface
      skipped sources in the validate/stats summary (e.g. '1 source skipped')".

      Fix: after loading, `list` and `stats` re-scan sources for
      `SkipKind::ParseError` skips (the existing REQ-062
      `load_artifacts_with_skips` channel) and, if any, print a concise loud
      block to stderr naming each dropped file + the parse error and pointing
      at `rivet validate`. Emitted regardless of log level (it signals data
      loss, not noise).

      Acceptance:
        - In a project whose `generic-yaml` source contains a file with a
          valid `artifacts:` list plus one unknown top-level key, `rivet list`
          exits 0 but prints to stderr a line matching
          `source(s) skipped` naming that file; `rivet stats` does likewise.
        - A project with no skipped sources prints no such block (no
          regression in the clean case).
        - `cargo test -p rivet-cli --test cli_commands
          list_reports_parse_error_skipped_sources` passes.
    tags: [bug, loud-fail, ux, dogfooding, self-found, issue-353]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-062

  - id: REQ-155
    type: requirement
    title: "prose-mention-without-typed-link must suppress when no schema-valid link type connects the two artifact types"
    status: implemented
    description: |
      From a cross-project agent report on issue #353 (the `sigil` project,
      "finding B"). After an agent edited a `cybersecurity-design` artifact's
      description to mention "UCA-4" as rationale, `rivet validate` raised:

        WARN: [CD-22] prose mentions 'UCA-4' but no typed link to it;
              add a link in `links:` or remove the mention

      Two defects:
        1. Unactionable remediation. `cybersecurity-design` only permits
           `satisfies -> cybersecurity-req`; the schema defines NO link type
           that could connect a design to a `uca`. The advised fix ("add a
           link in `links:`") is therefore impossible — the warning can only
           ever be cleared by rewording prose.
        2. False-trace pressure. The prose "UCA-4" referred to a local STPA
           report's numbering, but `UCA-4` happens to resolve to an
           unrelated artifact. Following the warning's advice would have
           created a wrong, fabricated trace link.

      Fix: in the prose-mention pass, suppress the diagnostic when the
      project schema permits no link type whose `source-types` allows the
      mentioning artifact's type AND whose `target-types` allows the
      mentioned artifact's type. A link type with empty `source-types` /
      `target-types` means "any -> any", so permissive schemas are
      unaffected; only fully-constrained schemas ever trigger suppression.
      When a link IS schema-valid, the warning fires unchanged.

      Acceptance:
        - In a schema whose only link type is `design -> requirement`, a
          `design` artifact whose prose mentions both a `uca` (unlinkable)
          and a `requirement` (linkable) yields exactly ONE
          prose-mention-without-typed-link warning — for the requirement,
          not the uca.
        - `cargo test -p rivet-core --lib
          prose_mention_suppressed_when_no_schema_valid_link_type` passes,
          and the existing `prose_mention_*` tests still pass.
        - `rivet validate` on the rivet repo still PASS.
    tags: [bug, diagnostics, false-positive, ux, dogfooding, issue-353]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-156
    type: requirement
    title: "Single chokepoint for schema-level consistency checks so salsa and --direct cannot diverge"
    status: implemented
    description: |
      Self-found while landing REQ-148 (#410). Adding a schema-level
      consistency check (one that inspects the schema, not artifacts) required
      hand-registering the same call in THREE validation entrypoints:
      `validate::validate_with_externals_and_variant` (direct path) and the two
      salsa queries in `db.rs` (`evaluate_conditional_rules`,
      `evaluate_conditional_rules_with_extras`). If a future check is added to
      only some sites, `rivet validate` (salsa, the default) and
      `validate --direct` silently disagree — the exact divergence class
      REQ-146 just fixed for status-gate rules.

      Fix: hoist all schema-level consistency checks behind a single
      `Schema::consistency_diagnostics()` method (conditional-rule dup/overlap
      + coverage-rule reachability). All three entrypoints now call exactly
      that one method, so a new check is a one-line edit there that cannot be
      partially applied. Pure refactor — diagnostic output is unchanged.

      Acceptance:
        - All three validation entrypoints call `schema.consistency_diagnostics()`
          and nothing calls `check_conditional_consistency` /
          `check_coverage_rule_consistency` directly outside that method and
          tests.
        - `rivet validate` and `rivet validate --direct` on the rivet repo
          produce identical Result lines (PASS, same warning count).
        - Unit test asserts `consistency_diagnostics()` surfaces BOTH a
          conditional-rule-consistency and a coverage-rule-consistency
          diagnostic for a schema that trips each.
        - `cargo test -p rivet-core` green; `rivet validate` on the rivet repo
          still PASS.
    tags: [refactor, validation, salsa-parity, maintainability, dogfooding, self-found, issue-410]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-146

  - id: REQ-157
    type: requirement
    title: "rivet validate must emit the per-file parse-error skip WARN once, not twice"
    status: implemented
    description: |
      From GitHub issue #406 (self-found while dogfooding REQ-154). `rivet
      validate` printed the per-file `skipping <file>: YAML parse error …`
      WARN twice for a single malformed `generic-yaml` source. Root cause:
      `cmd_validate` loads the project once via `ProjectContext::load`
      (load_artifacts -> import_generic_directory -> WARN #1), then re-scans
      every source with `load_artifacts_with_report` for the REQ-075
      duplicate-id / REQ-062 skip pass, which calls `load_artifacts` again ->
      a second `import_generic_directory` -> WARN #2.

      Decision (maintainer, on #406): option A (narrow) — kill the duplicate on
      the validate path only; leave the per-file WARN intact for other
      commands (get/trace/matrix/export still rely on it and aren't covered by
      `warn_parse_error_skips()`).

      Fix: thread a `warn_skips: bool` into `GenericYamlAdapter` /
      `import_generic_directory`; add `load_artifacts_quiet` +
      `load_artifacts_with_report_quiet` (the public `load_artifacts` signature
      is untouched). `cmd_validate`'s re-scan uses the quiet report loader, so
      the WARN fires once (from the initial load) but the skips/duplicates —
      and the hard `artifact-parse-error` / `duplicate-artifact-id` ERROR
      diagnostics derived from them — are unchanged.

      Acceptance:
        - On a fixture with one malformed `generic-yaml` source,
          `rivet validate 2>&1 | grep -c "skipping ./…/reqs.yaml"` returns 1
          (was 2).
        - The hard `artifact-parse-error` ERROR still fires and `Result: FAIL`.
        - `list`/`stats` REQ-154 skip-summary unaffected; rivet repo validate
          still PASS.
        - `cargo test -p rivet-cli --test cli_commands
          validate_emits_single_skip_warn_for_malformed_source` passes.
    tags: [bug, ux, noise, dogfooding, self-found, issue-406]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-062

  - id: REQ-158
    type: requirement
    title: "Detect near-duplicate intent: flag same-type artifacts with highly similar titles"
    status: implemented
    description: |
      From GitHub issue #397: `rivet add` rejects a duplicate *id* but nothing
      flags a duplicate *intent* — two artifacts that say the same thing under
      different ids — so a long-lived backlog accretes near-duplicate
      requirements.

      Signal (bounded + explainable, per the bounded-composite-score doctrine):
      Jaccard overlap of significant title tokens (lowercase, alphanumeric
      split, stopwords + sub-3-char tokens removed), in [0,1]. Threshold 0.6,
      empirically calibrated against the rivet repo's own 158 requirements,
      which produce ZERO pairs at 0.6 (1 at 0.5) — the default does not flood a
      well-differentiated backlog.

      Two surfaces, one shared `rivet_core::similarity` implementation so they
      can't disagree:
        - `rivet validate` emits a `near-duplicate-intent` INFO diagnostic for
          each same-type, non-external artifact pair at/above threshold
          (within-type O(n^2), titles tokenized once). INFO never blocks. Lives
          in the shared `validate_structural*` pass so it runs on both the
          salsa and --direct paths (same-surface parity).
        - `rivet add` emits a non-blocking `note:` when the new title is similar
          to an existing same-type artifact, then adds anyway.

      Acceptance:
        - In a project with REQ-1/REQ-2 (near-identical titles) and REQ-3
          (distinct), `rivet validate --direct --format json` yields exactly
          one `near-duplicate-intent` diagnostic, on REQ-2; REQ-3 is not named.
        - `rivet add` with a title similar to an existing same-type artifact
          prints a `note: intent is N% similar to …` to stderr and still
          succeeds; a distinct title prints no note.
        - The rivet repo's 158 `requirement`s produce zero
          `near-duplicate-intent` diagnostics; across all types the repo
          surfaces only true-positive pairs (e.g. two `feature`s sharing a
          title), all INFO, so `rivet validate` still PASS.
        - `cargo test -p rivet-core --lib similarity` and `cargo test -p
          rivet-cli --test cli_commands near_duplicate_intent_validate_and_add`
          pass.

      Deferred (noted on #397): per-rule `allow:` suppression in rivet.yaml —
      rivet has no general diagnostic-suppression config yet; INFO severity
      keeps the diagnostic non-disruptive until that infra exists.
    tags: [feature, validation, backlog-hygiene, dx, issue-397]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-159
    type: requirement
    title: "Store::iter() order is nondeterministic — provide a deterministic iter_sorted() and document the footgun"
    status: implemented
    description: |
      Self-found while landing REQ-158 (#415). `Store::iter()` returns
      `HashMap::values()`, whose order varies per process. Any caller that
      produces stable output or selects one representative among ties over
      `store.iter()` is silently nondeterministic — which is exactly how the
      REQ-158 `rivet add` near-duplicate note picked REQ-1 locally but REQ-2 in
      CI (same code, different hash seed), making the proptest job flaky on main
      until #414 added an explicit tie-break.

      Minimal, non-breaking hardening: document on `iter()` that order is
      unspecified and add `Store::iter_sorted()` (ascending by id) so the
      deterministic path is the easy one. Migrating existing
      `store.iter().collect::<Vec>()` output sites and the larger "back the
      store with an ordered map" option are deferred to #415 (each needs a
      per-site audit; some already sort downstream).

      Acceptance:
        - `Store::iter_sorted()` yields artifacts ascending by `id` regardless
          of insertion order.
        - `iter()`'s doc-comment states the order is nondeterministic and
          points at `iter_sorted()`.
        - `cargo test -p rivet-core --lib iter_sorted_is_deterministic_by_id`
          passes; `rivet validate` on the rivet repo still PASS.
    tags: [refactor, determinism, footgun, dogfooding, self-found, issue-415]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-158

  - id: REQ-160
    type: requirement
    title: "Make query/export/migrate output deterministic by sorting the Store::iter() consumers"
    status: implemented
    description: |
      Follow-up to REQ-159 (#415): migrate the `store.iter().collect()` sites
      that produce ordered, user-facing output so they are reproducible across
      runs (the store is HashMap-ordered).

      Audited every site. Order-irrelevant ones (collected into `HashSet<id>`
      membership sets) were left as-is; `render/externals.rs` already sorted.
      The output-producing sites fixed:
        - `query::execute` now returns results ascending by `id` — this is the
          shared result set behind `rivet list` (whose default path did NOT
          sort), the MCP query tool, and `{{query:…}}` embeds, so all of them
          become deterministic at one chokepoint.
        - `cmd_export` (ReqIF/etc.) and `cmd_export_zola` sort by id before
          emitting, so exports are byte-reproducible.
        - `rivet schema migrate` sorts artifacts before computing the rewrite
          map so the generated migration is reproducible.

      Acceptance:
        - `rivet list --format json` produces identical id order across runs.
        - `cargo test -p rivet-core --lib execute_returns_results_sorted_by_id`
          passes; export_reqif_roundtrip / export_zola / migrate_integration
          suites still pass.
        - `rivet validate` on the rivet repo still PASS.
    tags: [refactor, determinism, reproducibility, issue-415]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-159

  - id: REQ-161
    type: requirement
    title: "rivet validate --structural: gate on graph/parse/type integrity only, separate from coverage/lint"
    status: implemented
    description: |
      From issue #408 (and the recurring #353/#355 ask): `rivet validate`'s
      FAIL/PASS lumps structural integrity (broken graph) together with
      coverage/lint findings (incomplete/non-compliant), so a bulk status-flip
      or edit can't tell "did I break the graph?" from "the project is still
      incomplete". spar and sigil both hand-rolled a `0 broken cross-refs` gate
      for exactly this.

      Add `Diagnostic::is_structural()` — an explicit allowlist of the
      structural rule ids: artifact-parse-error, duplicate-artifact-id,
      known-type, unknown-link-type, link-target-type, cardinality, broken-link,
      doc-broken-ref, yaml-type-coercion, conditional-rule-consistency,
      coverage-rule-consistency. Everything else — including required-field,
      unknown-field, status-allowed-values, prose-mention, near-duplicate-intent,
      orphan, and all schema-defined coverage/status-gate rules — is coverage/
      lint. (The three borderline rules required-field / unknown-field /
      status-allowed-values are classified coverage by design: an
      incomplete/extra/typo'd field doesn't break the graph.)

      `rivet validate --structural` retains only structural diagnostics before
      counting/display, so the shown set, counts, and PASS/FAIL exit all reflect
      structural-only. The classification is a method on Diagnostic (no rename
      of validate_structural*, per the #408 option-b).

      Acceptance:
        - `Diagnostic::is_structural()` returns true for exactly the structural
          rule ids; a unit test enumerates every built-in rule and asserts its
          class.
        - On a project whose only error is a broken link, `rivet validate
          --structural` FAILs and shows the broken-link diagnostic; on a project
          whose only issues are coverage/lint, `--structural` PASSes.
        - `rivet validate --structural` on the rivet repo PASSes with 0
          diagnostics shown (its 200+ warnings are all coverage/lint).
        - Unit test `is_structural_classifies_every_builtin_rule` + CLI test
          `validate_structural_gates_on_structural_only` pass.

      Deferred: a per-rule `allow:` config and any rename of the
      `validate_structural*` functions (their name is unrelated to this gate).
    tags: [feature, validation, gating, dx, issue-408, issue-353, issue-355]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-162
    type: requirement
    title: "Declare the canonical status lifecycle in common.yaml so typo'd statuses fail validation"
    status: implemented
    description: |
      From #352/#355 Finding 1: the `status` base-field had no `allowed-values`,
      so a typo'd or off-vocabulary status (`implmented`, `acitve`) passed
      `rivet validate` silently and the valid set wasn't discoverable. The
      enforcement mechanism (REQ-135 — `status-allowed-values`) shipped but was
      inert until a set was declared.

      Maintainer decision (this session): declare the canonical lifecycle in
      the shared `schemas/common.yaml` `status` base-field —
      `[draft, proposed, approved, implemented, verified, released,
      deprecated, rejected]`. Ordering for promotion gates: draft -> proposed -> approved
      -> implemented -> verified -> released; deprecated/rejected terminal.

      Migrated the rivet repo's own drift to fit the set (the only 7 artifacts
      off-vocabulary): `accepted`/`active` design-decisions -> `approved`
      (a decision in force); the `active` feature FEAT-066 and `partial`
      component ARCH-ADAPT-WASM -> `implemented` (both exist in code). `proposed`
      was already in the set. No-status artifacts are unaffected (the check only
      fires when a status is present).

      Acceptance:
        - `schemas/common.yaml` `status` base-field declares the 8-value canonical set (incl. `released`).
        - An artifact with a typo'd status (`implmented`) fails `rivet validate`
          with a `status-allowed-values` ERROR + remediation; an artifact with
          no status does not.
        - `rivet validate` on the rivet repo PASSes (drift migrated; 0
          `status-allowed-values` errors).

      Deferred (separate, bigger): #355 Finding 2 — the ASPICE promotion gates
      hardcode `(= status "approved")` so approved -> implemented mis-fires;
      fixing it needs a `status-at-least` predicate over the ordering declared
      here. Tracked on #355.
    tags: [schema, status-lifecycle, validation, issue-352, issue-355, maintainer-decision]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-135

  - id: REQ-163
    type: requirement
    title: "Compliance CI must forward the version switcher + back-link inputs to the report action"
    status: implemented
    description: |
      Maintainer reported (#420) that the published compliance report for
      0.15.0 lost the version selector and the "back" navigation that 0.1.0
      has. Root cause traced: `rivet export --format html` still supports
      `--homepage` (back-link), `--versions` (switcher), and `--version-label`,
      and the compliance action exposes them as `homepage` / `other-versions` /
      `report-label`. But the CI never supplied them:
        - the reusable `compliance.yml` workflow passed `version:` (not an
          action input — silently ignored; the action's label input is
          `report-label`) and never exposed `other-versions`, so any caller
          using it could not produce a switcher;
        - `release.yml`'s compliance step passed no `homepage`/`report-label`.

      Fix (this repo's part): `compliance.yml` now maps `version ->
      report-label` and adds an `other-versions` passthrough input; `release.yml`
      labels the report with the release tag and sets a default `homepage`.
      The cross-version switcher inherently needs the full published-version
      list, which only the pulseengine.eu site-publishing pipeline knows — that
      remains the root-cause fix for the live regression (tracked on #420, plus
      the expired site TLS cert).

      Acceptance:
        - `compliance.yml` exposes an `other-versions` input and passes
          `report-label` + `other-versions` to the action.
        - `release.yml` passes `report-label` + `homepage` to the compliance
          action.
        - Both workflow files parse as valid YAML.
    tags: [ci, compliance, reporting, dx, issue-420, maintainer-reported]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-090

  - id: REQ-164
    type: requirement
    title: "Reduce parallel-PR merge conflicts in append-only ledgers (CHANGELOG union driver)"
    status: implemented
    description: |
      Self-found while landing several one-REQ-per-PR changes in one session
      (#422): every PR conflicts in CHANGELOG.md (entries at the top of
      `[Unreleased]`) and artifacts/requirements.yaml, forcing a manual
      rebase+resolve each time.

      Add `.gitattributes` `CHANGELOG.md merge=union` so git's built-in union
      driver keeps both sides' added lines instead of conflicting — safe for an
      append-only prose log. Deliberately NOT applied to artifacts/*.yaml:
      union-concatenating conflicting YAML hunks can silently produce malformed
      or misordered YAML, which is worse than a conflict that forces review;
      the guidance there is to append REQs at EOF (as this very artifact does)
      or use `rivet add`, never insert mid-file.

      Acceptance:
        - `.gitattributes` declares `CHANGELOG.md merge=union`.
        - This REQ was appended at end-of-file (not inserted mid-file).
        - `rivet validate` still PASSES.
    tags: [chore, dx, git, dogfooding, self-found, issue-422]
    fields:
      priority: should
      category: process
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-165
    type: requirement
    title: "ASPICE verification gates must accept a target that moved forward past approved"
    status: implemented
    description: |
      #355 Finding 2: the three ASPICE verification status-gates
      (V-sys/sw/unit-verification-needs-approved-*) hardcode
      `(forall-linked "verifies" (= status "approved"))` in the consequent, so
      a verifier that is approved/released mis-fires when the requirement or
      design it verifies has moved FORWARD past approved (implemented /
      verified / released). The antecedent already recognises
      `(or approved released)`, but the consequent did not — promoting a
      verified `sw-detail-design` from approved -> implemented wrongly
      re-raised "needs an approved design".

      Fix (schema-only): expand each consequent to accept approved-or-beyond —
      `(or (= status "approved") (= status "implemented")
           (= status "verified") (= status "released"))` — matching the
      canonical lifecycle ordering (the status base-field set). A target still BELOW approved
      (draft / proposed) correctly still fires. No s-expr engine change; a
      `status-at-least` predicate over the ordering remains a possible future
      simplification.

      Acceptance:
        - An approved `sys-verification` that verifies an `implemented`
          system-req does NOT raise
          `V-sys-verification-needs-approved-req`; one that verifies a `draft`
          system-req still does.
        - `cargo test -p rivet-core --test schema_validation_rules
          aspice_status_gate_accepts_forward_target_status` passes, and the
          existing `aspice_status_gate_rules_loaded_and_fire` still passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [schema, aspice, status-lifecycle, bug, issue-355, issue-350]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-135

  - id: REQ-166
    type: requirement
    title: "rivet matrix infers traversal direction (and link) from the from/to pair when --direction is omitted"
    status: implemented
    description: |
      From #402: `rivet matrix --from X --to Y` required the user to also know
      the right `--direction` (the default `backward` silently rendered an
      all-empty matrix for a forward relationship like
      `design-decision --satisfies--> requirement`). REQ-152 added a hint, but
      the user still had to re-run.

      Make `--direction` optional. When omitted, infer the direction + link
      type that actually connect `from` -> `to` by probing the graph (the
      declared source-/target-types on common links like `satisfies` are
      usually empty, so metadata alone can't decide). Candidate links: the one
      given via `--link`, else every distinct link type on a from-/to-type
      artifact; each is scored in both directions and the highest-coverage
      non-empty pair wins (ties break Forward-first then link name, for
      determinism). When `--direction` IS given, behaviour is byte-identical to
      before — inference only affects the omitted path.

      Scope: the CLI `rivet matrix` command. The `{{matrix:from:to}}` embed and
      the serve render path derive direction from schema rules and are left as
      a separate follow-up.

      Acceptance:
        - `rivet matrix --from design-decision --to requirement` (no
          --direction) is NON-empty and reports `link_type: satisfies`
          (inferred forward), not the old empty backward default.
        - `rivet matrix --direction backward …` and `--direction forward …`
          produce byte-identical output to before (explicit path unchanged).
        - `cargo test -p rivet-cli --test cli_commands
          matrix_infers_direction_when_omitted` passes, and the existing
          matrix tests (incl. the empty-hint test, now using explicit
          `--direction backward`) still pass.
        - `rivet validate` on the rivet repo still PASS.
    tags: [feature, matrix, dx, traceability, issue-402]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-152

  - id: REQ-167
    type: requirement
    title: "rivet trace <id>: discoverable per-artifact traceability view (+ deterministic link ordering)"
    status: implemented
    description: |
      Self-found while dogfooding (#426): `rivet`'s tagline is "SDLC artifact
      traceability and validation", yet `rivet trace <id>` was an unrecognised
      subcommand. The per-artifact neighbourhood view (rules satisfied/missing,
      incoming + outgoing links, diagnostics) existed only under the
      non-obvious `rivet validate --explain <id>` — nobody runs `validate` to
      navigate traceability.

      Add `rivet trace <id>` as a first-class command that renders that view
      (it calls the same `cmd_explain`; `validate --explain` stays as an alias).

      While verifying, found the view itself was nondeterministic: incoming
      links (`graph.backlinks_to`, HashMap order) and the rule-satisfier
      representative ("...from <X>") varied per run — bad for audit evidence
      (#415 footgun). Both now sort by (source, link-type), so `trace` /
      `validate --explain` output is reproducible.

      Acceptance:
        - `rivet trace REQ-001` exits 0 and prints the artifact + "Incoming
          links:"; output is byte-identical to `rivet validate --explain
          REQ-001` and identical across repeated runs.
        - `rivet trace <missing-id>` reports "not found" (clean, not a clap
          error).
        - `cargo test -p rivet-cli --test cli_commands
          trace_command_renders_and_is_deterministic` passes; the existing
          `validate_explain_shows_rule_status` test still passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [feature, dx, traceability, determinism, dogfooding, self-found, issue-426, issue-415]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-125

  - id: REQ-168
    type: requirement
    title: "rivet bundle --incoming: follow backlinks so bundling a sink (requirement) includes what realizes it"
    status: implemented
    description: |
      Self-found while dogfooding (#428). `rivet bundle` builds an artifact's
      link-graph closure for LLM context (#206), but traversed OUTGOING links
      only. A requirement is a graph SINK — everything links TO it (satisfies /
      verifies / allocated-to), it links OUT to nothing — so `rivet bundle
      REQ-001` returned just the bare artifact (count=1), dropping exactly the
      realizing design-decisions / features / tests you want as context.

      Add `bundle_with_graph(store, graph, root, depth, include_incoming)` and a
      `rivet bundle --incoming` flag. With it, each node's backlink sources are
      enqueued too (depth +1). `bundle()` stays outgoing-only (API + MCP
      stability). Backlink sources are visited in sorted order so the bundle is
      reproducible across runs (the backlink store is HashMap-backed; cf. #415).

      Acceptance:
        - `rivet bundle REQ-001` (no flag) is unchanged (just the root for a
          sink); `rivet bundle REQ-001 --incoming` includes the artifacts that
          link to it (count > 1) and is byte-identical across runs.
        - `cargo test -p rivet-core --lib
          incoming_includes_backlink_sources_deterministically` passes; the
          existing bundle tests still pass.
        - `rivet validate` on the rivet repo still PASS.
    tags: [feature, bundle, dx, llm-context, determinism, dogfooding, self-found, issue-428, issue-415]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-169
    type: requirement
    title: "rivet sync refuses to overwrite uncommitted changes in a git external's cached checkout"
    status: implemented
    description: |
      Self-found while triaging a maintainer question about co-developing two
      synced repos at once. `git:` externals are cloned into
      `.rivet/repos/<prefix>` and `rivet sync` re-runs `git fetch` + `git
      checkout <ref>` over that checkout. If you edited files there while
      working in both repos, the checkout silently moves HEAD out from under
      your edits (or fails cryptically) — uncommitted work is lost with no
      warning. `path:` externals are unaffected (they symlink to the live tree;
      cf. REQ-020), which is the supported way to co-develop both repos.

      Add a guard: before fetch+checkout, `is_working_tree_dirty()` checks `git
      status --porcelain` for staged/unstaged changes to TRACKED files
      (untracked `??` entries are preserved by checkout, so they do not block).
      When dirty and `--force` is not given, `sync` aborts non-zero naming the
      prefix and path, and points to commit+push / `--force` / `path:`. A new
      `rivet sync --force` opts out (discard). Loud-fail over silent loss (F2).

      Acceptance:
        - With a `git:` external whose `.rivet/repos/<prefix>` has an
          uncommitted tracked edit, `rivet sync` exits non-zero and the message
          names the prefix and `--force`; `rivet sync --force` proceeds.
        - `cargo test -p rivet-core --lib
          is_working_tree_dirty_detects_tracked_edits_not_untracked` passes; the
          existing externals tests still pass.
        - `rivet validate` on the rivet repo still PASS.
    tags: [externals, sync, cross-repo, safety, loud-fail, dx, dogfooding, self-found]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-020

  - id: REQ-170
    type: requirement
    title: "validate surfaces un-synced externals so cross-repo cycle/backlink checks aren't silently incomplete"
    status: implemented
    description: |
      Follow-up to the multi-repo co-development design (REQ-169). `rivet
      validate` runs `detect_circular_deps` to catch A->B->A link cycles across
      repos, building the graph by reading each external's own `rivet.yaml` from
      `.rivet/repos/<prefix>`. But that read is `if let Ok(...)`: an external
      that is not synced (or whose config won't parse) is silently skipped, so a
      cycle or broken reference routed through it is invisible and validate
      reports a clean "0 circular deps". The acyclicity guarantee is only as
      good as graph completeness, and the incompleteness was silent (violates
      F2: loud-fail over silent-success).

      Add `unresolved_externals(externals, project_dir) -> Vec<String>` (sorted)
      returning the prefixes whose cached `rivet.yaml` cannot be loaded. `rivet
      validate` reports them: a WARNING naming the un-synced prefixes and
      telling the user to run `rivet sync`, plus an `unresolved_externals` array
      in `--format json`. Externals resolvable via a `path:` (the live tree)
      are not flagged. Warning only — does not change PASS/FAIL.

      Acceptance:
        - `rivet validate` on a project with an un-synced `git:` external prints
          a warning containing "INCOMPLETE" and the prefix; `--format json`
          includes that prefix in `unresolved_externals` while `circular_deps`
          is 0.
        - `cargo test -p rivet-core --lib
          unresolved_externals_lists_only_unsynced_repos` passes.
        - `rivet validate` on the rivet repo still PASS (spar resolves via its
          `path:` fixture, so it is not flagged).
    tags: [externals, sync, cross-repo, validate, loud-fail, f2, dogfooding, self-found]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-020

  - id: REQ-171
    type: requirement
    title: "graph E2E tests must bound node count so dataset growth doesn't trip the render budget"
    status: implemented
    description: |
      Self-found while triaging why Playwright E2E was red. Three graph/diagram
      tests fail reproducibly (through the CI retry), and have been red across
      multiple merges — unnoticed because Playwright E2E is a non-required check
      and main runs are concurrency-cancelled, so it never reports a conclusive
      green. Root cause is dataset growth, not a serve regression:

      - `graph.spec.ts` "custom polygon shapes" hit `/graph?types=requirement,
        design-decision&depth=2` with NO `?limit=`. The dogfood dataset grew
        past the default 200-node render budget for that filter, so the view
        returns the "above node budget" placeholder (no SVG) and the polygon
        assertion gets 0. Verified locally: the same URL with `&limit=2000`
        renders the real graph.
      - `diagram-viewer.spec.ts` graph page used `/graph?limit=2000` (the FULL
        ~871-node graph); that page is now heavy enough that `goto`+settle
        exceeded the timeout, failing the `.svg-viewer` toolbar/fullscreen
        checks. A bounded focused subgraph renders the same wrapper fast.

      Fix (test-only): add `&limit=2000` to the polygon test, and point the
      diagram-viewer graph case at a focused subgraph
      (`/graph?focus=REQ-001&depth=1&limit=2000`). Both verified green in a real
      browser locally (7/7 in the affected specs).

      Acceptance:
        - `cd tests/playwright && npx playwright test graph.spec.ts
          diagram-viewer.spec.ts --project=chromium --grep
          "polygon|toolbar present|fullscreen toggles"` passes (was 3 failing).
        - `rivet validate` on the rivet repo still PASS.
    tags: [serve, dashboard, graph, playwright, e2e, dogfooding, self-found, ci-hygiene]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: FEAT-001

  - id: REQ-172
    type: requirement
    title: "serve: filtering/search preserves the active variant scope (don't drop ?variant= on hx-push-url)"
    status: implemented
    description: |
      Maintainer-reported (#430). In the serve dashboard the variant
      `<select id="variant-selector" name="variant">` lives in the context bar,
      OUTSIDE the filter form. The filter controls (search box, type filter,
      per-page, status) issue `hx-get` requests with `hx-include` listing only
      their own form fields and use `hx-push-url`. So filtering/searching a
      variant-scoped view sent NO `variant=` and the pushed URL lost `?variant=`
      — silently resetting the scope, and losing it again on reload.

      Fix: add `#variant-selector` to every filter control's `hx-include`
      (search_input, type_filter, per_page_select, filter_bar form + its inner
      search, and the validate status select), via a shared
      `hx_include_with_variant()` helper. The server already treats an empty
      `variant=` as unscoped, so this is safe when no variant is active, and the
      selector is only in the DOM when a variant model exists. The reverse flow
      (select variant after filtering) was already correct: the change handler
      rewrites only `variant` on the current URL, preserving other params.

      Acceptance:
        - `cd tests/playwright && npx playwright test serve-variant.spec.ts
          --project=chromium` passes, including "filtering a variant-scoped list
          keeps the variant in the URL (#430)".
        - `cargo test -p rivet-cli --bins components` passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [serve, dashboard, variant, htmx, ux, bugfix, maintainer-reported, issue-430]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-109

  - id: REQ-173
    type: requirement
    title: "rivet sync ignores only the external cache (.rivet/repos/), never the whole .rivet/ dir"
    status: implemented
    description: |
      Self-found while dogfooding (#433). `ensure_gitignore` (called by `rivet
      sync`) appended a blanket `.rivet/` to `.gitignore`, and its
      already-present check only matched an exact `.rivet/`/`.rivet` line. But
      rivet itself tracks files under `.rivet/` (e.g. `.rivet/agent-context.md`),
      and the cache it actually writes is the narrower `.rivet/repos/`. So a
      blanket `.rivet/` would silently un-track those committed files on a fresh
      checkout, and it was added even when `.rivet/repos/` was already ignored.

      Fix: append `.rivet/repos/` (the cache `sync` writes), and treat the entry
      as already-present if any of `.rivet/repos/`, `.rivet/repos`, `.rivet/`, or
      `.rivet` is present (so re-running is a no-op and a pre-existing broad
      ignore is respected).

      Acceptance:
        - After `ensure_gitignore` on a fresh dir, `.gitignore` contains
          `.rivet/repos/` and NO bare `.rivet/` line.
        - A `.gitignore` already containing `.rivet/repos/` is left unchanged
          (returns false).
        - `cargo test -p rivet-core --lib ensure_gitignore` passes; `rivet
          validate` on the rivet repo still PASS.
    tags: [externals, sync, gitignore, bugfix, dogfooding, self-found, issue-433]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-020

  - id: REQ-174
    type: requirement
    title: "install-hooks.sh pre-commit also runs cargo fmt --check on staged Rust (catch the most common CI Format failure locally)"
    status: implemented
    description: |
      Self-found while dogfooding (#438). The generated pre-commit hook in
      `scripts/install-hooks.sh` ran only `rivet validate` — not `cargo fmt
      --check` — so AI-agent/contributor commits passed local hooks but failed
      the CI Format job (the single most common first-push failure; cf. #437).
      A separate `scripts/pre-commit` file DID run fmt+clippy but was orphaned
      (the installer never wired it).

      Add a fast `cargo fmt --all -- --check` step to the installed pre-commit
      hook, gated on staged Rust files (`git diff --cached … | grep '\.rs$'`),
      blocking with a clear "run cargo fmt --all" message. Clippy/test stay out
      of the per-commit hook (too slow) — they remain CI's job. Convenience
      only; CI is still the real gate (REQ-051).

      Acceptance:
        - After `scripts/install-hooks.sh`, committing a staged Rust file with a
          formatting violation is blocked by the pre-commit hook (exit 1); a
          clean tree (or a commit with no staged `.rs`) is not blocked.
        - `bash -n scripts/install-hooks.sh` passes; `rivet validate` on the
          rivet repo still PASS.
    tags: [ci, hooks, dx, fmt, dogfooding, self-found, issue-438]
    fields:
      priority: should
      category: process
    links:
      - type: traces-to
        target: REQ-051

  - id: REQ-175
    type: requirement
    title: "serve /graph integration tests retry on status==0 so they don't flap red on unrelated PRs"
    status: implemented
    description: |
      Self-found while triaging a red CI Test job on an artifact/script-only PR
      (#440). `cargo nextest` exit 100 → the JUnit artifact named
      `serve_integration::graph_type_filter_renders_when_under_budget`, which
      asserted `/graph?types=requirement` returns 200 but got status=0 (a
      transient connection drop after the server's health probe passed — a
      transport failure, not a content mismatch). It passed locally; my change
      was shell + YAML only.

      The file already had `fetch_page_with_retry` (15s read timeout + one retry
      on status==0) created for exactly this flake class, but the two /graph
      tests (`graph_focused_view_renders_svg`,
      `graph_type_filter_renders_when_under_budget`) called `fetch_with_timeout`
      directly, bypassing the retry. Switch both to `fetch_page_with_retry`.

      Acceptance:
        - `cargo test -p rivet-cli --test serve_integration graph_` passes.
        - Neither `graph_focused_view_renders_svg` nor
          `graph_type_filter_renders_when_under_budget` calls
          `fetch_with_timeout` directly (both use the status==0 retry wrapper).
        - `rivet validate` on the rivet repo still PASS.
    tags: [serve, test, ci-stability, flake, dogfooding, self-found]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: FEAT-001

  - id: REQ-176
    type: requirement
    title: "rivet schema info reports whether a schema is on-disk (pinned) or embedded (binary-versioned)"
    status: implemented
    description: |
      Self-found triaging #431. `load_schemas_with_fallback` resolves each
      schema as on-disk (`<schemas_dir>/<name>.yaml`) if present, else the
      compiled-in embedded copy — but never reports which source it used. So a
      project relying on builtin schemas gets the embedded copy swapped
      silently when the rivet binary is upgraded, and cross-version diagnostic
      drift ("issues appearing") looks like it came from nowhere.

      `rivet schema info <name>` already prints the version; add a `Source:`
      line to its text output naming on-disk (with path) vs embedded (with a
      hint to vendor under schemas/ to pin). The on-disk-vs-embedded branch is
      already computed inline in the Info command, so this is a local change —
      no loader signature change. JSON output is unchanged (keeps its
      documented shape).

      Acceptance:
        - `rivet --schemas <dir-with-common.yaml> schema info common` text
          output contains `Source: on-disk`; with an empty schemas dir it
          contains `Source: embedded`.
        - `cargo test -p rivet-cli --test cli_commands schema_info` passes
          (incl. the unchanged `schema_info_json`).
        - `rivet validate` on the rivet repo still PASS.
    tags: [schema, embedded, dx, cross-version, dogfooding, self-found, issue-431]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-010

  - id: REQ-177
    type: requirement
    title: "rivet schema sources lists each active schema's resolution (on-disk pinned vs embedded binary-versioned)"
    status: implemented
    description: |
      Completes the #431 surfacing started in REQ-176. `schema info` shows one
      schema's source, but a user wants the whole active set at a glance to
      answer "am I on builtin (embedded, changes on upgrade) or vendored
      (pinned) schemas?". Add `rivet schema sources` (text + `--format json`)
      listing each name in `rivet.yaml`'s `schemas:` with on-disk (path) /
      embedded / missing.

      Backed by a reusable `embedded::schema_sources(names, schemas_dir) ->
      [(name, SchemaSource)]` helper (OnDisk(path) | Embedded | Missing),
      mirroring load_schemas_with_fallback's precedence. The command resolves
      sources WITHOUT loading the schema graph, so it still works (and reports
      `missing`) when a schema can't be loaded — diagnosing that is the point.

      Acceptance:
        - `rivet schema sources` (this repo, schemas vendored) shows every
          schema as `on-disk`.
        - For a project whose schemas aren't vendored, `rivet schema sources
          --format json` succeeds (exit 0) and reports a builtin name as
          `embedded` and an unknown name as `missing`.
        - `cargo test -p rivet-core --lib schema_sources` and `cargo test -p
          rivet-cli --test cli_commands schema_sources_reports_resolution` pass.
        - `rivet validate` on the rivet repo still PASS.
    tags: [schema, embedded, cli, dx, cross-version, dogfooding, self-found, issue-431]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-010

  - id: REQ-178
    type: requirement
    title: "validate --explain names which required backlink source types can attach directly vs only via the chain"
    status: implemented
    description: |
      From #350 (downstream aspice project). A required-backlink rule may list
      source types that cannot actually form that backlink to THIS artifact's
      type. Aspice `sw-req` expects a `verifies` backlink from
      `[sw-verification, unit-verification, sw-integration-verification]`, but
      only `sw-verification.verifies` targets `sw-req`; `unit-verification` and
      `sw-integration-verification` verify design artifacts (`sw-detail-design`).
      `validate --explain` listed all three, sending the user to author an
      impossible direct link (the friction the reporter hit).

      Fix: add `Schema::from_type_can_link(from_type, link, target_type)`
      (rivet-core, checks the per-type link-field target allow-list), and in
      `explain_rule` (rivet-cli) partition the rule's `from_types` by it. Surface
      the directly-linkable type(s) as the primary fix and annotate the rest:
      "... from one of [\"sw-verification\"] (note: [unit-verification,
      sw-integration-verification] can 'verifies' other types, not 'sw-req'
      directly — attach those via the intermediate design artifact)". The
      all-direct case (e.g. requirement satisfied by design-decision/feature) is
      left byte-identical, so existing output is unchanged.

      Acceptance:
        - `rivet validate --explain` on an aspice `sw-req` with a verification
          gap shows `from one of ["sw-verification"]` and the note `not 'sw-req'
          directly` (integration test
          `explain_names_directly_linkable_verification_type`).
        - The plain `requirement` case is unchanged — no spurious note.
        - `cargo test -p rivet-core --lib from_type_can_link` passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [validate, lifecycle, diagnostics, dx, aspice, issue-350]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-004

  - id: REQ-179
    type: requirement
    title: "rivet next-id accepts a positional type/prefix shorthand (rivet next-id requirement)"
    status: implemented
    description: |
      Self-found (#447): when I needed the next free FEAT id I reached for a
      manual grep (and collided, since ids span 9+ files) — not realising
      `rivet next-id` already existed (since #28) and scans the whole store.
      A discoverability miss, but it surfaced a real papercut: the natural
      positional form `rivet next-id feature` was rejected with "unexpected
      argument 'feature'"; you had to know to pass `--type feature`.

      Add an optional positional `TYPE_OR_PREFIX` that is a shorthand for
      `--type` (routed through `prefix_for_type`, whose uppercase fallback also
      makes a bare prefix like `FEAT` work). Explicit `--type`/`--prefix` win if
      given. The no-argument error now lists all three usable forms instead of
      "either --type or --prefix must be specified".

      Acceptance:
        - `rivet next-id requirement` equals `rivet next-id --type requirement`;
          `rivet next-id FEAT` prints a `FEAT-` id; `rivet next-id` with no arg
          exits non-zero with a message naming the positional + flag forms.
        - `cargo test -p rivet-cli --test cli_commands next_id_positional_shorthand`
          passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, dx, mutation, discoverability, dogfooding, self-found, issue-447]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-180
    type: requirement
    title: "generated agent guidance (AGENTS.md) surfaces rivet next-id so agents don't hand-grep for IDs"
    status: implemented
    description: |
      Completes #447. `rivet next-id` existed but wasn't discoverable where an
      agent reaches to create artifacts — the `rivet init --agents` template's
      command table and "Creating Artifacts" snippet only showed `rivet add`, so
      an agent (me, last loop) hand-grepped a single file for the next id and
      collided. Add a `rivet next-id` row to the generated command table and a
      one-line note in the Creating-Artifacts snippet ("IDs span many files;
      use `rivet next-id`"). Regenerating the repo's AGENTS.md/CLAUDE.md also
      resynced their managed sections, which had drifted (518->883 artifacts).

      Acceptance:
        - `rivet init --agents` output (and the repo's AGENTS.md managed block)
          contains a `rivet next-id` command-table row and references it in the
          Creating-Artifacts guidance.
        - `cargo test -p rivet-cli --test init_bootstrap --test init_integration`
          passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [docs, agents, dx, discoverability, dogfooding, self-found, issue-447]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-181
    type: requirement
    title: "zola export escapes backslashes in TOML front matter; gate it with a zola-build smoke CI job"
    status: implemented
    description: |
      Self-found by running #392's candidate gate (scripts/zola-export-smoke.sh)
      locally: `zola build` failed on the exported corpus because an artifact
      description containing a regex (`grep '\.rs$'`, in REQ-174) was emitted
      into a TOML multi-line basic string ("""...""") without escaping the
      backslash. TOML multi-line basic strings process escapes, so a bare `\` is
      an invalid escape and breaks the build — any description with a backslash
      poisons the whole site.

      Fix (cmd_export_zola): escape `\` -> `\\` in the description before the
      existing `"""` handling, mirroring what the title already does. Plus add
      the zola-export-smoke CI job (#392, ubuntu-latest, pinned zola, advisory
      `continue-on-error` to start) so this regression class is caught
      automatically.

      Acceptance:
        - `cargo test -p rivet-cli --test export_zola
          zola_frontmatter_escapes_backslashes_in_description` passes (a `\.rs$`
          description is emitted as `\\.rs$`).
        - `RIVET_BIN=… scripts/zola-export-smoke.sh` exits 0 (real zola build of
          the corpus succeeds).
        - `rivet validate` on the rivet repo still PASS.
    tags: [export, zola, toml, bugfix, ci, dogfooding, self-found, issue-392]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-115

  - id: REQ-182
    type: requirement
    title: "export to a directory for a single-file format gives an actionable error, not a raw OS error"
    status: implemented
    description: |
      Self-found by dogfooding exports. `rivet export --format reqif --output
      <dir>` (and generic-yaml/generic) failed with a cryptic raw OS error
      "writing <path>: Is a directory (os error 21)". These formats write a
      single file (the `--help` documents "file for reqif/generic-yaml,
      directory for html/zola"), so passing a directory is an easy mistake with
      an unhelpful message.

      Fix (cmd_export): before the single-file `fs::write`, if `--output` is an
      existing directory, bail with an actionable message naming the format,
      suggesting a file path (`--output out.reqif`), and pointing at the
      directory formats (html/zola). File-path export is unchanged.

      Acceptance:
        - `rivet export --format reqif --output <existing-dir>` exits non-zero
          with a message containing "is a directory" and "single file", and does
          NOT leak "os error 21".
        - `rivet export --format reqif --output out.reqif` still succeeds.
        - `cargo test -p rivet-cli --test export_reqif_roundtrip
          export_reqif_to_directory_gives_actionable_error` passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [export, cli, dx, error-ux, dogfooding, self-found]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-183
    type: requirement
    title: "reqif export of a title-less artifact stays re-importable (id fallback for LONG-NAME)"
    status: implemented
    description: |
      Self-found dogfooding a reqif round-trip: `rivet export --format reqif`
      then `rivet import-results --format reqif` on the same corpus FAILED —
      "ReqIF SPEC-OBJECT 'CA-CI-1' has neither a ReqIF.Name nor a @LONG-NAME —
      cannot derive the required `title`". Root cause: some artifact types
      legitimately have an empty title (an STPA control-action, identified by
      id; `rivet validate` PASSES with it). The export wrote
      `long_name = a.title` verbatim → an empty LONG-NAME, which rivet's own
      import (REQ-123, deliberately strict on title-less objects) then rejected.
      So rivet produced reqif it couldn't re-import.

      Fix (build_reqif_with_schema): when `a.title` is empty, fall back to the
      artifact id for the SPEC-OBJECT LONG-NAME (a meaningful name — control
      actions ARE identified by id). The import strictness is unchanged.

      Acceptance:
        - `cargo test -p rivet-core --lib test_empty_title_roundtrips_via_id`
          passes (title-less artifact exports with id as LONG-NAME and
          re-imports with title = id).
        - `rivet export --format reqif` of the corpus then `import-results
          --format reqif` of that file succeeds (previously errored on CA-CI-1).
        - `rivet validate` on the rivet repo still PASS.
    tags: [export, import, reqif, roundtrip, interop, bugfix, dogfooding, self-found]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-184
    type: requirement
    title: "rivet import is a visible alias for import-results (the natural inverse of export)"
    status: implemented
    description: |
      Self-found (#453) while dogfooding a reqif round-trip. `rivet export
      --format reqif` exists, so the natural way to bring a file back is `rivet
      import` — but in the default build that errored ("unrecognized subcommand
      'import'"). The capability lives in `import-results` (junit/needs-json/
      reqif). Note: `import` IS a real command but only under the `wasm` feature
      (a distinct custom-WASM-adapter importer), so it's absent from the
      default/common build.

      Add `import` as a visible alias of `import-results`, gated to non-wasm
      builds (`cfg_attr(not(feature = "wasm"), command(visible_alias =
      "import"))`) so it never collides with the wasm-adapter `import` command.

      Acceptance:
        - In the default build, `rivet import --format reqif <file> --output
          <dir>` succeeds (equivalent to `import-results`); `rivet --help` lists
          `import-results` with `[aliases: import]`.
        - `cargo test -p rivet-cli --test export_reqif_roundtrip
          import_alias_works_for_reqif` passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, import, dx, discoverability, dogfooding, self-found, issue-453]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-185
    type: requirement
    title: "LSP/VSIX resolves cross-repo external links (no false broken-link; hover works)"
    status: implemented
    description: |
      User-reported: in the VS Code extension (via the LSP), a `traces-to`
      link to `ulinc-zephyr:SWREQ-AP-007` was flagged "does not exist" and
      hover over the cross-repo reference showed nothing — even though the
      external is synced and `rivet serve` resolves it. Root cause: `cmd_lsp`
      loaded only `config.sources` into the salsa store, never
      `config.externals`. `rivet validate` (ProjectContext::load) and `rivet
      serve` compose externals into the store (prefixed `prefix:ID`), so they
      resolve; the LSP didn't — a diagnostic-consistency gap (#89).

      Fix: in `cmd_lsp`, load the declared externals via `load_all_externals`,
      prefix their ids + same-external link targets (mirroring
      ProjectContext::load), and feed them as a salsa `ExtraArtifactSet` via
      `db.load_extras`. Route every store/diagnostics/graph query through the
      `_with_extras` variants (the designed mechanism, already used for
      adapter-only AADL artifacts). This makes diagnostics, hover, and
      go-to-definition all external-aware, consistent with validate/serve.

      Acceptance:
        - `cargo test -p rivet-cli --test lsp_integration
          lsp_resolves_cross_repo_external_link` passes: the LSP, on a project
          with a path external, does NOT publish a broken-link diagnostic for a
          `prefix:ID` link into that external.
        - The full lsp_integration suite still passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [lsp, vsix, externals, cross-repo, diagnostics, consistency, bugfix, user-reported]
    fields:
      priority: must
      category: functional
    links:
      - type: traces-to
        target: REQ-089

  - id: REQ-186
    type: requirement
    title: "CLI supports a minimal build: validate/list/commit without the serve+MCP+LSP dep tree"
    status: draft
    description: |
      Dogfooding friction: `rivet validate`/`list`/`commit-msg-check` are the
      hook- and CI-critical hot paths, but `rivet-cli` has no minimal build. Its
      `[features]` are only `default = []` and `wasm`, while `axum`,
      `tower-http`, `rmcp` (full streamable-HTTP MCP server), `lsp-server`,
      `lsp-types`, and `notify` are UNCONDITIONAL dependencies. So ANY build from
      source — `cargo install rivet`, CI's compile step, or a local `cargo build`
      to test a validate change — compiles the entire web-server + MCP + LSP tree
      (hundreds of transitive crates), even though validate/list never use it.
      (Note: the pre-commit hook itself uses the already-installed `rivet` on
      PATH via `RIVET_BIN:-rivet`, so the hook does NOT rebuild — the cost is on
      every from-source build, not the hook.) `rivet-core` already gates its
      heavy deps (`wasm`/`reqwest`/`spar-*` are `optional`); the CLI does not.
      See GitHub issue #456 (serve/mcp/lsp feature-gating).

      Fix (additive — published binary unchanged): add `serve`/`mcp`/`lsp` cargo
      features, keep them in `default`, and `#[cfg(feature = ...)]`-gate the
      corresponding command handlers + module declarations in `main.rs`. Then
      `--no-default-features` yields a fast `validate`/`list`/`commit-msg-check`
      build for hooks, CI's validate gate, and the dogfooding loop. The default
      feature set (and thus `cargo install rivet`) is a maintainer call — filed
      draft pending that decision; do not change distribution semantics
      unilaterally.

      Acceptance:
        - `cargo build -p rivet-cli --no-default-features` succeeds and the
          resulting `rivet validate` exits 0 on the rivet repo.
        - `cargo tree -p rivet-cli --no-default-features -i axum` reports axum is
          NOT in the dependency graph (and likewise for `rmcp`).
        - The default build is unchanged: `rivet serve --help` and `rivet mcp
          --help` still succeed under `cargo build -p rivet-cli`.
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, build, features, dogfooding, friction, ci, hooks, dependencies]
    fields:
      priority: should
      category: non-functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-187
    type: requirement
    title: "`rivet query` accepts the s-expression as a positional shorthand (not only --sexpr)"
    status: implemented
    description: |
      Dogfooding friction: `rivet query '(= type "requirement")'` — the natural
      form an agent or human types — errors with "unexpected argument", because
      `query` only accepts the filter via the required `--sexpr` flag. The sibling
      command `next-id` already established the positional-shorthand pattern
      (`rivet next-id requirement` works via an optional positional `target` that
      defers to the explicit flag), but `query` was never given the same
      treatment — an avoidable inconsistency on the single most-used agent-facing
      command.

      Fix: add an optional positional `SEXPR` to the `query` clap struct and make
      `--sexpr` optional; the explicit flag wins if both are given, and a clear
      error is raised if neither is supplied. `--sexpr` keeps working unchanged
      (MCP/docs/scripts depend on it), so the change is purely additive.

      Acceptance:
        - `rivet query '(= id "REQ-007")' --format ids` (positional, no
          `--sexpr`) exits 0 and prints `REQ-007`.
        - The positional form and `--sexpr` form yield identical matches
          (`cargo test -p rivet-cli --test sexpr_filter_integration
          query_positional_and_flag_are_equivalent`).
        - `rivet query` with no filter exits non-zero with an error mentioning
          "s-expression".
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, query, sexpr, ergonomics, agent-ux, friction, consistency]
    fields:
      priority: should
      category: non-functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-188
    type: requirement
    title: "Agent CLI ergonomics: positional link/unlink target + accurate init/modify help"
    status: implemented
    description: |
      Dogfooding pass over agent-facing commands surfaced three friction points,
      all verified against current main:

      1. `rivet link <source> <target> --type <t>` — the natural form (source and
         target as an ordered positional pair) — errored "unexpected argument",
         because `target` was a required `--target` flag. Same friction class as
         the `query` positional gap (REQ-187) and the `next-id` positional. Same
         for `unlink`.
      2. `rivet init --help` showed two duplicated, contradictory `--preset`
         doc lines (one listing do-178c/en-50128, the other iec-61508/iec-62304)
         — clap concatenated them into a stuttering string that also OMITTED 5
         of the 14 presets resolve_preset actually accepts (stpa-ai, eu-ai-act,
         safety-case, iso-pas-8800, sotif).
      3. `rivet modify --help` stuttered: "Modify an existing artifact Modify an
         existing artifact (...)" — a duplicated doc-comment summary line.

      Fix (additive, low-risk): add an optional positional TARGET to link and
      unlink (the --target flag still works and wins if both given; clear error
      if neither). Replace the --preset doc-comment with one accurate line
      listing all 14 presets. Collapse the duplicated modify summary line.
      Follow-up idea (not done here): make --preset a single source of truth via
      a clap value-parser to prevent future drift.

      Acceptance:
        - `cargo test -p rivet-cli --test cli_commands link_positional_target_is_parsed`
          passes (positional link target no longer a clap parse error).
        - `cargo test -p rivet-cli --test cli_commands init_preset_help_lists_all_presets`
          passes (help lists every preset resolve_preset accepts).
        - `cargo test -p rivet-cli --test cli_commands modify_help_has_no_duplicated_summary`
          passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, link, unlink, init, modify, help, agent-ux, ergonomics, friction, docs]
    fields:
      priority: should
      category: non-functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-189
    type: requirement
    title: "`rivet query --format json` carries the `command` envelope field + ships a query-output JSON schema"
    status: implemented
    description: |
      Dogfooding pass on JSON-output consistency: every machine-readable command
      output carries a top-level `command` field (validate, get, list, stats,
      coverage, supplier all emit it; list/stats/coverage/validate even ship a
      `schemas/json/*-output.schema.json` registered in JSON_SCHEMA_REGISTRY and
      surfaced via `rivet schema list-json`/`get-json`). `rivet query --format
      json` — a primary agent-facing command with the same artifact-collection
      shape as `list` — was the lone exception: its envelope was
      `{filter, count, total, truncated, artifacts}` with NO `command` field and
      NO published output schema. An agent (or a JSON-schema consumer) keying off
      `command` finds query anomalous.

      Fix (additive): emit `"command": "query"` in the query JSON envelope; add
      `schemas/json/query-output.schema.json` (mirroring list-output, with
      query's `filter/total/truncated` and array-valued `links`); register it in
      JSON_SCHEMA_REGISTRY so `schema list-json`/`get-json query` work.

      Acceptance:
        - `rivet query --sexpr '(= type "requirement")' --format json | python3 -c
          'import sys,json;assert json.load(sys.stdin)["command"]=="query"'`
          exits 0.
        - `rivet schema get-json query` prints a path to an existing schema file.
        - `rivet schema list-json --format json` includes a "query" entry.
        - `cargo test -p rivet-cli --test cli_commands query_json_output_has_command_envelope`
          passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, query, json, schema, consistency, agent-ux, machine-readable]
    fields:
      priority: should
      category: non-functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-190
    type: requirement
    title: "`execute_sexpr` returns deterministic (id-sorted) matches so query output + --limit truncation are reproducible"
    status: implemented
    description: |
      Verified bug (issue #415): `rivet query --format json` for the same filter
      returns results in a DIFFERENT order on every run (run1: REQ-125,166,147...
      run2: REQ-174,011,117...), because the shared `execute_sexpr` engine
      iterated the HashMap-ordered `store.iter()`. Two consequences:
        - match order is nondeterministic across CLI / MCP `rivet_query` / the
          `{{query:...}}` embed (all three share `execute_sexpr`);
        - with `--limit`, the kept subset is an arbitrary HashMap-order slice, so
          an agent paging or sampling gets a different subset each run — a
          correctness bug, not just cosmetics.
      `execute` (the non-sexpr path) already sorted by id; `execute_sexpr` was
      missed. `Store::iter_sorted()` (REQ-159) already exists for exactly this.

      Fix: iterate `store.iter_sorted()` in `execute_sexpr`, making both the
      match order and the `--limit` truncation deterministic (lowest-id N).

      Acceptance:
        - `cargo test -p rivet-core --lib query::tests::execute_sexpr_returns_matches_sorted_by_id`
          passes.
        - `cargo test -p rivet-core --lib query::tests::execute_sexpr_limit_truncates_to_lowest_ids_deterministically`
          passes (limit keeps the lowest-id subset).
        - `rivet query --sexpr '(= type "requirement")' --format ids` produces
          byte-identical output across two consecutive runs.
        - `rivet validate` on the rivet repo still PASS.
    tags: [query, determinism, reproducibility, mcp, embed, bugfix, agent-ux]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-159

  - id: REQ-191
    type: requirement
    title: "`LinkGraph::orphans` returns id-sorted output so stats/validate/list orphan lists are reproducible"
    status: implemented
    description: |
      Verified bug (issue #415, sibling of REQ-190): `rivet stats --format json`
      produced a DIFFERENT `orphans` array order on every run (run1:
      [spar:SPAR-REQ-001, spar:SPAR-SYS-001, SL-TQ-004, ...]; run2: [SL-AI-003,
      SL-TQ-004, ..., spar:SPAR-REQ-001] — same set, different order). Root
      cause: `LinkGraph::orphans` iterated the HashMap-ordered `store.iter()`
      and collected without sorting. The orphan list feeds `rivet stats`,
      `rivet validate`, and `rivet list` orphan reporting, so all three were
      nondeterministic. (matrix/export/coverage/list-body were already stable.)

      Fix: iterate `store.iter_sorted()` in `LinkGraph::orphans` so the orphan
      list is ascending by id and reproducible across runs.

      Acceptance:
        - `cargo test -p rivet-core --lib links::tests::orphans_are_id_sorted_for_deterministic_output`
          passes.
        - `rivet stats --format json` produces byte-identical output across two
          consecutive runs.
        - `rivet validate` on the rivet repo still PASS.
    tags: [stats, validate, orphans, determinism, reproducibility, bugfix]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-159

  - id: REQ-192
    type: requirement
    title: "`rivet commits --format json` carries the `command` envelope field (consistency with all other JSON commands)"
    status: implemented
    description: |
      Dogfooding JSON-consistency audit (follow-on to REQ-189): every
      machine-readable command output carries a top-level `command` field —
      validate, get, list, stats, coverage, query (REQ-189), matrix, diff,
      impact all do. `rivet commits --format json` was the lone remaining
      exception: its envelope was `{summary, broken_refs, orphans, unimplemented,
      artifact_coverage}` with NO `command` field, so an agent keying off
      `command` to dispatch on output type finds commits anomalous.

      Verified the false-alarm siblings too: `get`/`coverage --format json`
      parse as valid JSON (an earlier "invalid control character" reading was a
      shell-quoting artifact, not a rivet bug), and `commits` output is
      deterministic — so the only real gap is the missing `command` field.

      Fix (additive): emit `"command": "commits"` in `cmd_commits_json`.

      Acceptance:
        - `rivet commits --format json | python3 -c 'import sys,json;
          assert json.load(sys.stdin)["command"]=="commits"'` exits 0.
        - `cargo test -p rivet-cli --test cli_commands commits_json_output_has_command_envelope`
          passes.
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, commits, json, consistency, agent-ux, machine-readable]
    fields:
      priority: should
      category: non-functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-193
    type: requirement
    title: "`{{query:...}}` doc embed renders rows id-sorted so generated docs are reproducible"
    status: implemented
    description: |
      Verified bug (issue #415; sibling of REQ-190): `rivet embed 'query:(= type
      "requirement")'` rendered a DIFFERENT row order on every run. REQ-190 fixed
      the shared `execute_sexpr` engine, but the `{{query:...}}` embed has its
      OWN match loop in `render_query` (rivet-core/src/embed.rs) that iterated
      the HashMap-ordered `ctx.store.iter()` directly — it never went through
      `execute_sexpr`. Consequences:
        - embed table rows render in nondeterministic order, so any document
          containing a `{{query:...}}` embed (compliance reports, zola sites,
          `rivet docs`) produces churny, non-reproducible output across builds;
        - the embed's default row `limit` truncated to an arbitrary HashMap-order
          subset, so an over-limit embed showed a different (and differently
          incomplete) set each render.
      The aggregate embeds (`stats:types`, `stats:status`, etc.) were already
      deterministic — they accumulate into a `BTreeMap`.

      Fix: iterate `ctx.store.iter_sorted()` in `render_query`, making both row
      order and limit truncation deterministic (lowest-id N).

      Acceptance:
        - `cargo test -p rivet-core --lib embed::tests::query_embed_renders_rows_id_sorted_for_deterministic_docs`
          passes.
        - `rivet embed 'query:(= type "requirement")'` produces byte-identical
          output across two consecutive runs.
        - `rivet validate` on the rivet repo still PASS.
    tags: [embed, docs, query, determinism, reproducibility, bugfix]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-159

  - id: REQ-194
    type: requirement
    title: "`rivet snapshot diff` accepts the baseline path as a positional shorthand (not only --baseline)"
    status: implemented
    description: |
      Dogfooding friction: `rivet snapshot diff <baseline.json>` — the natural
      form (mirroring `git diff <ref>`, `diff <file>`) — errored with "unexpected
      argument", because `snapshot diff` only accepted the baseline via the
      `--baseline` flag. Same friction class as the `query` (REQ-187), `link`
      (REQ-188), and `next-id` positional shorthands. An agent capturing then
      diffing a snapshot naturally writes `snapshot diff <path>`.

      Fix (additive): add an optional positional `BASELINE` to `snapshot diff`;
      the `--baseline` flag still works and wins if both are given. When neither
      is supplied, the existing auto-detect (latest snapshot under `snapshots/`)
      is preserved.

      Acceptance:
        - `cargo test -p rivet-cli --test cli_commands snapshot_diff_accepts_positional_baseline`
          passes (capture a snapshot, then `snapshot diff <path>` positionally
          exits 0 with no clap parse error).
        - `rivet snapshot diff --baseline <path>` still works unchanged.
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, snapshot, diff, ergonomics, agent-ux, friction, consistency]
    fields:
      priority: should
      category: non-functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-195
    type: requirement
    title: "`variant` subcommands accept a bare `--variant <name>` (resolve from artifacts/variants/), consistent with `query`"
    status: implemented
    description: |
      Dogfooding friction (issue #466, verified): `--variant` accepted a bare
      NAME in `query` (resolved from `artifacts/variants/<name>.yaml`, issue
      #287) but required a PATH in the `variant` subcommands — so an agent that
      learned `query --variant dashboard-only` hit a confusing
      `reading dashboard-only: No such file` on `variant solve/explain/...`,
      even though the SAME file (`artifacts/variants/dashboard-only.yaml`) drives
      both.

      Fix (additive): route the `--variant` arg of `variant
      check/solve/features/value/attr/explain/manifest` through the existing
      `resolve_variant_arg(project, arg)` helper at the dispatch layer (it
      resolves a direct path first, then `artifacts/variants/<name>.yaml`, then
      errors with the available names). Handlers are unchanged; a direct path
      keeps working.

      Acceptance:
        - `cargo test -p rivet-cli --test cli_commands variant_solve_accepts_bare_variant_name`
          passes (bare name resolves; direct path still works).
        - `rivet variant solve --model artifacts/feature-model.yaml --variant
          dashboard-only` exits 0 (no "No such file" error).
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, variant, ergonomics, agent-ux, consistency, friction]
    fields:
      priority: should
      category: non-functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-196
    type: requirement
    title: "Static HTML export shows an honest AADL-diagram fallback (no perpetual 'Loading…')"
    status: implemented
    description: |
      User-reported: AADL architecture diagrams in the published compliance
      report (e.g. .../0.15.0/compliance/documents/ARCH-001.html) are stuck on
      "Loading AADL diagram..." forever. Root cause: `document::render_to_html`
      emits a `<div class="aadl-diagram"><p class="aadl-loading">Loading AADL
      diagram...</p></div>` placeholder that is filled at runtime ONLY by
      `rivet serve`'s `initAadlDiagrams` JS (rivet-cli/src/serve/js.rs), which
      fetches server-only routes (`/source-raw/arch`, `/wasm/spar_wasm.js`) and a
      spar WASM renderer. A static export bundles neither that JS nor those
      routes, so the placeholder never updates — and even the JS's paths are
      absolute-from-domain-root, wrong for a report served at a subpath.

      This REQ covers the immediate, honest fix: in the export path
      (`render_document_body_for_export`), rewrite the perpetual "Loading AADL
      diagram..." into a static note pointing to the interactive `rivet serve`
      view, so the report no longer looks hung. Actually inlining the rendered
      SVG at export time (server-side via the spar wasm_runtime) is the larger
      follow-up tracked in a separate issue.

      Acceptance:
        - `cargo test -p rivet-core --lib export::tests::static_export_replaces_perpetual_aadl_loading_with_honest_note`
          passes.
        - A document with an ```aadl block, exported via `rivet export --format
          html`, contains no "Loading AADL diagram..." text and instead points
          to `rivet serve`.
        - `rivet validate` on the rivet repo still PASS.
    tags: [export, html, aadl, compliance-report, docs, ux, user-reported, bugfix]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-105

  - id: REQ-197
    type: requirement
    title: "Honest AADL/WASM-unavailable guidance — stop pointing users at broken/unpublished tooling"
    status: implemented
    description: |
      Follow-on to the user-reported AADL bug (#468): when the spar WASM isn't
      bundled, rivet pointed users at tooling that does not work for them. The
      serve dashboard fallback said "run ./scripts/build-wasm.sh and rebuild"
      (that script requires a local spar checkout + wasm32-wasip2 + Node/jco),
      and `rivet-cli/assets/wasm/README.md` advertised `./scripts/fetch-wasm.sh`
      under "Downloading from GitHub releases" — but spar publishes NO WASM
      release asset (pulseengine/spar#259), so fetch-wasm.sh always fails. Both
      sent users (incl. the reporter) down dead ends.

      Fix (docs + messaging, no behaviour change): the serve `AADL_JS` fallback
      messages now state the WASM is "not bundled in this build" and reference
      the tracking issue (#468) instead of the broken script; the README
      honestly describes the current state (stub fallback, fetch-wasm.sh blocked
      on spar#259, build-from-source requires a local spar checkout + toolchain).

      Acceptance:
        - `cargo test -p rivet-cli --bin rivet serve::js::tests::aadl_wasm_fallback_messages_are_honest`
          passes (fallback no longer instructs running build-wasm.sh; states
          "not bundled in this build"; references the tracking issue).
        - `grep -q 'spar does .*not.*publish' rivet-cli/assets/wasm/README.md`
          (README states the current blocked status).
        - `rivet validate` on the rivet repo still PASS.
    tags: [serve, aadl, wasm, docs, ux, user-reported, dx]
    fields:
      priority: should
      category: non-functional
    links:
      - type: traces-to
        target: REQ-007

  - id: REQ-198
    type: requirement
    title: "`rivet add` must emit valid YAML for multi-line/special-char fields (data-integrity bug)"
    status: implemented
    description: |
      Verified data-corruption bug (current main): `rivet add --type requirement
      --title T --description "Line one.<newline>Line two."` writes INVALID YAML
      and breaks the whole artifacts file. `render_artifact_yaml`
      (rivet-core/src/mutate.rs) was a naive serializer:
        - `description: >` folded scalar with only the FIRST line indented — a
          newline put line 2 at column 0 → "could not find expected ':'" parse
          error, so `rivet validate` then FAILs to load the file (2 errors).
        - `title: {raw}` / tag / field values emitted unquoted — a colon (e.g.
          "Multi word: with colon") also produced invalid YAML.
      The `modify`/`set_field` path already serializes safely via
      `yaml_edit::yaml_render_scalar_value`; only the `add` path diverged.

      Fix: route `add`'s title/status/description/tags/field values through the
      same `yaml_edit` safe emitters (`yaml_quote_inline_scalar` +
      `yaml_render_scalar_value`), so newlines become block-literal `|` scalars
      with correct indentation and special chars are quoted.

      Acceptance:
        - `cargo test -p rivet-core --lib mutate::tests::render_artifact_yaml_multiline_and_colon_is_valid_parseable_yaml`
          passes (rendered YAML parses; multi-line description round-trips).
        - In a temp project, `rivet add --type requirement --title X
          --description $'a\nb'` then `rivet validate` → PASS (was FAIL).
        - `rivet validate` on the rivet repo still PASS.
    tags: [mutate, add, yaml, data-integrity, serialization, bugfix, agent-ux]
    fields:
      priority: must
      category: functional
    links:
      - type: traces-to
        target: REQ-031

  - id: REQ-199
    type: requirement
    title: "`rivet add`/batch/MCP serialize nested (list/map) field values as structures, not block-scalar strings"
    status: implemented
    description: |
      Follow-on to REQ-198 (found while verifying its comprehensiveness): in
      `render_artifact_yaml` (rivet-core/src/mutate.rs), a non-scalar field value
      (a sequence or mapping — reachable via `rivet batch` or the MCP add tool,
      where field values aren't restricted to CLI `--field key=value` strings)
      hit the `other =>` arm, which `serde_yaml::to_string`-ed the structure
      (multi-line) and then — after REQ-198 — wrapped it in
      `yaml_render_scalar_value`, collapsing the list/map into a block-literal
      STRING (`owners: |-\n  - alice\n  - bob`). Pre-REQ-198 it was instead
      INVALID YAML (unindented continuation); neither preserved the structure.

      Fix: emit nested field values as properly-indented YAML under the key
      (serde produces valid YAML; shift it right by the field-content indent), so
      a list field round-trips as a list and a map as a map.

      Acceptance:
        - `cargo test -p rivet-core --lib mutate::tests::render_artifact_yaml_nested_field_value_stays_structured`
          passes (a sequence field round-trips as a sequence, not a string).
        - `rivet validate` on the rivet repo still PASS.
    tags: [mutate, add, batch, mcp, yaml, data-integrity, serialization, bugfix]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-198

  - id: REQ-200
    type: requirement
    title: "`rivet export --format html` shows honest AADL fallback (REQ-196 patched the wrong path — bug still live)"
    status: implemented
    description: |
      Verified by dogfooding the real export (build + `rivet export --format
      html` + scan): the multi-page HTML export STILL ships the perpetual
      `<p class="aadl-loading">Loading AADL diagram...</p>` placeholder in 2 of
      987 pages — `documents/ARCH-001.html` (the exact #468 page) and
      `artifacts/ARCH-SYS-001.html` (an aadl-component artifact). Neither has the
      honest fallback.

      Root cause: REQ-196 patched `static_aadl_fallback` inside
      `rivet_core::export::render_document_body_for_export`, but `rivet export
      --format html` is implemented by `cmd_export_html` (rivet-cli), which
      renders pages via the rivet-cli `render/` serve renderers and wraps them in
      a `wrap_page` layout — it never calls the rivet-core function REQ-196
      fixed. So REQ-196 was effectively a no-op for the real export (and the
      published compliance report), even though it was marked implemented (on a
      unit test of the helper, not an e2e export check).

      Fix: apply the same placeholder→honest-note replacement in `wrap_page`, the
      universal per-page wrapper in `cmd_export_html`, so every exported page is
      covered.

      Acceptance:
        - `rivet export --format html` produces ZERO pages containing
          `<p class="aadl-loading">Loading AADL diagram...</p>`.
        - The honest "rivet serve dashboard" note appears on pages that had an
          AADL diagram.
        - `rivet validate` on the rivet repo still PASS.
    tags: [export, html, aadl, compliance-report, user-reported, bugfix, regression]
    fields:
      priority: must
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-196

  - id: REQ-201
    type: requirement
    title: "`rivet export --format html --single-page` must not hang on real-sized projects (node-budget the graph render)"
    status: implemented
    description: |
      Verified hang: on the rivet repo (~900 artifacts), `rivet export --format
      html --single-page` runs for minutes producing nothing (multi-page export
      finishes in ~6s). Root cause: `render_section_graph`
      (rivet-core/src/export.rs) lays out the ENTIRE link graph eagerly via
      `etch::layout::layout(pg, ...)` with NO node budget; etch's layout is
      super-linear, so ~900 nodes makes the single-page export hang. The serve
      path already guards this (`rivet-cli/src/render/graph.rs`
      `DEFAULT_NODE_BUDGET = 200`, with a "Graph above node budget" page) — the
      static export did not.

      Fix: in `render_section_graph`, if `node_count > 200`, skip the layout and
      emit a short note ("Graph has N nodes, exceeding the static-render budget —
      view it interactively in `rivet serve`"), mirroring the serve guard.

      Acceptance:
        - `cargo test -p rivet-core --lib export::tests::render_section_graph_over_budget_skips_layout`
          passes (a graph with >200 nodes returns the budget note, not a layout).
        - `rivet export --format html --single-page` on the rivet repo completes
          in seconds (was: minutes / hang).
        - `rivet validate` on the rivet repo still PASS.
    tags: [export, html, single-page, graph, performance, hang, bugfix]
    fields:
      priority: must
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-007
  - id: REQ-202
    type: requirement
    title: "rivet-cli must support a minimal `--no-default-features` build (validate/list/commit-msg-check) that excludes the serve+MCP+LSP stack"
    status: draft
    description: |
      Finding (issue #456, AI-agent dogfooding, verified against
      rivet-cli/Cargo.toml). `rivet validate` is the hook-critical and
      CI-critical hot path, yet rivet-cli has NO way to build a minimal binary:
      `[features]` is `default = []` (only `wasm` is non-trivial), and `axum`,
      `tower-http`, `notify` (serve), `lsp-server`/`lsp-types` (LSP), and `rmcp`
      (full streamable-HTTP MCP server) are UNCONDITIONAL dependencies. So every
      from-source build — `cargo install rivet`, CI's compile step, and a local
      build to test a one-line validate change — compiles the entire web/MCP/LSP
      tree even though validate/list/commit-msg-check never use it. rivet-core
      already gates its heavy deps (`wasm`, `reqwest`, `spar-*` are
      `optional = true`); the coupling is specific to rivet-cli.

      Fix (additive, preserves published behavior): introduce cargo features and
      KEEP them in `default`, so the released binary is byte-for-byte unchanged:
        default = ["serve", "mcp", "lsp"]
        serve = ["dep:axum", "dep:tower-http", "dep:notify"]
        mcp   = ["dep:rmcp", "dep:tokio"]
        lsp   = ["dep:lsp-server", "dep:lsp-types"]
      …with `#[cfg(feature = ...)]` gating the corresponding command handlers and
      `mod serve`/`mod mcp`/`mod lsp` declarations in main.rs. The code-side
      gating across the serve/mcp/lsp command dispatch is the bulk of the work
      and needs care so `--no-default-features` still compiles cleanly. The
      default-feature set is a maintainer call (filed as draft).

      Acceptance:
        - `rivet-cli/Cargo.toml` declares `serve`/`mcp`/`lsp` features, all in
          `default`, with the corresponding deps marked `optional = true`
          (`dep:` syntax).
        - `cargo build -p rivet-cli --no-default-features` succeeds and the
          resulting binary runs `validate`, `list`, and `commit-msg-check`.
        - `cargo build -p rivet-cli --no-default-features` does NOT compile
          `axum`, `rmcp`, or `lsp-server` (verify via
          `cargo tree -p rivet-cli --no-default-features` absence).
        - Default build (`cargo build -p rivet-cli`) is unchanged: serve, mcp,
          and lsp subcommands all present.
        - `rivet validate` on the rivet repo still PASS.
    tags: [cli, build, cargo-features, minimal-build, friction, dogfooding]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-007
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-05T09:23:14Z
  - id: REQ-203
    type: requirement
    title: "`rivet add` must stamp AI provenance at creation (--created-by/--model + RIVET_PROVENANCE_CREATED_BY), and the add-path serializer must emit the provenance block"
    status: implemented
    description: |
      Finding (issue #476, AI-agent dogfooding). The AI-provenance system was
      wired to the editor, not to rivet's mutation commands, so the SANCTIONED
      creation path produced UNSTAMPED artifacts: `cmd_add`
      (rivet-cli/src/main.rs) built the `Artifact` with `provenance: None`, and
      the `.claude/settings.json` stamp hook only matches Edit/Write — a heredoc
      append or `rivet add` (both Bash calls) never fired it. For a tool whose
      pitch is AI-artifact auditability, AI-created artifacts silently missing
      provenance is a correctness gap.

      Compounding (caught during implementation): `mutate::render_artifact_yaml`
      — the single serializer for the add path — did NOT emit the `provenance`
      block at all, so merely setting `artifact.provenance` would have been a
      silent no-op (the REQ-196/200 class of bug). Both halves are required.

      Fix:
        - Add `--created-by <human|ai|ai-assisted>` and `--model <M>` to
          `rivet add`; `--created-by` falls back to the
          `RIVET_PROVENANCE_CREATED_BY` env var so CI/agent environments can set
          it once. Value validated up front (before project load). When absent,
          no provenance block is written (prior behavior preserved).
        - Extract `current_utc_timestamp()` (shared by cmd_stamp and cmd_add) so
          both surfaces emit identical ISO-8601 timestamps with no chrono dep.
        - Teach `render_artifact_yaml` to emit the `provenance:` block (field
          order matches yaml_edit::set_provenance: created-by, model, session-id,
          timestamp, reviewed-by).

      Acceptance:
        - `cargo test -p rivet-core --lib render_artifact_yaml_` passes
          (emits-when-present + omits-when-absent).
        - `rivet add -t requirement --title T --created-by ai-assisted --model M`
          writes a `provenance:` block with created-by/model/timestamp; a plain
          `rivet add` writes none; `RIVET_PROVENANCE_CREATED_BY=ai rivet add`
          stamps from the env; `--created-by robot` errors.
        - `rivet validate` PASS on the resulting project.
    tags: [cli, provenance, ai-provenance, rivet-add, friction, dogfooding, bugfix]
    fields:
      priority: should
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-007
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-05T09:38:15Z
  - id: REQ-204
    type: requirement
    title: "Remaining nondeterministic store.iter() ordered-output sites must sort by id (serve /api/search, LSP id-completion)"
    status: implemented
    description: |
      Follow-on audit of issue #415 (Store::iter() is HashMap-ordered). REQ-159
      already added `Store::iter_sorted()` + the doc warning and migrated the
      clear-cut sites (query.rs::execute, migrate_cmd, render/externals). This
      audits the remaining `store.iter()` ordered-output candidates the reporter
      flagged and fixes the two that genuinely regress:

        1. serve `/api/search` (rivet-cli/src/main.rs): pushed artifact + doc
           matches in HashMap order, then `results.truncate(50)`. So with >50
           matches, WHICH 50 survive — not just their order — varied per process.
           Fix: sort `results` by the `id` field before `truncate(50)`.
        2. LSP id-completion (`lsp_completion`, the `[[`/`target:` branch):
           pushed CompletionItems in HashMap order while the sibling `type:`
           branch already sorted. Fix: extract `artifact_id_completions(store)`
           that builds the list via `iter_sorted()` (ascending id), matching the
           sibling branch.

      Audited and confirmed already-safe (no change needed): query.rs::execute,
      migrate_cmd.rs (iter_sorted), render/externals.rs (sorts), export filter
      (sorts), serve tree/search-expand (BTreeMap / sort), validate & coverage
      filter-rebuilds (set membership), HashSet id collects, aggregation loops.

      Acceptance:
        - `cargo test -p rivet-cli --bin rivet lsp_tests::artifact_id_completions_are_sorted_by_id`
          passes (ids inserted out of order come back ascending).
        - `rg "results.sort_by" rivet-cli/src/main.rs` shows the search results
          sorted before `truncate(50)`.
        - `rivet validate` PASS.
    tags: [determinism, store, lsp, serve, search, bugfix, footgun, dogfooding]
    fields:
      priority: should
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-159
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-05T10:20:52Z
  - id: REQ-205
    type: requirement
    title: "`rivet validate` must surface the schema set it used (name@version + embedded/on-disk source) so upgrade-induced validation changes are visible"
    status: implemented
    description: |
      Finding (issue #431). A project using a builtin schema (no local
      `schemas/` dir) validates against whatever schema copy is compiled into
      the rivet binary: `embedded::load_schema_contents` prefers an on-disk
      `<schemas_dir>/<name>.yaml`, else falls back to the embedded copy. So every
      release that tightens or adds rules to a builtin schema silently changes
      validation semantics for embedded-schema consumers — `rivet validate` that
      passed before now flags unchanged artifacts, with nothing indicating the
      schema changed. Schemas carry a cosmetic `version:` field that was never
      surfaced.

      Fix (surfacing — option 3 of #431): `rivet validate` now reports the schema
      set it ran against. Added `embedded::resolve_schema_provenance` (builds on
      the existing `schema_sources` classifier from REQ-177, adding the parsed
      `version` and auto-discovered bridges); validate prints a
      `Schemas: <name>@<version> (embedded|on-disk), …` line (text) and a
      `schemas` array (json). This makes an upgrade-induced change visible at the
      point it bites, without changing any validation semantics. (Pinning/gating
      — option 2 — remains a separate maintainer call on syntax.)

      Acceptance:
        - `cargo test -p rivet-core --lib embedded::tests::resolve_schema_provenance_reports_version_and_source`
          passes (on-disk reports its version + source "on-disk"; embedded
          reports a non-empty compiled-in version + source "embedded"; missing
          omitted).
        - `rivet validate` text output contains a line starting `Schemas: ` that
          lists each schema as `name@version (embedded|on-disk)`.
        - `rivet validate --format json` output contains a top-level `schemas`
          array of objects with `name`, `version`, `source`.
        - `rivet schema sources` reports the same effective set (requested
          schemas + auto-discovered bridges) WITH each schema's version, so the
          dedicated command is consistent with the validate surfacing (#484);
          text shows `name version source`, json entries include `version`.
        - `rivet validate` on the rivet repo still PASS.
    tags: [validate, schema, versioning, reproducibility, surfacing, dogfooding]
    fields:
      priority: should
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-177
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-05T13:29:57Z
  - id: REQ-206
    type: requirement
    title: "CI must warn when a schema's rules change without a `version:` bump (schema-version drift discipline)"
    status: implemented
    description: |
      Finding (issue #485, verified). Built-in schemas embed into the rivet
      binary, so a rules change with the `version:` field left unchanged
      silently alters validation for every embedded-schema consumer (#431) — and
      the version surfaced by `rivet validate` / `rivet schema sources` (#483)
      stays uninformative. Evidence: every shipped schema is `0.1.0` except
      `aspice` (`0.2.0`); `schemas/common.yaml` was changed by 8 commits
      (several rule-affecting) with zero version bumps.

      Fix: a `scripts/check-schema-version-bump.sh` that diffs `schemas/*.yaml`
      (excluding `migrations/`) against a base ref and reports any schema whose
      content changed but whose `version:` line did not. Warn-only by default
      (a comment/whitespace edit legitimately needs no bump), `STRICT=1` to
      enforce — mirroring the docs-check coverage gate. Wired into ci.yml as a
      `schema-version-bump` job on pull_request (fetch-depth 0 to diff the base),
      emitting GitHub `::warning::` annotations inline.

      Acceptance:
        - `scripts/check-schema-version-bump.sh f2ecf31^ f2ecf31` warns about
          `schemas/common.yaml` (changed without a version bump) and exits 0.
        - `STRICT=1 scripts/check-schema-version-bump.sh f2ecf31^ f2ecf31` exits 1.
        - A diff range that changes no schema prints `OK` and exits 0.
        - `.github/workflows/ci.yml` has a `schema-version-bump` job invoking the
          script on `pull_request`.
        - `rivet validate` on the rivet repo still PASS.
    tags: [ci, schema, versioning, drift, advisory-gate, dogfooding]
    fields:
      priority: should
      category: non-functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-054
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-05T15:26:15Z
  - id: REQ-207
    type: requirement
    title: "`rivet serve` must offer a presentation/clean mode that hides working-state chrome for recordings/screenshots"
    status: implemented
    description: |
      Finding (issue #482). The `rivet serve` dashboard header (context bar)
      always shows the working-state chrome — branch@sha, an `N uncommitted`
      badge, the absolute project path, and Reload/Print buttons. That chrome
      leaks into any recorded video or screenshot (it was visible in every
      frame of the intro-video capture), looking unprofessional and exposing
      in-progress branch names / machine-specific paths in published media.

      Fix: a `?presentation=1` (alias `?clean=1`) query param adds a
      `presentation` class on the document root, and CSS hides `.context-bar`
      while it is set. Client-side and set once on load, so it survives HTMX
      navigations (which only swap `#content`); the capture spec / a user just
      appends the param to the URL. No server plumbing; default (no param) is
      unchanged.

      Acceptance:
        - `cargo build -p rivet-cli` succeeds; `rivet serve` page source for
          `/artifacts?presentation=1` contains the `presentation` toggle script
          and the `.presentation .context-bar{display:none}` rule.
        - Playwright `presentation-mode.spec.ts` passes: `.context-bar` is
          visible without the param and hidden with `?presentation=1` (and
          `?clean=1`), and the class survives an HTMX nav.
        - `rivet validate` on the rivet repo still PASS.
    tags: [serve, dashboard, presentation, screenshots, video, ux, dogfooding]
    fields:
      priority: should
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: FEAT-001
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-05T21:19:32Z
  - id: REQ-208
    type: requirement
    title: "`rivet validate --new-since <ref>` shows only diagnostics NEW relative to a git ref (edit-verify loop + 'no new diagnostics' CI gate)"
    status: implemented
    description: |
      Finding (issue #488). After editing, the question that matters is "did MY
      change add any diagnostics?" — not "what are all N warnings in the repo?".
      `--explain <ID>` needs a known id and covers one artifact; `--baseline` is
      a named cumulative scope; neither answers "new since a git ref". So the
      edit-verify loop meant `validate --format json | jq` over hundreds of
      diagnostics.

      Fix: `rivet validate --new-since <REF>` validates the current tree and a
      detached git worktree of <REF> (each via a `rivet validate --format json`
      subprocess of the same binary), then prints the set-difference of
      diagnostics keyed on (severity, artifact_id, rule, message). Exits
      non-zero when any NEW diagnostic is at or above `--fail-on` — a ready
      "this PR adds no new diagnostics" gate, e.g.
      `rivet validate --new-since origin/main --fail-on warning`. The pure diff
      is factored into `diff_new_diagnostics` (unit-tested); the worktree is
      always cleaned up; no new runtime dependency (PID-scoped temp dir).

      Acceptance:
        - `cargo test -p rivet-cli --bin rivet new_since_diff_tests` passes
          (current-minus-baseline by the 4-field key; empty when current ⊆ baseline).
        - `rivet validate --new-since <REF>` prints a line
          `New diagnostics since <REF>: N (E errors, W warnings, I infos)` and
          lists only diagnostics absent at <REF>; `--new-since HEAD` reports 0.
        - Exits non-zero iff a NEW diagnostic is at/above `--fail-on`.
        - No `git worktree` is left behind after the run.
        - `rivet validate` on the rivet repo still PASS.
    tags: [validate, diagnostics, diff, ci-gate, edit-verify, dogfooding]
    fields:
      priority: should
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-007
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-06T01:49:27Z

  - id: REQ-210
    type: requirement
    title: "`rivet init --hooks` pre-commit runs `cargo fmt --check` (language-agnostic) so AI/dev commits don't pass locally and fail CI Format"
    status: implemented
    description: |
      Finding (issue #438). The single most common first-push CI failure is a
      formatting diff, and AI agents routinely hit it: they edit Rust, commit,
      and the commit "passes" locally because the only pre-commit gate is
      `rivet validate` — then CI's `cargo fmt --check` job goes red.

      Root cause: the hook installed by the SHIPPED command `rivet init --hooks`
      (`cmd_init_hooks`, rivet-cli/src/main.rs) only runs `rivet validate`. It
      does NOT run `cargo fmt --check`. The fmt-checking pre-commit hook that
      lives in THIS repo was installed by the dev-only `scripts/install-hooks.sh`
      (and the `templates/pre-commit/.pre-commit-config.yaml`), neither of which
      `rivet init --hooks` uses. So every project that adopts hooks the shipped
      way gets no formatting gate.

      Fix: `cmd_init_hooks` writes a pre-commit hook that, BEFORE the existing
      `rivet validate` step, runs `cargo fmt --all -- --check` — but only when
      both (a) `cargo` is on PATH and (b) the commit stages at least one `.rs`
      file (`git diff --cached --name-only --diff-filter=ACM | grep -q '\.rs$'`).
      The guard keeps the hook language-agnostic: a non-Rust rivet project (or a
      machine without cargo) skips the fmt step silently, so the hook never
      blocks commits in unrelated repositories. Mirrors the proven logic already
      in `scripts/install-hooks.sh`. Convenience only; CI remains the real gate
      (REQ-051) and `--no-verify` still bypasses it.

      Acceptance:
        - In a temp git repo with a `rivet.yaml`, `rivet init --hooks` writes a
          `.git/hooks/pre-commit` whose body contains `cargo fmt --all -- --check`
          guarded by a staged-`.rs` check (`grep -q '\.rs$'`) and a
          `command -v cargo` test.
        - The generated hook still runs `rivet validate --format json` and blocks
          on `errors > 0` (existing behaviour preserved).
        - `cargo test -p rivet-cli --test cli_commands` passes (full CLI suite).
        - `cargo fmt --check` + `cargo clippy --all-targets -- -D warnings` clean.
        - `rivet validate` on the rivet repo still PASS.
    tags: [hooks, init, cargo-fmt, ci-parity, dogfooding, dx]
    fields:
      priority: should
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-051
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-06T10:30:00Z

  - id: REQ-211
    type: requirement
    title: "`rivet list --format json --full` emits description/tags/fields in bulk so machine-readable queries don't need N× `get` or a raw YAML parse"
    status: implemented
    description: |
      Finding (#506, dogfooding). AGENTS.md/CLAUDE.md direct agents to
      `rivet list --format json` for "machine-readable artifact queries", but
      that output is summary-only — `id`, `type`, `title`, `status`, `links`.
      It omits `description`, `tags`, and `fields`, so the values agents most
      often need (e.g. a `priority` field, the acceptance text) are not
      reachable in bulk. The full per-artifact view exists via
      `rivet get <ID> --format json`, but recovering fields for the whole
      store means one subprocess per artifact (917 in this repo) or parsing
      the raw YAML — which is exactly the fallback the next-id memory had to
      use. `--variant` emits a merged `fields:` view but requires a variant
      config and applies overlays, so it is not a clean "all fields" export.

      Fix: a `--full` flag on `rivet list`. When set with `--format json`,
      each artifact entry also carries `description`, `tags`, and `fields`,
      mirroring `get`'s JSON shape. Additive and opt-in: default output is
      unchanged, so existing consumers are unaffected
      (`list-output.schema.json` is `additionalProperties: true`). The same
      change corrects long-standing drift in that schema: `links` was still
      typed as an integer count, but the actual output has been an array of
      `{type,target}` objects since #358 — the schema now matches.

      Acceptance:
        - `rivet list --format json --full` emits, for each artifact,
          `description` (string), `tags` (array), and `fields` (object) in
          addition to the summary keys; plain `rivet list --format json`
          still emits only the summary keys (no `description`/`tags`/`fields`).
        - The `--full` `fields` object for a given id equals the `fields`
          object from `rivet get <id> --format json`.
        - `cargo test -p rivet-cli --test cli_commands` passes (full suite),
          including a new test asserting the presence/absence of the rich keys.
        - `cargo fmt --check` + `cargo clippy --all-targets -- -D warnings` clean.
        - `rivet validate` on the rivet repo still PASS.
    tags: [list, json, query, machine-readable, dogfooding, dx]
    fields:
      priority: should
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-007
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-06T11:00:00Z

  - id: REQ-212
    type: requirement
    title: "CI runs `rivet validate` + `rivet commits` as a gating job — the traceability tool gates its own PRs (the in-CI mechanism of REQ-051)"
    status: implemented
    description: |
      Finding (bootstrap-verification audit of rivet-as-tool). The project
      that BUILDS the traceability tool did not gate its own PRs on
      traceability: `ci.yml` ran `rivet docs check` but neither `rivet
      validate` nor `rivet commits`. `rivet validate` ran only at release
      time (`release.yml`), so a graph error or an untraced code commit could
      land on `main` and only surface at release. This is the in-CI mechanism
      REQ-051 calls for; REQ-051 stays the broader (and still-`draft`) parent
      because it additionally requires the job be a *branch-protection
      required check* (operator action, see #436) and a `validate
      --check-hooks` flag (not yet implemented).

      A new `traceability` job in `ci.yml`:
        - Gate 1: `cargo run -p rivet-cli -- validate` — exits 1 on ERRORs
          (broken links, dup ids, bad targets, cardinality); coverage/lint
          WARNINGS do not fail (default `--fail-on error`).
        - Gate 2 (pull_request only): `rivet commits --range <base.sha>..HEAD
          --format json`, fail if `orphans` or `broken_refs` is non-empty —
          i.e. a non-exempt code commit missing trailers, or a trailer
          pointing at an unknown id. NOT `--strict`, which promotes
          whole-store "artifact has no commit coverage" to errors and so can
          never pass on a narrow PR range (calibrated: `--strict` over the 3
          clean recent merges still exits 1 on uncovered-artifact findings).

      Acceptance:
        - `.github/workflows/ci.yml` defines a `traceability` job that runs
          `rivet validate` and (on pull_request) the `rivet commits`
          orphan/broken-ref check; `actionlint` is clean (modulo the
          pre-existing custom runner-label false positives).
        - `rivet validate` exits non-zero on a tree with a dangling link and
          0 on the current rivet tree (PASS, warnings only) — verified.
        - The commit gate's bash passes on a clean range (orphans=0,
          broken=0 → exit 0) and fails on a range containing the reverted
          REQ-209 trailer (broken_refs=2 → exit 1) — verified locally.
        - `rivet validate` on the rivet repo still PASS.
    tags: [ci, traceability, gate, dogfooding, req-051]
    fields:
      priority: must
      category: non-functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-051
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-06T11:30:00Z
  - id: REQ-213
    type: requirement
    title: "`rivet schema presets` lists the declarable built-in schemas so the enableable standard set is discoverable (#510)"
    status: implemented
    description: |
      Finding (#510). A user bootstrapping a multi-standard project went to
      declare DO-178C / ISO 26262 / IEC 61508 / EN 50128 in `rivet.yaml`'s
      `schemas:` list and found "no preset for any of the four". In fact all
      four are embedded and declarable (since before v0.15.0) — but there was
      NO way to DISCOVER them: `rivet schema` had `list` (loaded types),
      `links`, `rules`, `list-json`, `migrate`, but nothing enumerating the
      available presets. So the declarable set was undocumented and the user
      reasonably concluded the schemas didn't exist.

      Fix: `rivet schema presets` (text + json) lists every embedded schema
      preset with its version, artifact-type count, and description. Needs no
      project (the user runs it before one exists). The embedded registry was
      refactored to a single source of truth (`EMBEDDED_SCHEMAS: &[(name,
      content)]`) so `embedded_schema`, `embedded_schema_names`, and this
      command can never drift apart.

      Acceptance:
        - `rivet schema presets --format json` run in a directory with NO
          rivet.yaml exits 0 and lists >= 21 presets, each with `name`,
          `version`, and `types`; the set includes `do-178c`, `iso-26262`,
          `iec-61508`, `en-50128`.
        - `cargo test -p rivet-core --lib embedded::tests::every_embedded_preset_resolves_and_parses`
          passes (every listed preset resolves + parses — no drift).
        - `cargo test -p rivet-cli --test cli_commands` passes (full suite,
          incl. schema_presets_lists_declarable_standards_without_a_project).
        - `cargo fmt --check` + `cargo clippy --all-targets -- -D warnings` clean.
        - `rivet validate` on the rivet repo still PASS.
    tags: [schema, presets, discoverability, dogfooding, dx]
    fields:
      priority: should
      category: functional
      baseline: v0.16.0-track
    links:
      - type: traces-to
        target: REQ-007
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-09T09:30:00Z

  - id: REQ-214
    type: requirement
    title: "`release` is a first-class, queryable optional field on requirement-family types (release-planning) (#512)"
    status: draft
    description: |
      Finding (#512). The release-planning workflow scopes a release as the
      set of artifacts carrying `release: vX.Y` and computes readiness by
      filtering on release + status. But `release` is not a schema-declared
      field on the requirement family (`requirement`, `sw-req`, `system-req`,
      `stakeholder-req`, `design-decision`), so: (a) `rivet validate` emits an
      INFO "field 'release' is not defined in schema"; and (b) a TOP-LEVEL
      `release:` key is invisible to the s-expression filter (`(= release
      "vX.Y")` → count 0). NB verified on main: a `release` value placed under
      `fields:` IS already queryable via `(= release ...)`; only the top-level
      form and the validate INFO remain.

      Fix (additive): declare `release` as an optional field on the
      requirement-family types in the relevant schemas (common/dev/aspice) so
      the field is blessed (no INFO) and uniformly queryable, regardless of
      whether the value is written top-level or under `fields:`.

      Acceptance:
        - `rivet schema show requirement` lists `release` among optional fields.
        - An artifact with `release: v0.1.0` validates with no INFO about an
          undeclared `release` field.
        - `rivet list --filter '(= release "v0.1.0")' --format json` matches it
          whether `release` is top-level or under `fields:`.
        - `rivet validate` on the rivet repo still PASS.
    tags: [schema, release-planning, query, dx]
    fields:
      priority: should
      category: functional
    links:
      - type: traces-to
        target: REQ-010
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-09T09:30:00Z

  - id: REQ-215
    type: requirement
    title: "Proof CI jobs (Verus, Miri) must not be perma-red from self-hosted-pool infra; run them on a config that actually completes (v0.17.0, #509)"
    status: implemented
    description: |
      Finding (v0.16.0 post-release CI). The required gates were all green and
      v0.16.0 shipped, yet the main CI run was red from two advisory/proof jobs
      failing on self-hosted-pool infrastructure, not on code:
        - Verus Proofs: perma-red at "Install Nix". The no-sudo daemonless
          DeterminateSystems installer (crafted for the self-hosted
          NoNewPrivileges runner) broke on a version bump and escalated to
          sudo, which fails there — the proofs never ran.
        - Miri: timed out at 30 min on the slow `lean-mem` self-hosted runner
          while the pool was contended clearing the #509 outage backlog.

      Fix (first v0.17.0 CI-reliability work, #509 theme):
        - Move Verus to `ubuntu-latest` + `cachix/install-nix-action@v30`,
          mirroring the green `rocq` job exactly. Fixes the install and removes
          the self-hosted-pool dependency. Stays continue-on-error (advisory).
        - Bump Miri `timeout-minutes` 30 → 45 for headroom on the lean-mem
          class under contention, without masking a real hang.

      Acceptance:
        - The `Verus Proofs` job runs on `ubuntu-latest` with
          `cachix/install-nix-action` and its check goes GREEN (the proofs run,
          no Install-Nix failure).
        - The `Miri` job `timeout-minutes` is 45.
        - `actionlint` clean (modulo pre-existing custom runner-label notes).
        - A subsequent main CI run is green on all non-`continue-on-error` jobs.
    tags: [ci, reliability, verus, miri, nix, dogfooding]
    fields:
      priority: should
      category: non-functional
      baseline: v0.17.0-track
    links:
      - type: traces-to
        target: REQ-051
    provenance:
      created-by: ai-assisted
      model: claude-opus-4-8
      timestamp: 2026-06-10T08:00:00Z
