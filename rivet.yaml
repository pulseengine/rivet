project:
  name: rivet
  version: "0.1.0"
  schemas:
    - common
    - dev
    - aadl

sources:
  - path: artifacts
    format: generic-yaml
  - path: arch
    format: aadl
    config:
      root-classifier: RivetSystem::Rivet.Impl

docs:
  - docs
  - arch

results: results
